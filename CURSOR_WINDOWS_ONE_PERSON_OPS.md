# Cursor Windows — finish One Person Ops

You are the implementation owner for **One Person Ops (OPO)** on my Windows machine. Use **Cursor Auto with Multitask** and work through all genuinely open OPO tasks to a working, independently reviewed, released and autonomous product. Do not stop after planning, an inventory, one test or a small prototype. Continue through the next eligible task without asking me to approve each step.

## Current authority and isolation

I have authorized OPO implementation, testing, ordinary releases, and routine project decisions. Treat those approval-dependent steps as approved; do not ask again merely because an older handover says to wait. This is a new, dedicated OPO assignment. You own OPO source and OPO Jira updates; other Windows release sessions must exclude OPO. The separate Astra Ultra session plans wider autonomy and does not gate your execution. Do not claim that another session has started unless you see its receiving-host evidence.

Use configured accounts and existing authorized services. Missing URLs, account access, provider requirements or spending limits are missing inputs, not approvals you can invent. Record the exact need, ask one precise question when necessary, and continue independent work. Do not make unspecified purchases, fabricate external outcomes, leak credentials, evade platform controls or change unrelated projects. Preserve working Kai/Pri services and leave Digital Temple and unrelated infrastructure alone.

## Start from the actual source and Jira

The source repository is `https://github.com/pri8771/one-person-ops`. The private control library is `https://github.com/pri8771/astra-bot-launch`; an existing clone is at `D:\Astra\launch-20260912-1943\control`. Fetch current remote refs into your own checkout; preserve existing dirty work. Use a dedicated branch/worktree and follow the source repository's applicable AGENTS.md and deployment instructions.

In the control library read only the OPO-relevant parts of `BOT_ROADMAPS.md`, `AUTONOMY_BACKLOG_GAP.md`, `REPO_CLOUD_AUDIT.md`, `CLOUD_PUBLICATION.md`, OPO task files, and `WINDOWS_RELEASE_EXECUTION.md`. This dedicated prompt supersedes that packet's old combined-parent ownership of OPO. The historical 22-task package did **not** fully decompose all autonomy implementation; add the missing OPO scope rather than declare success at its boundary. Mac-only files may not be present here; use available evidence and record specific gaps.

Jira on Windows is already authenticated. Find **all open OPO issues**, including To Do, In Progress, In Review and blocked items, using live summaries/descriptions, project associations and the evidence. Existing BOTS-112 and BOTS-124–127 are matching candidates, not an exhaustive list or permission to edit unrelated tickets. Do not infer ownership from the BOTS project alone. Produce a precise OPO key list before writes, then reconcile implementations, dependencies and acceptance. Reuse matching issues; add linked missing tasks when the complete product needs them. Preserve history, original estimates, genuine actuals, statuses and unrelated fields.

## The product to finish

OPO is an **agent-facing business and website**, with a companion social presence aimed at reaching real AI/agent operators and agents. Its goal is to discover and deliver something useful that can earn real revenue. It must support observable conversations, experiments and business decisions. A contract validator is an optional initial service, not the whole mission. Do not promise revenue or mistake crawler visits or synthetic tests for external customers.

Complete, reusing existing implementation where possible:

1. A coherent working website with human-readable explanations and versioned machine-readable discovery, service contracts, examples and a useful initial service. Choose a practical first offer using current evidence; document the choice and its measurable business goal.
2. Durable intake, threaded messages, responses, validation, deduplication, authentication/operator controls, moderation, privacy, retention and clear handling of untrusted agent input. Other agents may suggest changes; they cannot become privileged instructions or execute arbitrary code.
3. A complete offer/request/fulfillment flow, with transparent pricing and terms, payment/refund integration where the configured provider supports it, and operational failure states. Finish preview and eligible deployment. Resolve the exact owner-controlled hostname from actual configuration or ask; never invent it from a partial domain name.
4. A qualified social publishing and engagement workflow using the existing OPO account where verified. Publish useful, accurately represented material under the authorized mission. Track channel attribution and platform receipts. Separate known agents, claimed agents, bots/crawlers, humans and unknown actors; never claim reliable identity from guesswork. No fake followers, spam or fabricated engagement.
5. The autonomous operating loop: observe an event or due experiment, select an eligible action, record the business hypothesis and success criteria, execute within policy/budget, verify the receiving-system effect, measure results and record the next decision. Persist both successes and failed experiments in repository evidence and Jira.
6. Deterministic scheduling/event handling with **no idle LLM polling**. Use a local or inexpensive model only when reasoning is needed; code enforces permissions, limits, schemas and transitions. Persist jobs, leases/fencing, retries, deduplication and action receipts. An uncertain external effect must be reconciled before retrying. Bound retries, model/tool spend and context; use compact evidence rather than the entire chat history.
7. Owner nudges, specific missing-input questions, pause/resume, acknowledged policy revisions, alerts, kill switch, restart/recovery and backup/restore instructions. Windows must operate without the mobile Mac. Document the later R730 hub move without making it a launch prerequisite.

## Multitask and completion discipline

Split independent work into isolated source-owned lanes, with one integrator for shared files, deployments and Jira. Keep three implementation lanes at most initially and one distinct-model reviewer when useful; expand only if the paths, dependencies and machine capacity support it. Do not run competing full-project sessions. Reviewers must record their own findings and actual model identity; repair and rereview real defects. Do not invent the model selected behind Auto.

Run meaningful unit/integration checks, browser workflow checks, failure/restart tests, and a bounded end-to-end autonomous cycle. Label synthetic input. Verify any release at its real URL and external action through its actual receipt. Test count alone is not release evidence. Move an issue to Done only when its scope, tests, attributable independent review and acceptance are satisfied; otherwise retain the true status and a concrete remaining action.

Every source commit must reference one or more real OPO Jira keys. Capture actual author, reviewer, effort when exposed, machine, timestamps, test/review evidence, and measured usage when available. Unknowns remain unknown. Do not turn an estimate, elapsed wait or observation period into a fabricated worklog. Validate the native fields before populating them.

Maintain `CONTINUE.md`, `WORK_QUEUE.json` and a concise `SESSION_RETURN.md` in your owned execution-evidence folder from the first useful work onward; update after every meaningful unit and before context rollover. Include issue keys, source refs, exact changes, test/review results, deployment/runtime receipts, unresolved inputs and next eligible work. Push sanitized code and evidence to the correct remote so another machine can continue. A context rollover is a checkpoint, not completion.

Keep working until every current OPO issue is either honestly accepted as Done or has a specific evidenced external blocker, all missing product/autonomy scope is accounted for, and the working release has been verified. Report separately: implementation complete, independently reviewed, released, autonomous cycle verified, and actual business results. Do not substitute “ready to launch” for a verified launch.
