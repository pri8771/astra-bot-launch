# WHB executable task cards

Planning only. Source ref: `270ccb5a59e053df1ec5f415358b03f93bf9c69e`. Observed 2026-09-12T21:25:57.929857+00:00. Paths below are proposed implementation ownership in the named product repository, not files changed by this planner. Existing release workers own current source: assignment/handoff must serialize any overlap. Shared SH tasks gate later autonomy integration, not useful independent releases. New estimates exclude elapsed observations and shared implementation.

## WHB-01 — Bind source, current account and retained startup work

Qualify the known publisher once and reuse the accepted regression work before making surgical changes.

**Status:** reuse_and_verify. **Owner:** Windows combined release parent / admitted WHB lane. **Source:** https://github.com/pri8771/orchestrator.

**Owned proposed paths**

- wait-how-big-social/docs/source-admission.json
- wait-how-big-social/docs/mission-contract.json

**Dependencies:** none. **Inputs:** existing admitted source/charter only. **Effect gates:** none for offline work.

**Evidence:** WHB-E01, WHB-E02, WHB-E07. **Requirements:** WHB-O01.

**Implementation**

1. Consume release-worker return/current ref and BOTS-29 accepted source/review if supplied; compare to 270ccb5a and archive SHA rather than rerunning old bootstrap.
2. Retain source classification: 13 queued candidates, historical connected accounts/empty state, current implementation gaps; choose X-first relevant audience as mission with scale explainers an editable tactic.
3. Bind existing owner and exact X/Buffer organization/channel identity plus capability/metric readback requirements; separate optional Instagram/TikTok from initial launch.

**Deliverables**

- source admission
- mission contract
- current Jira matching proposal

**Tests / pass-fail checks**

- A historical BOTS-29 In Review/25m worklog is preserved and cannot imply current main contains the accepted fix.
- An account with same service but different identity fails resource binding.
- Missing publishing access keeps editorial and offline runtime tasks executable.

**Independent review**

A reviewer other than the implementer records actual identity, model/effort and host; distinct-model review when required by the admitted contract. Self-review does not qualify. Review exact Git commit and SHA256 manifest of changed source/assets/config; distinguish Windows CRLF worktree bytes from Git blobs. Any repair invalidates affected prior approval. At most two repair/review attempts per candidate within admitted budget; unresolved failure creates one actionable block and leaves independent work eligible.

**Acceptance**

- A single source/owner/mission contract and reuse plan exists without changing the active operator.
- Historical accounts and state are not asserted current.

**Rollback:** Disable this capability, retain immutable evidence/receipts, restore the last reviewed source/config version and reconcile attempted external effects before any retry.

**Next eligible action:** Designated owner matches the proposed scope to current Jira/source and assigns disjoint paths; then perform implementation step 1. External gates block only the named effects.

**New planning estimate:** 2–4 engineering hours. New planning estimate, not historical estimate or worklog. Reuse accepted returned implementation where its exact source passes this contract. Excludes elapsed observation windows, account waiting and shared SH implementation. Host/provider/account qualification or conflicting release-worker ownership can delay dependent effects; offline work remains eligible.

**Jira:** key null; matching candidates BOTS-117, BOTS-29, BOTS-59, BOTS-2. Writer: Windows combined release parent; planner outbox only. Compare current description, accepted source and dependencies; reuse/extend only exact matching scope, otherwise propose a linked missing-scope task. Preserve original estimates, actuals and historical evidence; record native readback before admitted effects.

## WHB-02 — Repair and package the existing publisher safely

Retain reusable Buffer behavior while fixing the packaged startup, bootstrap and account-selection gaps.

**Status:** reuse_and_verify. **Owner:** Windows combined release parent / admitted WHB lane. **Source:** https://github.com/pri8771/orchestrator.

**Owned proposed paths**

- wait-how-big-social/operator/whb_operator.py
- wait-how-big-social/operator/build_bundle.py
- wait-how-big-social/operator/operator_bundle.zip
- .github/workflows/wait-how-big-operator.yml
- tests/test_whb_startup.py

**Dependencies:** WHB-01. **Inputs:** WHB-I01. **Effect gates:** none for offline work.

**Evidence:** WHB-E02, WHB-E03, WHB-E04. **Requirements:** WHB-O02.

**Implementation**

1. Recover qualified BOTS-29 fix where available; otherwise materialize archive source as whb_operator.py without the stdlib operator collision and deterministically rebuild the ZIP with path safety/checksum verification.
2. Replace first-organization/first-service choice with exact expected organization/channel IDs and explicit enabled platform set, initially X only; unsupported/ambiguous identities fail closed. Retain media checking, queue caps and stop controls as reusable code.
3. Make manual dry-run the bootstrap mode, publish disabled until a verified capability/review grant is recorded, keep dry-run state isolated with simulated classification, and serialize workflow edits with release parent. No schedule/secret changes in this planning task.

**Deliverables**

- readable source and deterministic bundle
- bootstrap and identity tests
- updated workflow candidate

**Tests / pass-fail checks**

- Python 3.12 packaged no-key smoke test emits WAIT_HOW_BIG_NOT_CONFIGURED, never circular import; deterministically rebuilt archive hash is reproducible.
- Dry-run makes zero createPost calls and cannot create scheduled/public receipts in production state.
- Multiple organizations/same-service channels/wrong account/disabled platform and newly installed secret with no grant all block writes.

**Independent review**

A reviewer other than the implementer records actual identity, model/effort and host; distinct-model review when required by the admitted contract. Self-review does not qualify. Review exact Git commit and SHA256 manifest of changed source/assets/config; distinguish Windows CRLF worktree bytes from Git blobs. Any repair invalidates affected prior approval. At most two repair/review attempts per candidate within admitted budget; unresolved failure creates one actionable block and leaves independent work eligible.

**Acceptance**

- Existing publisher starts safely and exact-account X-only dry-run can be qualified.
- Scheduled publishing cannot begin simply because a usable credential appears.

**Rollback:** Disable this capability, retain immutable evidence/receipts, restore the last reviewed source/config version and reconcile attempted external effects before any retry.

**Next eligible action:** Designated owner matches the proposed scope to current Jira/source and assigns disjoint paths; then perform implementation step 1. External gates block only the named effects.

**New planning estimate:** 5–10 engineering hours. New planning estimate, not historical estimate or worklog. Reuse accepted returned implementation where its exact source passes this contract. Excludes elapsed observation windows, account waiting and shared SH implementation. Host/provider/account qualification or conflicting release-worker ownership can delay dependent effects; offline work remains eligible.

**Jira:** key null; matching candidates BOTS-29, BOTS-119. Writer: Windows combined release parent; planner outbox only. Compare current description, accepted source and dependencies; reuse/extend only exact matching scope, otherwise propose a linked missing-scope task. Preserve original estimates, actuals and historical evidence; record native readback before admitted effects.

## WHB-03 — Build a sourced editorial queue and scientific validation

Turn retained candidates into accurate, current and reviewable original explainers.

**Status:** reuse_and_verify. **Owner:** Windows combined release parent / admitted WHB lane. **Source:** https://github.com/pri8771/orchestrator.

**Owned proposed paths**

- wait-how-big-social/editorial/queue.schema.json
- wait-how-big-social/editorial/source-ledger.json
- wait-how-big-social/editorial/candidates/
- wait-how-big-social/editorial/validate_math.py
- tests/test_whb_editorial.py

**Dependencies:** WHB-01. **Inputs:** WHB-I04. **Effect gates:** none for offline work.

**Evidence:** WHB-E03, WHB-E05. **Requirements:** WHB-O03.

**Implementation**

1. Quarantine the 13 retained queue entries as unreviewed candidates; bind content ID/version to primary source URLs, retrieval dates, quoted/paraphrased scope, units, assumptions, uncertainty and asset rights. Do not fetch or play remote videos merely to assert old format claims.
2. Choose one excellent scale explainer plus two next candidates. Recompute comparisons with units/tolerance and distinguish mean/range/context; Earth-Moon spacing and million/billion seconds are candidate topics, not preapproved facts.
3. Generate original venue-appropriate caption/visual transcript/alt text, accessible typography and source links; move candidate through proposed/sourced/math_checked/editorial_review/approved with reviewer and exact asset hash.

**Deliverables**

- sourced editorial schema/ledger
- one reviewed candidate
- two next candidates
- math/accessibility checks

**Tests / pass-fail checks**

- Unit conversion, dimension mismatch, rounded headline, outdated source and misleading analogy fixtures are rejected or corrected.
- Every factual claim in the candidate maps to a dated primary source or explicitly labeled inference; missing rights/source blocks approval.
- One candidate and two next-ready proposals have readable transcript/alt text and exact source/asset manifests.

**Independent review**

A reviewer other than the implementer records actual identity, model/effort and host; distinct-model review when required by the admitted contract. Self-review does not qualify. Review exact Git commit and SHA256 manifest of changed source/assets/config; distinguish Windows CRLF worktree bytes from Git blobs. Any repair invalidates affected prior approval. At most two repair/review attempts per candidate within admitted budget; unresolved failure creates one actionable block and leaves independent work eligible.

**Acceptance**

- An independently reviewed accurate explainer is ready without needing account access.
- Queue candidates cannot publish on old relative timing alone.

**Rollback:** Disable this capability, retain immutable evidence/receipts, restore the last reviewed source/config version and reconcile attempted external effects before any retry.

**Next eligible action:** Designated owner matches the proposed scope to current Jira/source and assigns disjoint paths; then perform implementation step 1. External gates block only the named effects.

**New planning estimate:** 7–13 engineering hours. New planning estimate, not historical estimate or worklog. Reuse accepted returned implementation where its exact source passes this contract. Excludes elapsed observation windows, account waiting and shared SH implementation. Host/provider/account qualification or conflicting release-worker ownership can delay dependent effects; offline work remains eligible.

**Jira:** key null; matching candidates BOTS-120. Writer: Windows combined release parent; planner outbox only. Compare current description, accepted source and dependencies; reuse/extend only exact matching scope, otherwise propose a linked missing-scope task. Preserve original estimates, actuals and historical evidence; record native readback before admitted effects.

## WHB-04 — Implement account-bound publication and real receipts

A reviewed piece reaches the intended account once, with uncertainty handled before retry.

**Status:** reuse_and_verify. **Owner:** Windows combined release parent / admitted WHB lane. **Source:** https://github.com/pri8771/orchestrator.

**Owned proposed paths**

- wait-how-big-social/operator/publisher.py
- wait-how-big-social/operator/receipts.py
- wait-how-big-social/operator/reconcile.py
- tests/test_whb_publish.py

**Dependencies:** WHB-02, WHB-03. **Inputs:** WHB-I01, WHB-I02, WHB-I03. **Effect gates:** WHB-I01, WHB-I02, WHB-I03, WHB-I06.

**Evidence:** WHB-E03, WHB-E04, WHB-E08. **Requirements:** WHB-O04, WHB-A04, WHB-A10, WHB-A11.

**Implementation**

1. Qualify current official Buffer API fields, scopes, exact organization/channel identity, account entitlement and zero-spend route; default to one reviewed X canary. Separate platform reply/interaction capability from original publication.
2. Create durable attempt and content/action key before send; paginate provider history to coverage bound or block ambiguity rather than trust first 100 rows/caption equality. Record provider accepted/scheduled/sending/sent/error plus public account/post/permalink/content readback.
3. Use stable action identity for retries, store returned receipt atomically, reconcile accepted-before-crash and public failure; supported correction/cancel/delete effects require distinct action grants and retained lineage.

**Deliverables**

- qualified publisher
- durable/public receipts
- ambiguous-send and history tests

**Tests / pass-fail checks**

- Timeout after accepted post is located in provider history and does not repost; old post beyond first page still deduplicates.
- Buffer scheduled/dry-run ID does not pass public launch; actual account, content/hash and external permalink are verified.
- Wrong account, unavailable media, route schema drift or invalid grant fails closed and preserves the candidate.

**Independent review**

A reviewer other than the implementer records actual identity, model/effort and host; distinct-model review when required by the admitted contract. Self-review does not qualify. Review exact Git commit and SHA256 manifest of changed source/assets/config; distinguish Windows CRLF worktree bytes from Git blobs. Any repair invalidates affected prior approval. At most two repair/review attempts per candidate within admitted budget; unresolved failure creates one actionable block and leaves independent work eligible.

**Acceptance**

- One eligible real publication has both provider and correct-account destination receipts.
- Absent external access yields a reviewed ready candidate and explicit gate, never a launch claim.

**Rollback:** Disable this capability, retain immutable evidence/receipts, restore the last reviewed source/config version and reconcile attempted external effects before any retry.

**Next eligible action:** Designated owner matches the proposed scope to current Jira/source and assigns disjoint paths; then perform implementation step 1. External gates block only the named effects.

**New planning estimate:** 8–15 engineering hours. New planning estimate, not historical estimate or worklog. Reuse accepted returned implementation where its exact source passes this contract. Excludes elapsed observation windows, account waiting and shared SH implementation. Host/provider/account qualification or conflicting release-worker ownership can delay dependent effects; offline work remains eligible.

**Jira:** key null; matching candidates BOTS-119. Writer: Windows combined release parent; planner outbox only. Compare current description, accepted source and dependencies; reuse/extend only exact matching scope, otherwise propose a linked missing-scope task. Preserve original estimates, actuals and historical evidence; record native readback before admitted effects.

## WHB-05 — Add trustworthy audience measurement and corrections

Measure useful audience response and correct factual problems without fabricating reach.

**Status:** planned. **Owner:** Windows combined release parent / admitted WHB lane. **Source:** https://github.com/pri8771/orchestrator.

**Owned proposed paths**

- wait-how-big-social/operator/analytics.py
- wait-how-big-social/operator/corrections.py
- wait-how-big-social/docs/metric-contract.json
- tests/test_whb_metrics.py

**Dependencies:** WHB-03, WHB-04. **Inputs:** WHB-I03, WHB-I05, WHB-I07. **Effect gates:** WHB-I03, WHB-I05, WHB-I07, WHB-I06.

**Evidence:** WHB-E01, WHB-E05, WHB-E08. **Requirements:** WHB-O05, WHB-O06.

**Implementation**

1. Qualify allowed collection of per-post impressions/views/engagement/follows and repeat-interest indicators, with baseline/window/coverage/account/timezone and denominators. If API analytics unavailable, accept authenticated export/manual observation as dated evidence with a bounded collection owner.
2. Collect comments/feedback only through permitted read capability; link claim corrections to source/content version and severity. Prevent AI auto-replies until platform-specific eligibility exists; drafts and public correction candidates remain useful.
3. Maintain per-platform metrics and qualified engagement versus superficial activity; optional channel expansion records its own baseline and does not silently change experiment totals.

**Deliverables**

- metric collector and contract
- correction workflow
- coverage/error evidence

**Tests / pass-fail checks**

- Missing metric is unknown, not zero; unavailable profile visits cannot produce a conversion rate.
- Bot/self-test activity, duplicated exports and changed baselines do not inflate audience outcomes.
- A substantiated source correction pauses queued affected pieces, preserves original and publishes only a reviewed allowed correction.

**Independent review**

A reviewer other than the implementer records actual identity, model/effort and host; distinct-model review when required by the admitted contract. Self-review does not qualify. Review exact Git commit and SHA256 manifest of changed source/assets/config; distinguish Windows CRLF worktree bytes from Git blobs. Any repair invalidates affected prior approval. At most two repair/review attempts per candidate within admitted budget; unresolved failure creates one actionable block and leaves independent work eligible.

**Acceptance**

- Audience evidence is timestamped, interpretable and linked to the exact content/experiment.
- Correction and moderation actions retain lineage and platform authority.

**Rollback:** Disable this capability, retain immutable evidence/receipts, restore the last reviewed source/config version and reconcile attempted external effects before any retry.

**Next eligible action:** Designated owner matches the proposed scope to current Jira/source and assigns disjoint paths; then perform implementation step 1. External gates block only the named effects.

**New planning estimate:** 6–12 engineering hours. New planning estimate, not historical estimate or worklog. Reuse accepted returned implementation where its exact source passes this contract. Excludes elapsed observation windows, account waiting and shared SH implementation. Host/provider/account qualification or conflicting release-worker ownership can delay dependent effects; offline work remains eligible.

**Jira:** key null; matching candidates BOTS-122. Writer: Windows combined release parent; planner outbox only. Compare current description, accepted source and dependencies; reuse/extend only exact matching scope, otherwise propose a linked missing-scope task. Preserve original estimates, actuals and historical evidence; record native readback before admitted effects.

## WHB-06 — Ship editorial and publishing release with review and rollback

Deliver one excellent piece and a repeatable content pipeline rather than only a canary.

**Status:** planned. **Owner:** Windows combined release parent / admitted WHB lane. **Source:** https://github.com/pri8771/orchestrator.

**Owned proposed paths**

- wait-how-big-social/docs/releases/manifest.json
- wait-how-big-social/docs/releases/review.md
- wait-how-big-social/docs/releases/readback.json
- wait-how-big-social/docs/releases/rollback.md

**Dependencies:** WHB-02, WHB-03, WHB-04, WHB-05. **Inputs:** WHB-I01, WHB-I02, WHB-I03. **Effect gates:** WHB-I01, WHB-I02, WHB-I03, WHB-I06.

**Evidence:** WHB-E01, WHB-E03, WHB-E07. **Requirements:** WHB-O07.

**Implementation**

1. Review exact publisher/source/asset/workflow manifest independently, preserving startup regression evidence and identified account scope.
2. Hand reviewed candidate and operator release to the existing release parent for qualified deployment; verify one public X post and two next candidates under the new pipeline. Optional platforms remain independent effects.
3. Record kill/pause/cancel versus already-public correction semantics, prior source/queue snapshot and release receipt. Reverting code alone must not imply public post deletion.

**Deliverables**

- reviewed release
- public canary and next queue
- rollback evidence

**Tests / pass-fail checks**

- Source/hash reviewed is the source/hash used by the job and public piece.
- One X public post, provider receipt and measurement baseline link correctly; next candidates are separately sourced/reviewable.
- Pause prevents new enqueue; rollback retains existing post receipts and defines how any scheduled item is canceled only through allowed route.

**Independent review**

A reviewer other than the implementer records actual identity, model/effort and host; distinct-model review when required by the admitted contract. Self-review does not qualify. Review exact Git commit and SHA256 manifest of changed source/assets/config; distinguish Windows CRLF worktree bytes from Git blobs. Any repair invalidates affected prior approval. At most two repair/review attempts per candidate within admitted budget; unresolved failure creates one actionable block and leaves independent work eligible.

**Acceptance**

- Substantive editorial operation has a useful public piece, usable next pipeline and recovery.
- Release does not wait for SH runtime completion or all three historical channels.

**Rollback:** Disable this capability, retain immutable evidence/receipts, restore the last reviewed source/config version and reconcile attempted external effects before any retry.

**Next eligible action:** Designated owner matches the proposed scope to current Jira/source and assigns disjoint paths; then perform implementation step 1. External gates block only the named effects.

**New planning estimate:** 4–8 engineering hours. New planning estimate, not historical estimate or worklog. Reuse accepted returned implementation where its exact source passes this contract. Excludes elapsed observation windows, account waiting and shared SH implementation. Host/provider/account qualification or conflicting release-worker ownership can delay dependent effects; offline work remains eligible.

**Jira:** key null; matching candidates BOTS-119, BOTS-120. Writer: Windows combined release parent; planner outbox only. Compare current description, accepted source and dependencies; reuse/extend only exact matching scope, otherwise propose a linked missing-scope task. Preserve original estimates, actuals and historical evidence; record native readback before admitted effects.

## WHB-07 — Integrate durable editorial jobs and deterministic scheduling

Editorial decisions, publication and metric windows use durable single-owner state.

**Status:** planned. **Owner:** Windows combined release parent / admitted WHB lane. **Source:** https://github.com/pri8771/orchestrator.

**Owned proposed paths**

- wait-how-big-social/autonomy/state.py
- wait-how-big-social/autonomy/events.py
- wait-how-big-social/autonomy/schedule.json
- tests/test_whb_events.py

**Dependencies:** WHB-02, WHB-03, SH-02, SH-03, SH-04. **Inputs:** existing admitted source/charter only. **Effect gates:** none for offline work.

**Evidence:** WHB-E03, WHB-E04, WHB-E06. **Requirements:** WHB-A01, WHB-A02.

**Implementation**

1. Map candidate.sourced, review.approved, source.changed, publish.accepted, publish.verified, publish.failed, metrics.updated, experiment.due and direction.changed to durable source-versioned events.
2. Bind candidates/experiments/attempts/receipts to transactional mission state and fencing epoch; replace loose state JSON as authoritative state through a versioned one-time import preserving historical emptiness/receipts.
3. Use deterministic due checks with next_check_at/evidence_version/decision_fingerprint, per-channel posting capacity and explicit enabled channels. No inference while queue/metrics/source are unchanged and decision not due.

**Deliverables**

- durable event adapter
- state migration
- idle/race/replay tests

**Tests / pass-fail checks**

- Two hosts racing an approved piece produce one eligible owner; stale owner cannot publish after takeover.
- State migration replay is idempotent and labels historical empty/dry-run records correctly.
- 24-hour simulated idle plus repeated identical metrics events generates zero model calls.

**Independent review**

A reviewer other than the implementer records actual identity, model/effort and host; distinct-model review when required by the admitted contract. Self-review does not qualify. Review exact Git commit and SHA256 manifest of changed source/assets/config; distinguish Windows CRLF worktree bytes from Git blobs. Any repair invalidates affected prior approval. At most two repair/review attempts per candidate within admitted budget; unresolved failure creates one actionable block and leaves independent work eligible.

**Acceptance**

- WHB can resume editorial/publication/evaluation states from durable events safely.
- Schedule is cheap and mission-specific instead of repeated generative cron.

**Rollback:** Disable this capability, retain immutable evidence/receipts, restore the last reviewed source/config version and reconcile attempted external effects before any retry.

**Next eligible action:** Designated owner matches the proposed scope to current Jira/source and assigns disjoint paths; then perform implementation step 1. External gates block only the named effects.

**New planning estimate:** 7–13 engineering hours. New planning estimate, not historical estimate or worklog. Reuse accepted returned implementation where its exact source passes this contract. Excludes elapsed observation windows, account waiting and shared SH implementation. Host/provider/account qualification or conflicting release-worker ownership can delay dependent effects; offline work remains eligible.

**Jira:** key null; matching candidates none yet. Writer: Windows combined release parent; planner outbox only. Compare current description, accepted source and dependencies; reuse/extend only exact matching scope, otherwise propose a linked missing-scope task. Preserve original estimates, actuals and historical evidence; record native readback before admitted effects.

## WHB-08 — Implement audience experiments and justified next content decisions

The operator adapts topic, format, profile and channel tactics using actual audience evidence.

**Status:** planned. **Owner:** Windows combined release parent / admitted WHB lane. **Source:** https://github.com/pri8771/orchestrator.

**Owned proposed paths**

- wait-how-big-social/autonomy/decisions.py
- wait-how-big-social/autonomy/experiments.py
- wait-how-big-social/autonomy/budgets.json
- tests/test_whb_decisions.py

**Dependencies:** WHB-05, WHB-07, SH-05, SH-07, SH-14. **Inputs:** WHB-I05, WHB-I08. **Effect gates:** WHB-I05, WHB-I06, WHB-I08.

**Evidence:** WHB-E01, WHB-E05, WHB-E08. **Requirements:** WHB-A03, WHB-A05, WHB-A16, WHB-O08.

**Implementation**

1. Freeze first hypothesis: whether one concrete original scale explanation earns relevant responses/return interest; select its metric and minimum informative evidence from actual baseline, not universal sample thresholds.
2. Record alternative topic/hook/format, expected benefit, source/review cost, quality guardrails, zero-cash budget and window before action. Subsequent profile/image/channel changes require exact saved variants and independent grants, not broad source drift.
3. Evaluate supported/unsupported/inconclusive/invalidated and choose adopt/continue/change/extend/revert/stop; stop low-value repeated posting and wait cheaply if evidence cannot support a decision. Monetization is a later explicit offer hypothesis, not assumed sponsor income.

**Deliverables**

- decision/experiment adapters
- first and next experiment contracts
- limit/evaluation tests

**Tests / pass-fail checks**

- Publication alone never equals audience growth; before/after change is not represented as causal proof.
- Changing topic and avatar together is a bundled experiment with limits; baseline and original thresholds remain immutable.
- Budget exhaustion, unavailable metrics and unchanged input cause bounded wait/block with no paid model fallback.

**Independent review**

A reviewer other than the implementer records actual identity, model/effort and host; distinct-model review when required by the admitted contract. Self-review does not qualify. Review exact Git commit and SHA256 manifest of changed source/assets/config; distinguish Windows CRLF worktree bytes from Git blobs. Any repair invalidates affected prior approval. At most two repair/review attempts per candidate within admitted budget; unresolved failure creates one actionable block and leaves independent work eligible.

**Acceptance**

- The second content decision is attributable to actual observed evidence and original audience goal.
- Low traffic/negative results are valid outcomes with explicit next choices, not manufactured success.

**Rollback:** Disable this capability, retain immutable evidence/receipts, restore the last reviewed source/config version and reconcile attempted external effects before any retry.

**Next eligible action:** Designated owner matches the proposed scope to current Jira/source and assigns disjoint paths; then perform implementation step 1. External gates block only the named effects.

**New planning estimate:** 7–13 engineering hours. New planning estimate, not historical estimate or worklog. Reuse accepted returned implementation where its exact source passes this contract. Excludes elapsed observation windows, account waiting and shared SH implementation. Host/provider/account qualification or conflicting release-worker ownership can delay dependent effects; offline work remains eligible.

**Jira:** key null; matching candidates BOTS-121, BOTS-122. Writer: Windows combined release parent; planner outbox only. Compare current description, accepted source and dependencies; reuse/extend only exact matching scope, otherwise propose a linked missing-scope task. Preserve original estimates, actuals and historical evidence; record native readback before admitted effects.

## WHB-09 — Bind publisher and corrections to shared effect controls

Autonomous publishing inherits review, account, action-key and uncertain-effect protection.

**Status:** planned. **Owner:** Windows combined release parent / admitted WHB lane. **Source:** https://github.com/pri8771/orchestrator.

**Owned proposed paths**

- wait-how-big-social/autonomy/actions.py
- tests/test_whb_autonomous_actions.py

**Dependencies:** WHB-04, WHB-05, WHB-07, SH-06. **Inputs:** WHB-I01, WHB-I02, WHB-I03, WHB-I07. **Effect gates:** WHB-I01, WHB-I02, WHB-I03, WHB-I06, WHB-I07.

**Evidence:** WHB-E03, WHB-E04, WHB-E08. **Requirements:** WHB-A04, WHB-A10, WHB-A11.

**Implementation**

1. Register create/schedule/cancel/correct/profile-change capabilities separately by exact account/content/version, approved route, budget and scope. Public content review approval must match bytes at execution.
2. Adapt existing publisher/receipt reconciliation to shared action intent/attempt/receipt protocol; treat provider acknowledgement, public readback and audience measurement as separate states.
3. Require direction revision, owner fence and registered experiment evidence immediately before effects; failures create bounded reconciliation work and do not silently switch accounts or platforms.

**Deliverables**

- effect capability integration
- fault-injection and review binding evidence

**Tests / pass-fail checks**

- Review-to-publish content change, old owner epoch, changed destination and expired grant all block.
- Accepted-before-crash public piece reconciles once; duplicate delivery of same action does not create another post.
- An unqualified AI reply/profile action is rejected while an already-qualified original-post capability can remain eligible.

**Independent review**

A reviewer other than the implementer records actual identity, model/effort and host; distinct-model review when required by the admitted contract. Self-review does not qualify. Review exact Git commit and SHA256 manifest of changed source/assets/config; distinguish Windows CRLF worktree bytes from Git blobs. Any repair invalidates affected prior approval. At most two repair/review attempts per candidate within admitted budget; unresolved failure creates one actionable block and leaves independent work eligible.

**Acceptance**

- Every autonomous WHB effect is bound to reviewed bytes, exact resource and durable outcome.
- Unknown result never triggers blind reposting.

**Rollback:** Disable this capability, retain immutable evidence/receipts, restore the last reviewed source/config version and reconcile attempted external effects before any retry.

**Next eligible action:** Designated owner matches the proposed scope to current Jira/source and assigns disjoint paths; then perform implementation step 1. External gates block only the named effects.

**New planning estimate:** 5–10 engineering hours. New planning estimate, not historical estimate or worklog. Reuse accepted returned implementation where its exact source passes this contract. Excludes elapsed observation windows, account waiting and shared SH implementation. Host/provider/account qualification or conflicting release-worker ownership can delay dependent effects; offline work remains eligible.

**Jira:** key null; matching candidates none yet. Writer: Windows combined release parent; planner outbox only. Compare current description, accepted source and dependencies; reuse/extend only exact matching scope, otherwise propose a linked missing-scope task. Preserve original estimates, actuals and historical evidence; record native readback before admitted effects.

## WHB-10 — Integrate owner nudges, questions and Jira documentation

Changed direction can pause publishing and safely resume once exact missing inputs arrive.

**Status:** planned. **Owner:** Windows combined release parent / admitted WHB lane. **Source:** https://github.com/pri8771/orchestrator.

**Owned proposed paths**

- wait-how-big-social/autonomy/direction.py
- wait-how-big-social/autonomy/projection.py
- tests/test_whb_direction.py

**Dependencies:** WHB-07, SH-08, SH-09. **Inputs:** WHB-I06. **Effect gates:** WHB-I06.

**Evidence:** WHB-E01, WHB-E07. **Requirements:** WHB-A06, WHB-A07, WHB-A08.

**Implementation**

1. Consume authenticated WHB direction revisions and acknowledge exact consumed version; example pause scheduled posts, prioritize sourced astronomy and keep TikTok disabled. Preserve previous experiment evidence.
2. Reuse account/resource questions and issue one minimum needs_input record per missing item; validate responding owner and exact mission/resource before resuming. Source/review and offline packaging continue while access is absent.
3. Write experiment-before-action, outcome, review and documentation outboxes for the combined Windows parent only. Persist retries/dedupe and require native matching/source admission; preserve BOTS-29 historical actuals and all original estimates.

**Deliverables**

- direction and question integration
- Jira/docs outbox adapter
- restart/dedupe tests

**Tests / pass-fail checks**

- Repeated pause input is acknowledged once; queued post cannot execute under older direction.
- Answer naming a different organization fails validation and cannot authorize publication.
- Writer outage leaves durable outbox and blocks only its unregistered effects; recovered readback resumes once.

**Independent review**

A reviewer other than the implementer records actual identity, model/effort and host; distinct-model review when required by the admitted contract. Self-review does not qualify. Review exact Git commit and SHA256 manifest of changed source/assets/config; distinguish Windows CRLF worktree bytes from Git blobs. Any repair invalidates affected prior approval. At most two repair/review attempts per candidate within admitted budget; unresolved failure creates one actionable block and leaves independent work eligible.

**Acceptance**

- WHB acknowledges owner direction and resumes exact work without recurring input questions.
- No planner or mission child becomes a competing Jira writer.

**Rollback:** Disable this capability, retain immutable evidence/receipts, restore the last reviewed source/config version and reconcile attempted external effects before any retry.

**Next eligible action:** Designated owner matches the proposed scope to current Jira/source and assigns disjoint paths; then perform implementation step 1. External gates block only the named effects.

**New planning estimate:** 5–9 engineering hours. New planning estimate, not historical estimate or worklog. Reuse accepted returned implementation where its exact source passes this contract. Excludes elapsed observation windows, account waiting and shared SH implementation. Host/provider/account qualification or conflicting release-worker ownership can delay dependent effects; offline work remains eligible.

**Jira:** key null; matching candidates none yet. Writer: Windows combined release parent; planner outbox only. Compare current description, accepted source and dependencies; reuse/extend only exact matching scope, otherwise propose a linked missing-scope task. Preserve original estimates, actuals and historical evidence; record native readback before admitted effects.

## WHB-11 — Add Windows recovery, bounded retries, alerts and isolation

WHB operates independently and survives faults without cross-account effects.

**Status:** planned. **Owner:** Windows combined release parent / admitted WHB lane. **Source:** https://github.com/pri8771/orchestrator.

**Owned proposed paths**

- wait-how-big-social/autonomy/windows-service.json
- wait-how-big-social/autonomy/ops.py
- wait-how-big-social/docs/restore-runbook.md
- tests/test_whb_recovery.py

**Dependencies:** WHB-07, WHB-09, WHB-10, SH-10, SH-11, SH-12, SH-13. **Inputs:** WHB-I08, WHB-I09. **Effect gates:** WHB-I09.

**Evidence:** WHB-E03, WHB-E04, WHB-E06. **Requirements:** WHB-A09, WHB-A12, WHB-A13, WHB-A14, WHB-A15, WHB-A18.

**Implementation**

1. Use shared Windows process-tree execution and host-local state/secret references; do not copy POSIX procutil.py unchanged or modify existing release scheduler without recorded ownership handoff.
2. Define finite transport retry versus identity/content/permission stop, max two reviewed repair attempts, timeout, storage backup/restore and alerts for rejected posts, unresolved send, expired account capability and exhausted budget.
3. Exercise Mac-disconnected operation and optional R730 fenced takeover with portable state/evidence manifest and locally injected credential references; retain Windows fallback, keep i9 deferred.

**Deliverables**

- Windows operations profile
- restore/isolation evidence
- optional migration acceptance

**Tests / pass-fail checks**

- Child/grandchild held-pipe fault terminates within bound and preserves actionable failed attempt.
- Backup restore after unknown publish reconciles provider/public receipt before new ownership posts.
- Secret-bearing exception fixture is redacted; unrelated OPO account cannot be selected; unchanged alerts do not repeat.

**Independent review**

A reviewer other than the implementer records actual identity, model/effort and host; distinct-model review when required by the admitted contract. Self-review does not qualify. Review exact Git commit and SHA256 manifest of changed source/assets/config; distinguish Windows CRLF worktree bytes from Git blobs. Any repair invalidates affected prior approval. At most two repair/review attempts per candidate within admitted budget; unresolved failure creates one actionable block and leaves independent work eligible.

**Acceptance**

- Windows scheduling/recovery/stop and alerting have mission-specific evidence.
- Optional R730 portability is defined and tested only when target is qualified; release continues independently.

**Rollback:** Disable this capability, retain immutable evidence/receipts, restore the last reviewed source/config version and reconcile attempted external effects before any retry.

**Conditional dependency:** Optional R730 migration only requires SH-16 qualified target/fence/restore/receipt/rollback acceptance. Independent Windows recovery and runtime acceptance remain eligible without migration.

**Next eligible action:** Designated owner matches the proposed scope to current Jira/source and assigns disjoint paths; then perform implementation step 1. External gates block only the named effects.

**New planning estimate:** 6–12 engineering hours. New planning estimate, not historical estimate or worklog. Reuse accepted returned implementation where its exact source passes this contract. Excludes elapsed observation windows, account waiting and shared SH implementation. Host/provider/account qualification or conflicting release-worker ownership can delay dependent effects; offline work remains eligible.

**Jira:** key null; matching candidates none yet. Writer: Windows combined release parent; planner outbox only. Compare current description, accepted source and dependencies; reuse/extend only exact matching scope, otherwise propose a linked missing-scope task. Preserve original estimates, actuals and historical evidence; record native readback before admitted effects.

## WHB-12 — Prove repeated autonomous editorial cycles

Demonstrate observation through next decision twice with real correct-account receipts and independent review.

**Status:** planned. **Owner:** Windows combined release parent / admitted WHB lane. **Source:** https://github.com/pri8771/orchestrator.

**Owned proposed paths**

- wait-how-big-social/docs/acceptance/cycles.json
- wait-how-big-social/docs/acceptance/review.md
- tests/test_whb_full_cycle.py

**Dependencies:** WHB-06, WHB-08, WHB-09, WHB-10, WHB-11, SH-15. **Inputs:** WHB-I01, WHB-I02, WHB-I03, WHB-I05, WHB-I06, WHB-I08. **Effect gates:** WHB-I01, WHB-I02, WHB-I03, WHB-I05, WHB-I06, WHB-I08.

**Evidence:** WHB-E01, WHB-E03, WHB-E07, WHB-E08. **Requirements:** WHB-A17, WHB-O08.

**Implementation**

1. Run labeled offline cycle and crash/owner-revision fixtures on exact reviewed source; archive pipeline lineage from factual source to publication version.
2. Under qualified zero-cash account grants, observe baseline, register an experiment, select/review/publish one excellent piece, verify actual public account/content/permalink and collect the declared measurement window.
3. Evaluate and autonomously choose the next justified experiment without a new launch prompt; complete and verify a second real receiving-system cycle, owner pause/resume and crash/restart. If evidence remains insufficient, persist bounded waiting with a next trigger. Waiting is a valid operational state, but runtime autonomy acceptance remains incomplete until the second real allowed cycle completes; audience/business validation is a separate evidence outcome.

**Deliverables**

- two-cycle evidence
- public receipts
- owner/restart tests
- independent exact-source review
- next decision

**Tests / pass-fail checks**

- Two cycle traces connect observation -> decision -> registered reviewed action -> actual public receipt -> evaluation -> next decision.
- Reviewer identity/model/source hash and verification independently match the published candidate; synthetic fixtures cannot satisfy real receipt acceptance.
- Pause after approval and crash after post acceptance cause no duplicate or wrong-account post; idle periods consume zero inference.

**Independent review**

A reviewer other than the implementer records actual identity, model/effort and host; distinct-model review when required by the admitted contract. Self-review does not qualify. Review exact Git commit and SHA256 manifest of changed source/assets/config; distinguish Windows CRLF worktree bytes from Git blobs. Any repair invalidates affected prior approval. At most two repair/review attempts per candidate within admitted budget; unresolved failure creates one actionable block and leaves independent work eligible.

**Acceptance**

- WHB runtime autonomy is demonstrated independently from content performance and growth.
- Real audience growth/return interest is reported only from observed metrics, including inconclusive or negative results.

**Rollback:** Disable this capability, retain immutable evidence/receipts, restore the last reviewed source/config version and reconcile attempted external effects before any retry.

**Next eligible action:** Designated owner matches the proposed scope to current Jira/source and assigns disjoint paths; then perform implementation step 1. External gates block only the named effects.

**New planning estimate:** 6–12 engineering hours. New planning estimate, not historical estimate or worklog. Reuse accepted returned implementation where its exact source passes this contract. Excludes elapsed observation windows, account waiting and shared SH implementation. Host/provider/account qualification or conflicting release-worker ownership can delay dependent effects; offline work remains eligible.

**Jira:** key null; matching candidates BOTS-59, BOTS-122. Writer: Windows combined release parent; planner outbox only. Compare current description, accepted source and dependencies; reuse/extend only exact matching scope, otherwise propose a linked missing-scope task. Preserve original estimates, actuals and historical evidence; record native readback before admitted effects.
