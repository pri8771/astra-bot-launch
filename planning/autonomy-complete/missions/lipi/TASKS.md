# Lipi Standard executable task cards

All tasks are proposed incremental work. Existing accepted work is reused; runtime/business completion is not claimed. Product paths are proposed future ownership reservations; select exact canonical source and serialize conflicting paths through admission. Estimates are engineering effort only; observation/shipping/owner waiting is excluded.

## LIPI-01 — Bind current source to accepted Lipi residuals

Reuse LIPI-63–87 local work while proving which source and external residuals the next executor actually owns.

- Status: reuse_and_verify
- Owner: Suggested Lipi executor, admitted by Lipi coordinator
- Source: pri8771/lipi-standard-store
- Owned paths: `ops/autonomy/reconciliation/`; `ops/autonomy/admission/lipi-source.json`
- Evidence: LIPI-E01, LIPI-E02, LIPI-E03, LIPI-E07, LIPI-E08
- Dependencies: None
- Inputs: LIPI-I01; LIPI-I09
- Effect gates: No external effect authorized by this task preparation
- Requirements: LIPI-O01

Implementation steps:

1. Compare clean main 0d896016f2fa113ce07e504bc5d6528717598b5f with retained 37c447bbd90b8aac86f768a480b25eb4d8fc6c44 and cloud session4 branch 52954f1f3d07f160a414713250923993a7d5f3f5; request only missing named return/contract artifacts through existing handoff route, without querying the old Mac.
2. Build a 25-row local-contract/external-acceptance matrix using current Jira metadata and exact source hashes; preserve local satisfied claims separately from owner/provider residuals.
3. Reuse validated 31-check main and control-plane baseline; qualify a Windows UTF8/Pillow interpreter without installing, and bind incoming release-worker changes through a new source revision before any edit.

Deliverables:

- 25-row acceptance reconciliation
- source/admission manifest with path reservations

Pass/fail checks:

- Reject a missing retained artifact as unknown, not accepted-current or absent implementation.
- Main structural checks reproduce31 passes and no publication flag; no dirty worker checkout is modified.

Acceptance:

- Each LIPI-63–87 row says reused artifact, source availability, external residual and next owner.
- Jira key remains null until designated writer returns current readback.

Completion rule: A blocked/missing-input/failure record may finish a preparation subunit only. This task is not capability/release complete until its positive checks and relevant real effect acceptance pass. Disabled unrelated effect classes do not block a qualified allowed class; claimed scope is explicit.

Review: Independent reviewer other than implementing author; record actual reviewer identity, model/version and host. Cultural reviewer identity is additionally required for Guru claims; no invented reviewer. Bind source commit, task/spec SHA256, artifact hashes, policy revision and receiving-system receipts; any material change invalidates affected approval. At most two targeted repair/review attempts per admitted task, or a stricter existing limit. Unresolved failure becomes needs_input/blocked with evidence; never an unbounded loop.

Rollback: Discard only unadmitted reconciliation proposal; preserve evidence and accepted prior artifacts.

Next eligible action: Run LIPI-02 and separate variant/economics preparation on the admitted source; missing external evidence blocks only its dependent effect.

Planning estimate: 2–5 engineering hours. New incremental planning range, not original Jira estimate or worklog. Reuse accepted assets; elapsed shipping, review waiting, observation windows and provider verification are excluded. Dependency risk: Retained exact revision not served by remote; writer and targeted return delivery unknown.

Jira: actual key null. Candidates LIPI-63, LIPI-64, LIPI-65, LIPI-66, LIPI-67, LIPI-68, LIPI-69, LIPI-70, LIPI-71, LIPI-72, LIPI-73, LIPI-74, LIPI-75, LIPI-76, LIPI-77, LIPI-78, LIPI-79, LIPI-80, LIPI-81, LIPI-82, LIPI-83, LIPI-84, LIPI-85, LIPI-86, LIPI-87. Designated writer compares current scope and retained acceptance, reuses matching issue or adds only genuine residual scope, preserves all original estimates/actuals, and returns native key/readback bound to this specification. Writer: Lipi writer designation unresolved: LIPI-I09.

## LIPI-02 — Resolve capsule membership and design acceptance

Produce one exact coherent capsule proposal using existing production art and approved brand direction.

- Status: reuse_and_verify
- Owner: Suggested Lipi executor, admitted by Lipi coordinator
- Source: pri8771/lipi-standard-store
- Owned paths: `ops/autonomy/capsule/`
- Evidence: LIPI-E02, LIPI-E04, LIPI-E07
- Dependencies: LIPI-01
- Inputs: LIPI-I02
- Effect gates: No external effect authorized by this task preparation
- Requirements: LIPI-O02

Implementation steps:

1. Retain Meridian, Open Current and Threshold Study12×16 as the proposal, with Quiet Route explicitly retain/exclude/conditional; do not promote proposal to owner approval.
2. Review production files, dimensions, placement, mockup bindings and usage claims against exact product contracts; preserve English-first premium American direction and excluded Hindi/Devanagari assets.
3. Present owner one concrete membership decision with all remaining cost/sample facts; apply only that signed/versioned decision and identify replacement work for rejected items.

Deliverables:

- capsule membership and design matrix
- owner decision packet

Pass/fail checks:

- A conditional or rejected SKU cannot enter publication/sample payloads.
- Every visual references exact art hash and variant; legacy glyph assets remain outside public candidate.

Acceptance:

- Membership state is explicit for every candidate.
- Owner decision receipt or a single persisted unanswered question exists without blocking independent engineering.

Completion rule: A blocked/missing-input/failure record may finish a preparation subunit only. This task is not capability/release complete until its positive checks and relevant real effect acceptance pass. Disabled unrelated effect classes do not block a qualified allowed class; claimed scope is explicit.

Review: Independent reviewer other than implementing author; record actual reviewer identity, model/version and host. Cultural reviewer identity is additionally required for Guru claims; no invented reviewer. Bind source commit, task/spec SHA256, artifact hashes, policy revision and receiving-system receipts; any material change invalidates affected approval. At most two targeted repair/review attempts per admitted task, or a stricter existing limit. Unresolved failure becomes needs_input/blocked with evidence; never an unbounded loop.

Rollback: Restore prior proposal revision; no public catalog mutation belongs to this task.

Next eligible action: Proceed to LIPI-03–06 for retained/conditional candidates and LIPI-24 independently.

Planning estimate: 2–4 engineering hours. New incremental planning range, not original Jira estimate or worklog. Reuse accepted assets; elapsed shipping, review waiting, observation windows and provider verification are excluded. Dependency risk: Owner membership answer missing.

Jira: actual key null. Candidates LIPI-65. Designated writer compares current scope and retained acceptance, reuses matching issue or adds only genuine residual scope, preserves all original estimates/actuals, and returns native key/readback bound to this specification. Writer: Lipi writer designation unresolved: LIPI-I09.

## LIPI-03 — Qualify Meridian cap provider identity

Reconcile the existing product instead of duplicating supplier templates or drafts.

- Status: reuse_and_verify
- Owner: Suggested Lipi executor, admitted by Lipi coordinator
- Source: pri8771/lipi-standard-store
- Owned paths: `ops/autonomy/providers/meridian/`
- Evidence: LIPI-E03, LIPI-E04, LIPI-E07
- Dependencies: LIPI-01
- Inputs: LIPI-I03
- Effect gates: LIPI-G01
- Requirements: LIPI-O03

Implementation steps:

1. Use retained identity only as the search anchor: Yupoong6245CM, template106647957, four historical colors Black/Navy/Cranberry/Spruce. Refresh exact shop/store, product, variant, SKU, size/color, technique, placement, art hash and provider region.
2. Read current Shopify Draft/channel/media/retail values and Printful sync-variant/fulfillment status through the qualified route; compare both sides and isolate each mismatch.
3. Prepare an exact before/after repair for only mismatched private mapping or media. Reuse existing grants if sufficient; live changes require the applicable scoped capability and hash-bound receipt.

Effect-specific gate applicability:

- LIPI-G01: External shop/provider read or private mapping change only: intended account, current scoped supported route and any required existing grant.

Deliverables:

- variant-level joined provider mapping
- dated cost/availability/media evidence
- bounded correction proposal

Pass/fail checks:

- Variant-count, SKU, color/size, production hash and one-to-one mapping comparisons fail on missing/duplicate/mismatched IDs.
- Reopen/read both receiving systems after any authorized correction; Draft status and zero channels stay explicit until launch.

Acceptance:

- Each intended variant either has current matching mapping/fulfillment evidence or an exact blocked reason.
- No source hint or mockup is treated as current provider truth.

Completion rule: A blocked/missing-input/failure record may finish a preparation subunit only. This task is not capability/release complete until its positive checks and relevant real effect acceptance pass. Disabled unrelated effect classes do not block a qualified allowed class; claimed scope is explicit.

Review: Independent reviewer other than implementing author; record actual reviewer identity, model/version and host. Cultural reviewer identity is additionally required for Guru claims; no invented reviewer. Bind source commit, task/spec SHA256, artifact hashes, policy revision and receiving-system receipts; any material change invalidates affected approval. At most two targeted repair/review attempts per admitted task, or a stricter existing limit. Unresolved failure becomes needs_input/blocked with evidence; never an unbounded loop.

Rollback: Restore only the exact private field mapping from captured prior state if reversible and authorized; do not delete provider records or unpublish automatically.

Next eligible action: Feed verified identities into LIPI-07/08; withheld variants stay excluded.

Planning estimate: 2–5 engineering hours. New incremental planning range, not original Jira estimate or worklog. Reuse accepted assets; elapsed shipping, review waiting, observation windows and provider verification are excluded. Dependency risk: Provider access/availability and mapping controls require current verification.

Jira: actual key null. Candidates LIPI-64. Designated writer compares current scope and retained acceptance, reuses matching issue or adds only genuine residual scope, preserves all original estimates/actuals, and returns native key/readback bound to this specification. Writer: Lipi writer designation unresolved: LIPI-I09.

## LIPI-04 — Qualify Open Current tote provider identity

Reconcile the existing product instead of duplicating supplier templates or drafts.

- Status: reuse_and_verify
- Owner: Suggested Lipi executor, admitted by Lipi coordinator
- Source: pri8771/lipi-standard-store
- Owned paths: `ops/autonomy/providers/open-current/`
- Evidence: LIPI-E03, LIPI-E04, LIPI-E07
- Dependencies: LIPI-01
- Inputs: LIPI-I03
- Effect gates: LIPI-G01
- Requirements: LIPI-O03

Implementation steps:

1. Use retained identity only as the search anchor: Mantis M196 Denim Blue, template106648374; sample hint natural conflicts and must never be ordered. Refresh exact shop/store, product, variant, SKU, size/color, technique, placement, art hash and provider region.
2. Read current Shopify Draft/channel/media/retail values and Printful sync-variant/fulfillment status through the qualified route; compare both sides and isolate each mismatch.
3. Prepare an exact before/after repair for only mismatched private mapping or media. Reuse existing grants if sufficient; live changes require the applicable scoped capability and hash-bound receipt.

Effect-specific gate applicability:

- LIPI-G01: External shop/provider read or private mapping change only: intended account, current scoped supported route and any required existing grant.

Deliverables:

- variant-level joined provider mapping
- dated cost/availability/media evidence
- bounded correction proposal

Pass/fail checks:

- Variant-count, SKU, color/size, production hash and one-to-one mapping comparisons fail on missing/duplicate/mismatched IDs.
- Reopen/read both receiving systems after any authorized correction; Draft status and zero channels stay explicit until launch.

Acceptance:

- Each intended variant either has current matching mapping/fulfillment evidence or an exact blocked reason.
- No source hint or mockup is treated as current provider truth.

Completion rule: A blocked/missing-input/failure record may finish a preparation subunit only. This task is not capability/release complete until its positive checks and relevant real effect acceptance pass. Disabled unrelated effect classes do not block a qualified allowed class; claimed scope is explicit.

Review: Independent reviewer other than implementing author; record actual reviewer identity, model/version and host. Cultural reviewer identity is additionally required for Guru claims; no invented reviewer. Bind source commit, task/spec SHA256, artifact hashes, policy revision and receiving-system receipts; any material change invalidates affected approval. At most two targeted repair/review attempts per admitted task, or a stricter existing limit. Unresolved failure becomes needs_input/blocked with evidence; never an unbounded loop.

Rollback: Restore only the exact private field mapping from captured prior state if reversible and authorized; do not delete provider records or unpublish automatically.

Next eligible action: Feed verified identities into LIPI-07/08; withheld variants stay excluded.

Planning estimate: 2–5 engineering hours. New incremental planning range, not original Jira estimate or worklog. Reuse accepted assets; elapsed shipping, review waiting, observation windows and provider verification are excluded. Dependency risk: Provider access/availability and mapping controls require current verification.

Jira: actual key null. Candidates LIPI-67. Designated writer compares current scope and retained acceptance, reuses matching issue or adds only genuine residual scope, preserves all original estimates/actuals, and returns native key/readback bound to this specification. Writer: Lipi writer designation unresolved: LIPI-I09.

## LIPI-05 — Qualify Quiet Route jogger provider identity

Reconcile the existing product instead of duplicating supplier templates or drafts.

- Status: reuse_and_verify
- Owner: Suggested Lipi executor, admitted by Lipi coordinator
- Source: pri8771/lipi-standard-store
- Owned paths: `ops/autonomy/providers/quiet-route/`
- Evidence: LIPI-E03, LIPI-E04, LIPI-E07
- Dependencies: LIPI-01
- Inputs: LIPI-I03
- Effect gates: LIPI-G01
- Requirements: LIPI-O03

Implementation steps:

1. Use retained identity only as the search anchor: Jerzees975MPR, template106648266, historical20 variants across4 colors/5 sizes; membership conditional. Refresh exact shop/store, product, variant, SKU, size/color, technique, placement, art hash and provider region.
2. Read current Shopify Draft/channel/media/retail values and Printful sync-variant/fulfillment status through the qualified route; compare both sides and isolate each mismatch.
3. Prepare an exact before/after repair for only mismatched private mapping or media. Reuse existing grants if sufficient; live changes require the applicable scoped capability and hash-bound receipt.

Effect-specific gate applicability:

- LIPI-G01: External shop/provider read or private mapping change only: intended account, current scoped supported route and any required existing grant.

Deliverables:

- variant-level joined provider mapping
- dated cost/availability/media evidence
- bounded correction proposal

Pass/fail checks:

- Variant-count, SKU, color/size, production hash and one-to-one mapping comparisons fail on missing/duplicate/mismatched IDs.
- Reopen/read both receiving systems after any authorized correction; Draft status and zero channels stay explicit until launch.

Acceptance:

- Each intended variant either has current matching mapping/fulfillment evidence or an exact blocked reason.
- No source hint or mockup is treated as current provider truth.

Completion rule: A blocked/missing-input/failure record may finish a preparation subunit only. This task is not capability/release complete until its positive checks and relevant real effect acceptance pass. Disabled unrelated effect classes do not block a qualified allowed class; claimed scope is explicit.

Review: Independent reviewer other than implementing author; record actual reviewer identity, model/version and host. Cultural reviewer identity is additionally required for Guru claims; no invented reviewer. Bind source commit, task/spec SHA256, artifact hashes, policy revision and receiving-system receipts; any material change invalidates affected approval. At most two targeted repair/review attempts per admitted task, or a stricter existing limit. Unresolved failure becomes needs_input/blocked with evidence; never an unbounded loop.

Rollback: Restore only the exact private field mapping from captured prior state if reversible and authorized; do not delete provider records or unpublish automatically.

Next eligible action: Feed verified identities into LIPI-07/08; withheld variants stay excluded.

Planning estimate: 2–5 engineering hours. New incremental planning range, not original Jira estimate or worklog. Reuse accepted assets; elapsed shipping, review waiting, observation windows and provider verification are excluded. Dependency risk: Provider access/availability and mapping controls require current verification.

Jira: actual key null. Candidates LIPI-68. Designated writer compares current scope and retained acceptance, reuses matching issue or adds only genuine residual scope, preserves all original estimates/actuals, and returns native key/readback bound to this specification. Writer: Lipi writer designation unresolved: LIPI-I09.

## LIPI-06 — Qualify Threshold Study print provider identity

Reconcile the existing product instead of duplicating supplier templates or drafts.

- Status: reuse_and_verify
- Owner: Suggested Lipi executor, admitted by Lipi coordinator
- Source: pri8771/lipi-standard-store
- Owned paths: `ops/autonomy/providers/threshold-study/`
- Evidence: LIPI-E03, LIPI-E04, LIPI-E07
- Dependencies: LIPI-01
- Inputs: LIPI-I03
- Effect gates: LIPI-G01
- Requirements: LIPI-O03

Implementation steps:

1. Use retained identity only as the search anchor: Threshold Study01 12×16, one finish; exact production file and supplier size/finish mapping remain explicit. Refresh exact shop/store, product, variant, SKU, size/color, technique, placement, art hash and provider region.
2. Read current Shopify Draft/channel/media/retail values and Printful sync-variant/fulfillment status through the qualified route; compare both sides and isolate each mismatch.
3. Prepare an exact before/after repair for only mismatched private mapping or media. Reuse existing grants if sufficient; live changes require the applicable scoped capability and hash-bound receipt.

Effect-specific gate applicability:

- LIPI-G01: External shop/provider read or private mapping change only: intended account, current scoped supported route and any required existing grant.

Deliverables:

- variant-level joined provider mapping
- dated cost/availability/media evidence
- bounded correction proposal

Pass/fail checks:

- Variant-count, SKU, color/size, production hash and one-to-one mapping comparisons fail on missing/duplicate/mismatched IDs.
- Reopen/read both receiving systems after any authorized correction; Draft status and zero channels stay explicit until launch.

Acceptance:

- Each intended variant either has current matching mapping/fulfillment evidence or an exact blocked reason.
- No source hint or mockup is treated as current provider truth.

Completion rule: A blocked/missing-input/failure record may finish a preparation subunit only. This task is not capability/release complete until its positive checks and relevant real effect acceptance pass. Disabled unrelated effect classes do not block a qualified allowed class; claimed scope is explicit.

Review: Independent reviewer other than implementing author; record actual reviewer identity, model/version and host. Cultural reviewer identity is additionally required for Guru claims; no invented reviewer. Bind source commit, task/spec SHA256, artifact hashes, policy revision and receiving-system receipts; any material change invalidates affected approval. At most two targeted repair/review attempts per admitted task, or a stricter existing limit. Unresolved failure becomes needs_input/blocked with evidence; never an unbounded loop.

Rollback: Restore only the exact private field mapping from captured prior state if reversible and authorized; do not delete provider records or unpublish automatically.

Next eligible action: Feed verified identities into LIPI-07/08; withheld variants stay excluded.

Planning estimate: 2–5 engineering hours. New incremental planning range, not original Jira estimate or worklog. Reuse accepted assets; elapsed shipping, review waiting, observation windows and provider verification are excluded. Dependency risk: Provider access/availability and mapping controls require current verification.

Jira: actual key null. Candidates LIPI-66. Designated writer compares current scope and retained acceptance, reuses matching issue or adds only genuine residual scope, preserves all original estimates/actuals, and returns native key/readback bound to this specification. Writer: Lipi writer designation unresolved: LIPI-I09.

## LIPI-07 — Enforce all-in economics and US cart shipping

Make every permitted selling basket satisfy the binding 20% floor with 10% reserve before price or shipping action.

- Status: reuse_and_verify
- Owner: Suggested Lipi executor, admitted by Lipi coordinator
- Source: pri8771/lipi-standard-store
- Owned paths: `services/lipi-autonomy/src/economics/`; `services/lipi-autonomy/test/economics/`; `ops/autonomy/economics/`
- Evidence: LIPI-E04, LIPI-E05
- Dependencies: LIPI-01
- Inputs: LIPI-I03; LIPI-I04
- Effect gates: LIPI-G02, LIPI-G03
- Requirements: LIPI-O04

Implementation steps:

1. Reuse the standardized integer-cents method: R=P−D; total revenue=R+customer shipping; contribution subtracts supplier product/decoration/shipping, platform allocation, variable/fixed payment/channel fees, 10%×R reserve and actual absorbed costs. Do not use retired30% caution as floor or 2–5% reserve hypotheses. Contribution percent=contribution/(R+customer shipping); reject nonpositive denominator. Represent cents and rate arithmetic exactly using integer/rational intermediates and round only the final displayed money; threshold comparisons use unrounded values. Collected/remitted pass-through tax is excluded from revenue; fee bases follow actual provider rules. Preserve dated owner no-collection posture as an assumption and include any actually absorbed/import cost only when evidenced.
2. Refresh fee rules, exact variant costs and US destination/service quotes; enumerate single-item, mixed-cart, high-cost-size, discounts and $98.99/$99/$99.01 after-discount threshold cases without double counting shipping.
3. Fail closed for unknown/stale inputs, prohibited discount+merchant-paid-shipping stack or any allowed basket below 20%. Compile a signed policy/economics result and exact price proposal; $99 free-US-shipping remains disabled until mixed-cart proof and existing approval. Preserve owner tax record without supplying a legal determination.

Conditional dependencies (only the named effect branch):

- Actual retained-capsule launch/price/shipping economics; offline scenario modeling is independent. Requires LIPI-02. Bind owner-accepted membership. Require positive exact variant mapping for every included product and exclude explicitly rejected/withheld variants. Conditional Quiet Route never blocks the other accepted capsule products.
- Meridian is included in the actual permitted selling/sample basket. Requires LIPI-03. Current exact mapping/cost/availability evidence for that included product must pass before treating its actual economics as qualified; otherwise its basket remains blocked, while unrelated scenarios continue.
- Open Current is included in the actual permitted selling/sample basket. Requires LIPI-04. Current exact mapping/cost/availability evidence for that included product must pass before treating its actual economics as qualified; otherwise its basket remains blocked, while unrelated scenarios continue.
- Quiet Route is included in the actual permitted selling/sample basket. Requires LIPI-05. Current exact mapping/cost/availability evidence for that included product must pass before treating its actual economics as qualified; otherwise its basket remains blocked, while unrelated scenarios continue.
- Threshold Study is included in the actual permitted selling/sample basket. Requires LIPI-06. Current exact mapping/cost/availability evidence for that included product must pass before treating its actual economics as qualified; otherwise its basket remains blocked, while unrelated scenarios continue.

Effect-specific gate applicability:

- LIPI-G02: Live retail price change only: existing conditional owner authority, current worst-case 20% math, exact before/after and required Jira/worklog admission; no new generic price approval.
- LIPI-G03: Discount change or free-shipping activation only: exact action-time owner grant, mixed-cart economics and cadence/expiry rules.

Deliverables:

- versioned margin evaluator
- shipping matrix and quote freshness policy
- price/shipping decision packet

Pass/fail checks:

- Boundary fixtures at 19.99/20.00/20.01% and null fee fail/pass correctly with no intermediate rounding.
- Mixed supplier/cart shipments cannot be treated as linear single-item shipping; stale or changed cost invalidates queued price action.

Acceptance:

- Every permitted basket clears 20% under current evidenced assumptions; failing baskets cannot sell under the proposed configuration.
- Reserve remains 10% until owner revisits after 100 actual orders; economic and tax unknowns stay explicit.

Completion rule: A blocked/missing-input/failure record may finish a preparation subunit only. This task is not capability/release complete until its positive checks and relevant real effect acceptance pass. Disabled unrelated effect classes do not block a qualified allowed class; claimed scope is explicit.

Review: Independent reviewer other than implementing author; record actual reviewer identity, model/version and host. Cultural reviewer identity is additionally required for Guru claims; no invented reviewer. Bind source commit, task/spec SHA256, artifact hashes, policy revision and receiving-system receipts; any material change invalidates affected approval. At most two targeted repair/review attempts per admitted task, or a stricter existing limit. Unresolved failure becomes needs_input/blocked with evidence; never an unbounded loop.

Rollback: Revert proposed calculations/config to previous admitted policy; a live price rollback must itself satisfy current margin authority.

Next eligible action: Produce exact purchase quote in LIPI-08; price execution only through LIPI-16.

Planning estimate: 5–9 engineering hours. New incremental planning range, not original Jira estimate or worklog. Reuse accepted assets; elapsed shipping, review waiting, observation windows and provider verification are excluded. Dependency risk: Current quotes, fees and threshold behavior missing.

Jira: actual key null. Candidates LIPI-69, LIPI-71. Designated writer compares current scope and retained acceptance, reuses matching issue or adds only genuine residual scope, preserves all original estimates/actuals, and returns native key/readback bound to this specification. Writer: Lipi writer designation unresolved: LIPI-I09.

## LIPI-08 — Prepare and execute only an approved exact sample order

Turn the retained incomplete sample packet into a purchase-ready decision.

- Status: reuse_and_verify
- Owner: Suggested Lipi executor, admitted by Lipi coordinator
- Source: pri8771/lipi-standard-store
- Owned paths: `ops/autonomy/samples/order/`
- Evidence: LIPI-E02, LIPI-E04
- Dependencies: LIPI-02, LIPI-07
- Inputs: LIPI-I05
- Effect gates: LIPI-G04
- Requirements: LIPI-O05, LIPI-A11

Implementation steps:

1. Repair null quote totals and Open Current natural/Denim Blue conflict using verified provider IDs; record each item, quantity, design hash, shipping service, dated quote, landed total, expiry and private delivery-reference.
2. Reuse any current exact sample grant; otherwise persist one owner purchase decision with maximum total/currency and selected items. No standing sample budget is assumed.
3. After exact authorization, submit once through the permitted provider route and capture provider order ID, total and line-item readback; timeout enters reconciliation by order/external ID before any retry.

Effect-specific gate applicability:

- LIPI-G04: Sample/purchase/charged test/provider order or paid supplier action only: exact identities, total, currency, cap, expiry and owner grant.

Deliverables:

- purchase-ready sample packet
- owner approval or needs-input record
- provider sample order receipt

Pass/fail checks:

- Null total, changed color/design, expired quote or cap overrun blocks submission.
- Crash after submit resolves the existing provider order rather than placing a second order.

Acceptance:

- Sample purchase matches exact authorized identities and total, or remains clearly awaiting the one required decision.
- Receipt is provider-observed; preparing a packet is not purchasing or delivery.

Completion rule: A blocked/missing-input/failure record may finish a preparation subunit only. This task is not capability/release complete until its positive checks and relevant real effect acceptance pass. Disabled unrelated effect classes do not block a qualified allowed class; claimed scope is explicit.

Review: Independent reviewer other than implementing author; record actual reviewer identity, model/version and host. Cultural reviewer identity is additionally required for Guru claims; no invented reviewer. Bind source commit, task/spec SHA256, artifact hashes, policy revision and receiving-system receipts; any material change invalidates affected approval. At most two targeted repair/review attempts per admitted task, or a stricter existing limit. Unresolved failure becomes needs_input/blocked with evidence; never an unbounded loop.

Rollback: Cancel only when provider confirms reversibility and owner authorizes; otherwise retain order and monitor delivery.

Next eligible action: LIPI-09 tracks delivery and physical inspection; unrelated storefront/runtime work continues.

Planning estimate: 2–4 engineering hours. New incremental planning range, not original Jira estimate or worklog. Reuse accepted assets; elapsed shipping, review waiting, observation windows and provider verification are excluded. Dependency risk: Owner per-purchase approval and fulfillment lead time.

Jira: actual key null. Candidates LIPI-70. Designated writer compares current scope and retained acceptance, reuses matching issue or adds only genuine residual scope, preserves all original estimates/actuals, and returns native key/readback bound to this specification. Writer: Lipi writer designation unresolved: LIPI-I09.

## LIPI-09 — Inspect delivered samples and narrow quality claims

Verify physical quality before exposing replacement products or expanding acquisition.

- Status: reuse_and_verify
- Owner: Suggested Lipi executor, admitted by Lipi coordinator
- Source: pri8771/lipi-standard-store
- Owned paths: `ops/autonomy/samples/inspection/`
- Evidence: LIPI-E02, LIPI-E04
- Dependencies: LIPI-08
- Inputs: LIPI-I06
- Effect gates: LIPI-G04
- Requirements: LIPI-O05

Implementation steps:

1. Track actual shipped/delivered states against the sample receipt without pretending elapsed time has passed.
2. Have the receiving owner inspect material, size/fit, print/embroidery registration, color, care/packaging, damage and delivered appearance using the exact ordered variants; retain redacted photos and attributable observations.
3. Record accepted/narrowed/rejected per SKU, reconcile mockup-vs-delivered claims and create targeted rework only for failures. Use a new exact purchase gate for replacements that cost money.

Effect-specific gate applicability:

- LIPI-G04: Sample/purchase/charged test/provider order or paid supplier action only: exact identities, total, currency, cap, expiry and owner grant.

Deliverables:

- delivery receipt
- physical QA matrix
- accepted capsule revision

Pass/fail checks:

- A tracking label or synthetic photo cannot mark delivered/quality-accepted.
- A failed sample removes affected claims from launch candidate and triggers finite repair review.

Acceptance:

- Each launch SKU has attributable physical acceptance or remains withheld.
- Inspection result is linked to the same provider order and product configuration.

Completion rule: A blocked/missing-input/failure record may finish a preparation subunit only. This task is not capability/release complete until its positive checks and relevant real effect acceptance pass. Disabled unrelated effect classes do not block a qualified allowed class; claimed scope is explicit.

Review: Independent reviewer other than implementing author; record actual reviewer identity, model/version and host. Cultural reviewer identity is additionally required for Guru claims; no invented reviewer. Bind source commit, task/spec SHA256, artifact hashes, policy revision and receiving-system receipts; any material change invalidates affected approval. At most two targeted repair/review attempts per admitted task, or a stricter existing limit. Unresolved failure becomes needs_input/blocked with evidence; never an unbounded loop.

Rollback: Withhold affected candidate and preserve prior draft; no autonomous refund/replacement.

Next eligible action: LIPI-10/12 finish sample-bound content and purchase journey; alternate design experiment may be proposed.

Planning estimate: 2–5 engineering hours. New incremental planning range, not original Jira estimate or worklog. Reuse accepted assets; elapsed shipping, review waiting, observation windows and provider verification are excluded. Dependency risk: Real delivery and owner inspection waiting excluded.

Jira: actual key null. Candidates LIPI-72. Designated writer compares current scope and retained acceptance, reuses matching issue or adds only genuine residual scope, preserves all original estimates/actuals, and returns native key/readback bound to this specification. Writer: Lipi writer designation unresolved: LIPI-I09.

## LIPI-10 — Finish product content and coordinated storefront candidate

Reuse existing art/theme work and make all customer surfaces tell the same verified product story.

- Status: reuse_and_verify
- Owner: Suggested Lipi executor, admitted by Lipi coordinator
- Source: pri8771/lipi-standard-store
- Owned paths: `ops/autonomy/storefront/candidate/`; `services/lipi-autonomy/test/storefront-contract/`
- Evidence: LIPI-E02, LIPI-E04
- Dependencies: LIPI-02, LIPI-07
- Inputs: LIPI-I03
- Effect gates: LIPI-G01
- Requirements: LIPI-O06

Implementation steps:

1. Stage exact capsule copy, care/size information, official media/alt bindings and truthful shipping language; bind final media to LIPI-09 acceptance before publication.
2. Create a coordinated manifest for product status/channels, collections, Search/Cart, About, metadata, navigation/contact, Forms popup and lifecycle copy; preserve existing theme IDs as historical search anchors, not live truth.
3. Run private preview responsive and semantic tests, keyboard focus/escape/return, error/empty states, reduced motion and actual NVDA/device checks. Distinguish real assistive-device evidence from automated DOM checks.

Effect-specific gate applicability:

- LIPI-G01: External shop/provider read or private mapping change only: intended account, current scoped supported route and any required existing grant.

Deliverables:

- exact release-candidate surface manifest
- content/media and accessibility evidence
- targeted remediation list

Pass/fail checks:

- No legacy character-led identities, generic mockup names, inactive discount claims or inaccessible purchase controls leak from the candidate.
- Public/shared surfaces are not changed one-at-a-time as private QA.

Acceptance:

- All candidate surfaces agree on approved identity and available variants.
- Responsive/accessibility residuals are explicit and owned, not hidden behind passed source checks.

Completion rule: A blocked/missing-input/failure record may finish a preparation subunit only. This task is not capability/release complete until its positive checks and relevant real effect acceptance pass. Disabled unrelated effect classes do not block a qualified allowed class; claimed scope is explicit.

Review: Independent reviewer other than implementing author; record actual reviewer identity, model/version and host. Cultural reviewer identity is additionally required for Guru claims; no invented reviewer. Bind source commit, task/spec SHA256, artifact hashes, policy revision and receiving-system receipts; any material change invalidates affected approval. At most two targeted repair/review attempts per admitted task, or a stricter existing limit. Unresolved failure becomes needs_input/blocked with evidence; never an unbounded loop.

Rollback: Restore private candidate files and previous preview; shared public cutover belongs only to LIPI-14.

Next eligible action: LIPI-11 policy and LIPI-12 checkout qualify alongside the final physical sample binding.

Planning estimate: 4–8 engineering hours. New incremental planning range, not original Jira estimate or worklog. Reuse accepted assets; elapsed shipping, review waiting, observation windows and provider verification are excluded. Dependency risk: Live shared surfaces and actual assistive QA remain unverified.

Jira: actual key null. Candidates LIPI-73, LIPI-74, LIPI-76. Designated writer compares current scope and retained acceptance, reuses matching issue or adds only genuine residual scope, preserves all original estimates/actuals, and returns native key/readback bound to this specification. Writer: Lipi writer designation unresolved: LIPI-I09.

## LIPI-11 — Bind policies, consent and support decisions

Give buyers accurate terms and a recoverable support path within existing owner-only remedy authority.

- Status: reuse_and_verify
- Owner: Suggested Lipi executor, admitted by Lipi coordinator
- Source: pri8771/lipi-standard-store
- Owned paths: `ops/autonomy/policy/`; `services/lipi-autonomy/test/consent/`
- Evidence: LIPI-E04, LIPI-E05
- Dependencies: LIPI-01
- Inputs: LIPI-I07
- Effect gates: LIPI-G05
- Requirements: LIPI-O07

Implementation steps:

1. Reuse policy drafts and inspect current shipping/returns/privacy/contact/consent surfaces; identify owner decisions still required without repeating accepted decisions.
2. Keep customer replies draft-only, refund/replacement autonomous cap$0, chargebacks owner-only, and 1-business-day response target; map merchant/provider policy conflicts explicitly.
3. Prepare exact policy/consent changes for owner review, minimize personal data retention, and test opt-in/withdrawal/unsubscribe and contact route. Record tax posture as dated owner decision with follow-up, not legal advice or inferred authority.

Effect-specific gate applicability:

- LIPI-G05: Policy publication, customer send, refund, replacement or chargeback handling only: apply the specific owner-controlled operation; refund/replacement autonomous cap0 and chargebacks owner-only.

Deliverables:

- policy/consent decision manifest
- support route proof
- data retention and deletion contract

Pass/fail checks:

- Unapproved marketing consent or missing destination cannot trigger a customer send.
- Policy UI does not promise automatic refunds, stock, delivery deadlines or new legal conclusions.

Acceptance:

- Each policy field has current evidence/owner decision or a precise unresolved input.
- Contact/support and consent states survive reopen and route to an accountable owner.

Completion rule: A blocked/missing-input/failure record may finish a preparation subunit only. This task is not capability/release complete until its positive checks and relevant real effect acceptance pass. Disabled unrelated effect classes do not block a qualified allowed class; claimed scope is explicit.

Review: Independent reviewer other than implementing author; record actual reviewer identity, model/version and host. Cultural reviewer identity is additionally required for Guru claims; no invented reviewer. Bind source commit, task/spec SHA256, artifact hashes, policy revision and receiving-system receipts; any material change invalidates affected approval. At most two targeted repair/review attempts per admitted task, or a stricter existing limit. Unresolved failure becomes needs_input/blocked with evidence; never an unbounded loop.

Rollback: Revert staged policy revision; do not retroactively erase customer consent/complaint records.

Next eligible action: LIPI-12 checkout and LIPI-18 support use the accepted version.

Planning estimate: 3–6 engineering hours. New incremental planning range, not original Jira estimate or worklog. Reuse accepted assets; elapsed shipping, review waiting, observation windows and provider verification are excluded. Dependency risk: Owner policy decisions and live consent route.

Jira: actual key null. Candidates LIPI-77, LIPI-84. Designated writer compares current scope and retained acceptance, reuses matching issue or adds only genuine residual scope, preserves all original estimates/actuals, and returns native key/readback bound to this specification. Writer: Lipi writer designation unresolved: LIPI-I09.

## LIPI-12 — Verify checkout and transactional journey

Prove a buyer can purchase the correct approved item and receive the correct order handoff.

- Status: reuse_and_verify
- Owner: Suggested Lipi executor, admitted by Lipi coordinator
- Source: pri8771/lipi-standard-store
- Owned paths: `ops/autonomy/checkout/`; `services/lipi-autonomy/test/checkout/`
- Evidence: LIPI-E02, LIPI-E04
- Dependencies: LIPI-07, LIPI-09, LIPI-10, LIPI-11
- Inputs: LIPI-I03; LIPI-I04; LIPI-I05
- Effect gates: LIPI-G04, LIPI-G05
- Requirements: LIPI-O08

Implementation steps:

1. Exercise variant selection, quantity, cart persistence, discount rejection, US address/shipping, mixed cart, threshold, payment failure/cancel, confirmation and support links using private/test routes first.
2. Verify merchant/payment eligibility and exact charged-test allowance separately; do not use a synthetic checkout as a real payment or charge without authority.
3. For the authorized purchase canary, reconcile Shopify order/payment status, Printful mapping/hold state and transactional delivery receipt using secret/PII references only.

Effect-specific gate applicability:

- LIPI-G04: Sample/purchase/charged test/provider order or paid supplier action only: exact identities, total, currency, cap, expiry and owner grant.
- LIPI-G05: Policy publication, customer send, refund, replacement or chargeback handling only: apply the specific owner-controlled operation; refund/replacement autonomous cap0 and chargebacks owner-only.

Deliverables:

- checkout scenario results
- charge/test authority record
- joined order/payment/provider receipt

Pass/fail checks:

- Wrong variant, stale price, unauthorized discount or below-floor basket blocks completion.
- Payment timeout does not create a second charge/order; user sees useful failure and reconciliation status.

Acceptance:

- Relevant edge cases pass on exact candidate and a separately identified real or test order receipt exists.
- Real payment/fulfillment acceptance remains open if only a test route was used.

Completion rule: A blocked/missing-input/failure record may finish a preparation subunit only. This task is not capability/release complete until its positive checks and relevant real effect acceptance pass. Disabled unrelated effect classes do not block a qualified allowed class; claimed scope is explicit.

Review: Independent reviewer other than implementing author; record actual reviewer identity, model/version and host. Cultural reviewer identity is additionally required for Guru claims; no invented reviewer. Bind source commit, task/spec SHA256, artifact hashes, policy revision and receiving-system receipts; any material change invalidates affected approval. At most two targeted repair/review attempts per admitted task, or a stricter existing limit. Unresolved failure becomes needs_input/blocked with evidence; never an unbounded loop.

Rollback: Restore staged checkout configuration; refunds, voids and cancellations use exact owner authorization and destination readback.

Next eligible action: LIPI-13 review/restore and LIPI-14 release; LIPI-17 observes fulfillment.

Planning estimate: 4–8 engineering hours. New incremental planning range, not original Jira estimate or worklog. Reuse accepted assets; elapsed shipping, review waiting, observation windows and provider verification are excluded. Dependency risk: Payment eligibility, sample acceptance and exact purchase allowance.

Jira: actual key null. Candidates LIPI-75. Designated writer compares current scope and retained acceptance, reuses matching issue or adds only genuine residual scope, preserves all original estimates/actuals, and returns native key/readback bound to this specification. Writer: Lipi writer designation unresolved: LIPI-I09.

## LIPI-13 — Review exact release and rehearse rollback

Make launch a reviewable candidate with a tested recovery procedure.

- Status: reuse_and_verify
- Owner: Suggested Lipi executor, admitted by Lipi coordinator
- Source: pri8771/lipi-standard-store
- Owned paths: `ops/autonomy/release/review/`; `ops/autonomy/release/restore/`
- Evidence: LIPI-E02, LIPI-E04
- Dependencies: LIPI-10, LIPI-11, LIPI-12
- Inputs: LIPI-I08
- Effect gates: LIPI-G06
- Requirements: LIPI-O09, LIPI-A17

Implementation steps:

1. Capture current theme/catalog/channel/policy/discount snapshot without secrets and hash the exact candidate, cost inputs, sample acceptance and checkout evidence.
2. Rehearse private restore from the snapshot; distinguish technical rollback capability from permission to change public or irreversible orders.
3. Obtain attributable independent technical/content review and concrete owner launch decision covering exact catalog/shared surfaces. Limit repairs to2 and invalidate approval if payload drifts.

Effect-specific gate applicability:

- LIPI-G06: Public product/theme/catalog/shared-surface cutover only: exact independent review, owner launch grant, preimage and reversible rollback.

Deliverables:

- review receipt
- private restore evidence
- exact owner launch packet

Pass/fail checks:

- Restore reproduces snapshot hashes and preserves order/payment receipts.
- Unreviewed file or changed price/mapping invalidates candidate admission.

Acceptance:

- Exact-source independent review and private recovery must pass; failed/withheld review leaves release acceptance incomplete.
- Owner can approve one complete current candidate without unspecified future changes.

Completion rule: A blocked/missing-input/failure record may finish a preparation subunit only. This task is not capability/release complete until its positive checks and relevant real effect acceptance pass. Disabled unrelated effect classes do not block a qualified allowed class; claimed scope is explicit.

Review: Independent reviewer other than implementing author; record actual reviewer identity, model/version and host. Cultural reviewer identity is additionally required for Guru claims; no invented reviewer. Bind source commit, task/spec SHA256, artifact hashes, policy revision and receiving-system receipts; any material change invalidates affected approval. At most two targeted repair/review attempts per admitted task, or a stricter existing limit. Unresolved failure becomes needs_input/blocked with evidence; never an unbounded loop.

Rollback: Keep existing live state; restore private candidate only; no forced refund or deletion.

Next eligible action: LIPI-14 applies only the approved cutover.

Planning estimate: 3–6 engineering hours. New incremental planning range, not original Jira estimate or worklog. Reuse accepted assets; elapsed shipping, review waiting, observation windows and provider verification are excluded. Dependency risk: Independent reviewer and exact owner launch decision.

Jira: actual key null. Candidates LIPI-78, LIPI-79. Designated writer compares current scope and retained acceptance, reuses matching issue or adds only genuine residual scope, preserves all original estimates/actuals, and returns native key/readback bound to this specification. Writer: Lipi writer designation unresolved: LIPI-I09.

## LIPI-14 — Apply approved capsule cutover and verify public destinations

Release the accepted purchase-ready capsule with real public readback.

- Status: reuse_and_verify
- Owner: Suggested Lipi executor, admitted by Lipi coordinator
- Source: pri8771/lipi-standard-store
- Owned paths: `ops/autonomy/release/cutover/`
- Evidence: LIPI-E02, LIPI-E04
- Dependencies: LIPI-13
- Inputs: LIPI-I08
- Effect gates: LIPI-G06
- Requirements: LIPI-O09

Implementation steps:

1. Reserve the one store writer and compare current live preconditions to approved snapshot; apply approved products/channels/collections/shared surfaces in the declared sequence.
2. Read public product/variant prices, Search/Cart/About/contact/policies/Forms and checkout back from the actual domain; verify the intended shop and theme, not a preview.
3. Record per-surface success/uncertain/failure; stop dependent acquisition on inconsistency and use the approved rollback decision with readback, leaving irreversible receipts intact.

Effect-specific gate applicability:

- LIPI-G06: Public product/theme/catalog/shared-surface cutover only: exact independent review, owner launch grant, preimage and reversible rollback.

Deliverables:

- cutover receipt manifest
- public destination evidence
- accepted live release ID

Pass/fail checks:

- Stale owner/changed live preconditions abort before mutation.
- Partial cutover and restart reconcile each surface; no second publication or blind whole-store overwrite.

Acceptance:

- Approved capsule and consistent shared surfaces are publicly verified. Any partial cutover keeps release acceptance incomplete until reconciled and repaired or rolled back.
- Release is distinct from autonomous operation or sales validation.

Completion rule: A blocked/missing-input/failure record may finish a preparation subunit only. This task is not capability/release complete until its positive checks and relevant real effect acceptance pass. Disabled unrelated effect classes do not block a qualified allowed class; claimed scope is explicit.

Review: Independent reviewer other than implementing author; record actual reviewer identity, model/version and host. Cultural reviewer identity is additionally required for Guru claims; no invented reviewer. Bind source commit, task/spec SHA256, artifact hashes, policy revision and receiving-system receipts; any material change invalidates affected approval. At most two targeted repair/review attempts per admitted task, or a stricter existing limit. Unresolved failure becomes needs_input/blocked with evidence; never an unbounded loop.

Rollback: Execute only approved reversible theme/catalog rollback from captured prior values; escalate irreversible/customer effects.

Next eligible action: LIPI-19 authorized organic distribution and LIPI-17/18 operations start when admitted.

Planning estimate: 2–5 engineering hours. New incremental planning range, not original Jira estimate or worklog. Reuse accepted assets; elapsed shipping, review waiting, observation windows and provider verification are excluded. Dependency risk: Exact launch grant and provider availability.

Jira: actual key null. Candidates LIPI-82, LIPI-83. Designated writer compares current scope and retained acceptance, reuses matching issue or adds only genuine residual scope, preserves all original estimates/actuals, and returns native key/readback bound to this specification. Writer: Lipi writer designation unresolved: LIPI-I09.

## LIPI-15 — Connect durable commerce observations and due events

Replace repository-only business answers with narrowly scoped current evidence feeding shared scheduling.

- Status: planned
- Owner: Suggested Lipi executor, admitted by Lipi coordinator
- Source: pri8771/lipi-standard-store
- Owned paths: `services/lipi-autonomy/src/observations/`; `services/lipi-autonomy/src/events/`; `services/lipi-autonomy/test/observations/`
- Evidence: LIPI-E03, LIPI-E05, LIPI-E09
- Dependencies: LIPI-01, SH-02, SH-03, SH-04, SH-13
- Inputs: LIPI-I03; LIPI-I10
- Effect gates: LIPI-G01, LIPI-G08
- Requirements: LIPI-O10, LIPI-O13, LIPI-A01, LIPI-A02, LIPI-A10, LIPI-A15

Implementation steps:

1. Keep services/lipi-control-plane readonly. Add a separate mission integration that normalizes shop/product/variant/cost changes, order/payment/fulfillment transitions, consent/support events and aggregate analytics into shared durable state.
2. Qualify supported versioned Shopify and Printful read routes and webhook authenticity/delivery IDs from current official contracts; if webhook access is unavailable use finite due-time incremental reads with cursor/watermark and quota caps.
3. Deduplicate by provider+shop+event ID/version; persist observed_at, provider time, schema/source hash and next_check_at. Schedule held-order escalation before 12h and unshipped review at 3 business days; unchanged snapshots trigger no model call.

Effect-specific gate applicability:

- LIPI-G01: External shop/provider read or private mapping change only: intended account, current scoped supported route and any required existing grant.
- LIPI-G08: New runtime/startup/model/tool/alert route only: admitted source, exclusive owner, private storage, existing route and finite allowed caps; no monetary allowance inferred.

Deliverables:

- commerce event adapters
- mission scheduler configuration
- redacted observation ledger

Pass/fail checks:

- Duplicate/out-of-order webhook and cursor restart produce one state transition without losing new evidence.
- Wrong shop, stale timestamp, invalid authenticity or malformed payload cannot modify authority or leak PII.

Acceptance:

- One current authorized read is persisted with provenance and repeat idle ticks show0 model calls.
- Missing providers yield explicit stale/unknown state and one resource question.

Completion rule: A blocked/missing-input/failure record may finish a preparation subunit only. This task is not capability/release complete until its positive checks and relevant real effect acceptance pass. Disabled unrelated effect classes do not block a qualified allowed class; claimed scope is explicit.

Review: Independent reviewer other than implementing author; record actual reviewer identity, model/version and host. Cultural reviewer identity is additionally required for Guru claims; no invented reviewer. Bind source commit, task/spec SHA256, artifact hashes, policy revision and receiving-system receipts; any material change invalidates affected approval. At most two targeted repair/review attempts per admitted task, or a stricter existing limit. Unresolved failure becomes needs_input/blocked with evidence; never an unbounded loop.

Rollback: Disable new consumer and retain cursor/state; keep old readonly service intact. Re-enable only one fenced consumer.

Next eligible action: LIPI-16 actions and LIPI-17/18 routines consume these events.

Planning estimate: 7–12 engineering hours. New incremental planning range, not original Jira estimate or worklog. Reuse accepted assets; elapsed shipping, review waiting, observation windows and provider verification are excluded. Dependency risk: Provider access and webhook format qualification; shared persistence.

Jira: actual key null. Candidates none established. Designated writer compares current scope and retained acceptance, reuses matching issue or adds only genuine residual scope, preserves all original estimates/actuals, and returns native key/readback bound to this specification. Writer: Lipi writer designation unresolved: LIPI-I09.

## LIPI-16 — Execute eligible commerce actions with durable approvals

Implement real permitted mutations without widening the existing read-only MCP service.

- Status: planned
- Owner: Suggested Lipi executor, admitted by Lipi coordinator
- Source: pri8771/lipi-standard-store
- Owned paths: `services/lipi-autonomy/src/actions/commerce/`; `services/lipi-autonomy/test/actions/commerce/`
- Evidence: LIPI-E03, LIPI-E04, LIPI-E05
- Dependencies: LIPI-07, LIPI-15, SH-05, SH-06, SH-10, SH-14
- Inputs: LIPI-I03; LIPI-I10
- Effect gates: LIPI-G01, LIPI-G02, LIPI-G03, LIPI-G04, LIPI-G05, LIPI-G06, LIPI-G08
- Requirements: LIPI-O13, LIPI-A01, LIPI-A03, LIPI-A04, LIPI-A09, LIPI-A10, LIPI-A11, LIPI-A16

Implementation steps:

1. Reuse ActionEnvelope canonical payload/expected-before/policy binding as a contract; replace process-local consumed-token Set semantics at the adapter boundary with shared transactional durable consumption and fenced claim.
2. Implement a strict allowlist for current authorized price changes and separately gated product/private mapping/publication/discount/order/refund actions. Verify destination shop, current direction, reviewed exact source, margin result, quota and grant immediately before effect.
3. Persist intent before submission, then provider operation ID and exact readback. On timeout reconcile provider state/order/payment ID first; never blindly replay a charged order or price mutation. Log before/after margin math and writer-bound Jira/worklog receipt as existing price authority requires.

Conditional dependencies (only the named effect branch):

- Live price change requiring existing Jira/worklog before/after projection. Requires SH-09. Verify current writer admission and required price logging evidence before the price effect. An unrelated refund, purchase, publish, marketing grant is not required for price-only scope.

Effect-specific gate applicability:

- LIPI-G01: External shop/provider read or private mapping change only: intended account, current scoped supported route and any required existing grant.
- LIPI-G02: Live retail price change only: existing conditional owner authority, current worst-case 20% math, exact before/after and required Jira/worklog admission; no new generic price approval.
- LIPI-G03: Discount change or free-shipping activation only: exact action-time owner grant, mixed-cart economics and cadence/expiry rules.
- LIPI-G04: Sample/purchase/charged test/provider order or paid supplier action only: exact identities, total, currency, cap, expiry and owner grant.
- LIPI-G05: Policy publication, customer send, refund, replacement or chargeback handling only: apply the specific owner-controlled operation; refund/replacement autonomous cap0 and chargebacks owner-only.
- LIPI-G06: Public product/theme/catalog/shared-surface cutover only: exact independent review, owner launch grant, preimage and reversible rollback.
- LIPI-G08: New runtime/startup/model/tool/alert route only: admitted source, exclusive owner, private storage, existing route and finite allowed caps; no monetary allowance inferred.

Deliverables:

- effect eligibility matrix
- commerce action adapters
- durable mutation/reconciliation ledger

Pass/fail checks:

- Restart after grant validation cannot reuse consumed grant or admit stale fencing token.
- Unknown budget, changed variant cost, different shop, missing price logging admission or expired grant prevents effect.

Acceptance:

- Authorized price change can be observed at destination with current≥20% worst-case contribution.
- Refund/replacement cap remains$0 and discounts/publication/customer sends stay gated; generic shell/HTTP does not become an effect tool.

Completion rule: A blocked/missing-input/failure record may finish a preparation subunit only. This task is not capability/release complete until its positive checks and relevant real effect acceptance pass. Disabled unrelated effect classes do not block a qualified allowed class; claimed scope is explicit.

Review: Independent reviewer other than implementing author; record actual reviewer identity, model/version and host. Cultural reviewer identity is additionally required for Guru claims; no invented reviewer. Bind source commit, task/spec SHA256, artifact hashes, policy revision and receiving-system receipts; any material change invalidates affected approval. At most two targeted repair/review attempts per admitted task, or a stricter existing limit. Unresolved failure becomes needs_input/blocked with evidence; never an unbounded loop.

Rollback: Pause action intake and reconcile pending effect; reversible field rollback itself rechecks current authority/economics.

Next eligible action: LIPI-20 may schedule a bounded permitted merchandising experiment; other action classes wait for exact grants.

Planning estimate: 8–14 engineering hours. New incremental planning range, not original Jira estimate or worklog. Reuse accepted assets; elapsed shipping, review waiting, observation windows and provider verification are excluded. Dependency risk: Action credentials and durable single-use approval integration.

Jira: actual key null. Candidates none established. Designated writer compares current scope and retained acceptance, reuses matching issue or adds only genuine residual scope, preserves all original estimates/actuals, and returns native key/readback bound to this specification. Writer: Lipi writer designation unresolved: LIPI-I09.

## LIPI-17 — Operate fulfillment and inventory exceptions

Track every actual paid order through provider acceptance, shipment, delivery and exceptions.

- Status: planned
- Owner: Suggested Lipi executor, admitted by Lipi coordinator
- Source: pri8771/lipi-standard-store
- Owned paths: `services/lipi-autonomy/src/fulfillment/`; `services/lipi-autonomy/test/fulfillment/`; `ops/autonomy/fulfillment/`
- Evidence: LIPI-E04, LIPI-E05
- Dependencies: LIPI-15, LIPI-16, SH-12
- Inputs: LIPI-I03; LIPI-I07
- Effect gates: LIPI-G01, LIPI-G04, LIPI-G05
- Requirements: LIPI-O10, LIPI-A04, LIPI-A11, LIPI-A14

Implementation steps:

1. Join Shopify order line IDs to exact Printful synced variants and provider order/status; distinguish paid, held, accepted, shipped, delivered, returned and canceled states without putting addresses in Git.
2. Use deterministic deadlines for held/failed escalation within 12h, unshipped after 3 business days drafting, and carrier exceptions; record due business calendar/time zone and receipt-derived timestamps.
3. Prepare replenishment/mapping/unavailability remedies and proactive notice drafts. No unapproved resubmit/purchase, substitution, automatic unpublish, delivery promise or customer send; ask once for the exact exception decision.

Effect-specific gate applicability:

- LIPI-G01: External shop/provider read or private mapping change only: intended account, current scoped supported route and any required existing grant.
- LIPI-G04: Sample/purchase/charged test/provider order or paid supplier action only: exact identities, total, currency, cap, expiry and owner grant.
- LIPI-G05: Policy publication, customer send, refund, replacement or chargeback handling only: apply the specific owner-controlled operation; refund/replacement autonomous cap0 and chargebacks owner-only.

Deliverables:

- order lifecycle reconciler
- SLA/exception queue
- owner remedy proposals

Pass/fail checks:

- Duplicate shipment events cannot duplicate a notice or second provider order.
- Provider cancellation after payment and out-of-stock event leave explicit unresolved effect with actionable deadline alert.

Acceptance:

- Each observed order has current joined lifecycle or named mapping gap.
- A synthetic held-order test and an actual authorized provider observation prove routing; zero real orders stays zero with order-cycle validation pending.

Completion rule: A blocked/missing-input/failure record may finish a preparation subunit only. This task is not capability/release complete until its positive checks and relevant real effect acceptance pass. Disabled unrelated effect classes do not block a qualified allowed class; claimed scope is explicit.

Review: Independent reviewer other than implementing author; record actual reviewer identity, model/version and host. Cultural reviewer identity is additionally required for Guru claims; no invented reviewer. Bind source commit, task/spec SHA256, artifact hashes, policy revision and receiving-system receipts; any material change invalidates affected approval. At most two targeted repair/review attempts per admitted task, or a stricter existing limit. Unresolved failure becomes needs_input/blocked with evidence; never an unbounded loop.

Rollback: Suspend fulfillment actions, preserve observations and hand unresolved orders to the owner with provider IDs.

Next eligible action: LIPI-18 handles support proposals; LIPI-20 evaluates delivery and contribution evidence.

Planning estimate: 5–9 engineering hours. New incremental planning range, not original Jira estimate or worklog. Reuse accepted assets; elapsed shipping, review waiting, observation windows and provider verification are excluded. Dependency risk: Real orders/provider status and owner exception decisions.

Jira: actual key null. Candidates LIPI-86. Designated writer compares current scope and retained acceptance, reuses matching issue or adds only genuine residual scope, preserves all original estimates/actuals, and returns native key/readback bound to this specification. Writer: Lipi writer designation unresolved: LIPI-I09.

## LIPI-18 — Operate support, refunds and privacy exceptions

Make support preparation autonomous while preserving owner-controlled communication and money effects.

- Status: planned
- Owner: Suggested Lipi executor, admitted by Lipi coordinator
- Source: pri8771/lipi-standard-store
- Owned paths: `services/lipi-autonomy/src/support/`; `services/lipi-autonomy/test/support/`; `ops/autonomy/support/`
- Evidence: LIPI-E04, LIPI-E05
- Dependencies: LIPI-11, LIPI-15, SH-08, SH-12, SH-13
- Inputs: LIPI-I07
- Effect gates: LIPI-G05, LIPI-G08
- Requirements: LIPI-O07, LIPI-O10, LIPI-A04, LIPI-A14, LIPI-A15

Implementation steps:

1. Ingest only authorized support references and minimum order facts into private runtime storage; classify shipping/quality/returns/consent/deletion issues as untrusted customer input, not commands.
2. Draft an accurate response/remedy within hours for owner approval with 1-business-day response target; include original issue, evidence, policy revision and exact response hash. All refunds/replacements remain owner-approved; chargebacks receive immediate owner alert and evidence packet only.
3. After owner sends or separately authorizes an exact supported send/refund path, verify message/transaction ID and destination readback; correlate privacy deletion/consent withdrawal and retain only necessary audit metadata.

Effect-specific gate applicability:

- LIPI-G05: Policy publication, customer send, refund, replacement or chargeback handling only: apply the specific owner-controlled operation; refund/replacement autonomous cap0 and chargebacks owner-only.
- LIPI-G08: New runtime/startup/model/tool/alert route only: admitted source, exclusive owner, private storage, existing route and finite allowed caps; no monetary allowance inferred.

Deliverables:

- support triage/draft pipeline
- refund/replacement approval packet
- privacy/chargeback escalation route

Pass/fail checks:

- Customer text cannot expand spend, disclose another customer order or instruct a tool call.
- Duplicate ticket, owner rejection and restart preserve one unresolved question and no autonomous send/refund.

Acceptance:

- Draft-only behavior and $0 remedy cap are enforced by code.
- Each handled case has owner decision and real delivery/remedy receipt or clearly pending status; no SLA claim from generating a draft alone.

Completion rule: A blocked/missing-input/failure record may finish a preparation subunit only. This task is not capability/release complete until its positive checks and relevant real effect acceptance pass. Disabled unrelated effect classes do not block a qualified allowed class; claimed scope is explicit.

Review: Independent reviewer other than implementing author; record actual reviewer identity, model/version and host. Cultural reviewer identity is additionally required for Guru claims; no invented reviewer. Bind source commit, task/spec SHA256, artifact hashes, policy revision and receiving-system receipts; any material change invalidates affected approval. At most two targeted repair/review attempts per admitted task, or a stricter existing limit. Unresolved failure becomes needs_input/blocked with evidence; never an unbounded loop.

Rollback: Pause send/remedy adapters; preserve case history; customer-facing correction requires owner approval.

Next eligible action: LIPI-20 uses redacted aggregate support/defect rates and proposes changes within rules.

Planning estimate: 5–9 engineering hours. New incremental planning range, not original Jira estimate or worklog. Reuse accepted assets; elapsed shipping, review waiting, observation windows and provider verification are excluded. Dependency risk: Private support access and owner handling time.

Jira: actual key null. Candidates LIPI-84. Designated writer compares current scope and retained acceptance, reuses matching issue or adds only genuine residual scope, preserves all original estimates/actuals, and returns native key/readback bound to this specification. Writer: Lipi writer designation unresolved: LIPI-I09.

## LIPI-19 — Run repeatable organic acquisition and customer discovery

Turn the existing asset pack into a measurable first-sale and repeat-use motion.

- Status: planned
- Owner: Suggested Lipi executor, admitted by Lipi coordinator
- Source: pri8771/lipi-standard-store
- Owned paths: `services/lipi-autonomy/src/marketing/`; `services/lipi-autonomy/test/marketing/`; `ops/autonomy/marketing/`
- Evidence: LIPI-E02, LIPI-E04
- Dependencies: LIPI-10, LIPI-11, SH-06, SH-07
- Inputs: LIPI-I11
- Effect gates: LIPI-G07
- Requirements: LIPI-O11, LIPI-A04

Implementation steps:

1. Reuse three product-led assets and select one owner-controlled eligible organic channel; bind exact handle/account, product availability, media rights/alt, landing URL, UTM and consent. Prepare content offline before LIPI-14; publishing depends on verified live destination.
2. Predeclare one question: whether a product-detail/use-case post yields qualified product visits, cart/checkouts and purchase inquiries. Collect voluntary non-sensitive objections about quality, shipping, fit and price; do not invent customer commitments or send unsolicited outreach.
3. Publish only the exact approved content/cadence through supported route and verify permalink/payload/account. Keep paid ads closed and lifecycle messages consent-bound/owner-controlled; queue next reviewed asset and declared observation due time.

Conditional dependencies (only the named effect branch):

- Actual organic publication linking to the capsule; offline content/discovery preparation proceeds. Requires LIPI-14. The exact current product/landing destination must be publicly verified and sale-eligible before sending traffic.

Effect-specific gate applicability:

- LIPI-G07: Organic publication/lifecycle marketing effect only: actual account, exact content/cadence grant, live destination, consent where applicable; paid ads remain disabled.

Deliverables:

- repeatable content/discovery queue
- qualified channel adapter
- post and landing receipt chain

Pass/fail checks:

- Unavailable product, stale quote/claim, wrong account, missing consent or absent publication grant blocks send.
- Duplicate publication timeout reconciles existing post; UTM/test traffic exclusion works.

Acceptance:

- One authorized post has exact real destination evidence and attribution. A fully prepared offline candidate is preparation only and does not satisfy publication acceptance.
- Acquisition is tied to profitable delivered-order objective; impressions do not become revenue.

Completion rule: A blocked/missing-input/failure record may finish a preparation subunit only. This task is not capability/release complete until its positive checks and relevant real effect acceptance pass. Disabled unrelated effect classes do not block a qualified allowed class; claimed scope is explicit.

Review: Independent reviewer other than implementing author; record actual reviewer identity, model/version and host. Cultural reviewer identity is additionally required for Guru claims; no invented reviewer. Bind source commit, task/spec SHA256, artifact hashes, policy revision and receiving-system receipts; any material change invalidates affected approval. At most two targeted repair/review attempts per admitted task, or a stricter existing limit. Unresolved failure becomes needs_input/blocked with evidence; never an unbounded loop.

Rollback: Pause future posts; corrections/deletion require exact authority and destination verification; retain published evidence.

Next eligible action: LIPI-20 evaluates objections/conversion and queues the next finite product/content experiment.

Planning estimate: 5–9 engineering hours. New incremental planning range, not original Jira estimate or worklog. Reuse accepted assets; elapsed shipping, review waiting, observation windows and provider verification are excluded. Dependency risk: Current channel identity, permission and live product destination.

Jira: actual key null. Candidates LIPI-81, LIPI-85. Designated writer compares current scope and retained acceptance, reuses matching issue or adds only genuine residual scope, preserves all original estimates/actuals, and returns native key/readback bound to this specification. Writer: Lipi writer designation unresolved: LIPI-I09.

## LIPI-20 — Evaluate commerce experiments and choose continuing actions

Close each finite learning cycle with truthful outcomes and a justified executable next decision.

- Status: planned
- Owner: Suggested Lipi executor, admitted by Lipi coordinator
- Source: pri8771/lipi-standard-store
- Owned paths: `services/lipi-autonomy/src/experiments/`; `services/lipi-autonomy/test/experiments/`; `ops/autonomy/experiments/`
- Evidence: LIPI-E02, LIPI-E04, LIPI-E05
- Dependencies: LIPI-15, LIPI-16, LIPI-17, LIPI-18, LIPI-19, SH-05, SH-07, SH-09
- Inputs: LIPI-I04; LIPI-I10
- Effect gates: LIPI-G02, LIPI-G07, LIPI-G08
- Requirements: LIPI-O12, LIPI-A02, LIPI-A03, LIPI-A05, LIPI-A08, LIPI-A16

Implementation steps:

1. Define immutable experiment baseline, hypothesis, target buyer/use case, intervention, allowed action/cost cap, data window, minimum evidence, stop rule and next_check_at before action; first cycle uses real7-day operations only after launch.
2. Reconcile qualified sessions, product views, cart/checkouts, paid and delivered orders, returns/defects, actual receipts/costs and owner interventions. Separate QA traffic, missing data, zero, refunds/chargebacks and gross/net contribution; do not count elapsed waiting as work.
3. At due evidence decide supported/unsupported/inconclusive and choose continue/change/stop with bounded next experiment: low qualified visits→new approved useful-content angle; visits/no carts→one product clarity/fit test; carts/no checkout→shipping clarity test; below-floor economics→hold affected action and price proposal; delivery defects→quality task. Queue selected task automatically without a new launch prompt.

Effect-specific gate applicability:

- LIPI-G02: Live retail price change only: existing conditional owner authority, current worst-case 20% math, exact before/after and required Jira/worklog admission; no new generic price approval.
- LIPI-G07: Organic publication/lifecycle marketing effect only: actual account, exact content/cadence grant, live destination, consent where applicable; paid ads remain disabled.
- LIPI-G08: New runtime/startup/model/tool/alert route only: admitted source, exclusive owner, private storage, existing route and finite allowed caps; no monetary allowance inferred.

Deliverables:

- aggregate scorecard
- experiment evaluator and next-task selector
- Jira/documentation experiment outbox

Pass/fail checks:

- Unknown analytics cannot become zero; synthetic orders cannot become revenue.
- An unchanged evidence fingerprint produces0 inference and no duplicated next experiment.

Acceptance:

- Two consecutive finite decision cycles preserve original hypotheses and generate eligible next actions or timed evidence waits.
- Financial and growth outcomes remain measured, including honest inconclusive/negative results.

Completion rule: A blocked/missing-input/failure record may finish a preparation subunit only. This task is not capability/release complete until its positive checks and relevant real effect acceptance pass. Disabled unrelated effect classes do not block a qualified allowed class; claimed scope is explicit.

Review: Independent reviewer other than implementing author; record actual reviewer identity, model/version and host. Cultural reviewer identity is additionally required for Guru claims; no invented reviewer. Bind source commit, task/spec SHA256, artifact hashes, policy revision and receiving-system receipts; any material change invalidates affected approval. At most two targeted repair/review attempts per admitted task, or a stricter existing limit. Unresolved failure becomes needs_input/blocked with evidence; never an unbounded loop.

Rollback: Pause experiment intervention and preserve baseline/results; return to prior admitted configuration only if safe and authorized.

Next eligible action: Dispatch the selected task through admission/fencing, or persist one missing-input question and wait cheaply.

Planning estimate: 6–10 engineering hours. New incremental planning range, not original Jira estimate or worklog. Reuse accepted assets; elapsed shipping, review waiting, observation windows and provider verification are excluded. Dependency risk: Real observation windows and customer outcomes excluded from effort.

Jira: actual key null. Candidates LIPI-80, LIPI-87. Designated writer compares current scope and retained acceptance, reuses matching issue or adds only genuine residual scope, preserves all original estimates/actuals, and returns native key/readback bound to this specification. Writer: Lipi writer designation unresolved: LIPI-I09.

## LIPI-21 — Consume owner direction and produce reconciled outboxes

Make ordinary owner steering change the next job once, with durable acknowledgement and resume.

- Status: planned
- Owner: Suggested Lipi executor, admitted by Lipi coordinator
- Source: pri8771/lipi-standard-store
- Owned paths: `services/lipi-autonomy/src/direction/`; `services/lipi-autonomy/src/outbox/`; `services/lipi-autonomy/test/direction/`
- Evidence: LIPI-E06, LIPI-E08
- Dependencies: LIPI-15, SH-08, SH-09
- Inputs: LIPI-I09; LIPI-I10
- Effect gates: LIPI-G08
- Requirements: LIPI-O13, LIPI-A06, LIPI-A07, LIPI-A08, LIPI-A10

Implementation steps:

1. Bridge existing ops/DIRECTION.md and business rules to shared issuer/mission/version validation. Persist recorded, seen, applied, waiting-for-effect, needs-input, superseded or expired with actual consumed revision.
2. Test focus on first customers and evaluate Indian clothing suppliers as prioritization changes; neither widens purchases, sends or brand direction. Pause stops new affected effects, while already submitted sample/order actions reconcile before resume.
3. Persist one question per missing input/revision and consume the validated answer once. Emit immutable task/experiment/result/review proposals to the designated Lipi writer and canonical ops-document owner; queue through writer outage without a second writer.

Effect-specific gate applicability:

- LIPI-G08: New runtime/startup/model/tool/alert route only: admitted source, exclusive owner, private storage, existing route and finite allowed caps; no monetary allowance inferred.

Deliverables:

- direction adapter and acknowledgement ledger
- deduplicated question/resume flow
- Lipi Jira/documentation outbox

Pass/fail checks:

- Old or duplicate direction cannot override newer accepted direction; external customer text cannot be owner direction.
- Pause immediately before price/order submit blocks it; restart after question does not ask again.

Acceptance:

- A real owner direction receipt names revision actually consumed and next action.
- Native writer readback or explicit pending outbox state exists; stored file alone is not worker acknowledgement.

Completion rule: A blocked/missing-input/failure record may finish a preparation subunit only. This task is not capability/release complete until its positive checks and relevant real effect acceptance pass. Disabled unrelated effect classes do not block a qualified allowed class; claimed scope is explicit.

Review: Independent reviewer other than implementing author; record actual reviewer identity, model/version and host. Cultural reviewer identity is additionally required for Guru claims; no invented reviewer. Bind source commit, task/spec SHA256, artifact hashes, policy revision and receiving-system receipts; any material change invalidates affected approval. At most two targeted repair/review attempts per admitted task, or a stricter existing limit. Unresolved failure becomes needs_input/blocked with evidence; never an unbounded loop.

Rollback: Pause direction effects and preserve append-only revisions/outbox; never rewrite prior experiment results.

Next eligible action: LIPI-22/23 prove recovery and repeated operation with these directions.

Planning estimate: 4–7 engineering hours. New incremental planning range, not original Jira estimate or worklog. Reuse accepted assets; elapsed shipping, review waiting, observation windows and provider verification are excluded. Dependency risk: Lipi writer identity and valid owner intake route.

Jira: actual key null. Candidates none established. Designated writer compares current scope and retained acceptance, reuses matching issue or adds only genuine residual scope, preserves all original estimates/actuals, and returns native key/readback bound to this specification. Writer: Lipi writer designation unresolved: LIPI-I09.

## LIPI-22 — Qualify independent Windows operations and recovery

Run one bounded Lipi operator without a Mac dependency or duplicate consumer.

- Status: planned
- Owner: Suggested Lipi executor, admitted by Lipi coordinator
- Source: pri8771/lipi-standard-store
- Owned paths: `services/lipi-autonomy/ops/windows/`; `services/lipi-autonomy/test/recovery/`; `ops/autonomy/recovery/`
- Evidence: LIPI-E03, LIPI-E06
- Dependencies: LIPI-15, LIPI-16, LIPI-21, SH-10, SH-11, SH-12, SH-13, SH-14
- Inputs: LIPI-I10
- Effect gates: LIPI-G08
- Requirements: LIPI-O13, LIPI-A09, LIPI-A11, LIPI-A12, LIPI-A13, LIPI-A14, LIPI-A15, LIPI-A16, LIPI-A18

Implementation steps:

1. Package pinned dependencies/config with host-local credential references, private data paths and Windows process-tree/job-object timeout cleanup; keep existing readonly service isolation and old watchdog removal evidence distinct from current health.
2. Register explicit finite due jobs only after admission. Enforce per-run model/tool/token/time/currency caps, 0 idle inference, 2-repair maximum and actionable alert state transitions; P0 chargeback and 12h fulfillment deadlines use cheap timers.
3. Inject crash before submit, after provider accepts, before readback, after state commit and during owner pause; restore checkpoint/backup, cursor, claims, consumed grants and outbox, fence former owner and reconcile pending effects before takeover. Produce optional R730 migration manifest and rollback; no migration/i9 prerequisite.

Conditional dependencies (only the named effect branch):

- Optional R730 migration or host transfer only; Windows acceptance is independent. Requires SH-16. Qualify target storage/backup/secret references, fence previous owner, reconcile pending effects and prove new-host receipt/rollback before cutover.

Effect-specific gate applicability:

- LIPI-G08: New runtime/startup/model/tool/alert route only: admitted source, exclusive owner, private storage, existing route and finite allowed caps; no monetary allowance inferred.

Deliverables:

- Windows service/runbook package
- fault/restore evidence
- alert/budget/isolation qualification
- optional R730 manifest

Pass/fail checks:

- Killing the parent terminates its owned process tree only; unrelated release workers survive.
- Stale owner cannot execute after restore; idle interval records0 inference and exhausted budget prevents paid fallback.

Acceptance:

- One Windows cycle runs with mobile Mac disconnected and produces correct receipts.
- Backup restore preserves one consumer, all pending effect knowledge and no cross-mission secret/customer access.

Completion rule: A blocked/missing-input/failure record may finish a preparation subunit only. This task is not capability/release complete until its positive checks and relevant real effect acceptance pass. Disabled unrelated effect classes do not block a qualified allowed class; claimed scope is explicit.

Review: Independent reviewer other than implementing author; record actual reviewer identity, model/version and host. Cultural reviewer identity is additionally required for Guru claims; no invented reviewer. Bind source commit, task/spec SHA256, artifact hashes, policy revision and receiving-system receipts; any material change invalidates affected approval. At most two targeted repair/review attempts per admitted task, or a stricter existing limit. Unresolved failure becomes needs_input/blocked with evidence; never an unbounded loop.

Rollback: Disable only the new Lipi consumer, fence it, reconcile pending effects and restore last verified package/state; never awaken removed Mac watchdog.

Next eligible action: LIPI-23 independent exact-source autonomy acceptance; optional SH-16 migration later.

Planning estimate: 6–11 engineering hours. New incremental planning range, not original Jira estimate or worklog. Reuse accepted assets; elapsed shipping, review waiting, observation windows and provider verification are excluded. Dependency risk: Current Windows runtime and host-local credentials unverified.

Jira: actual key null. Candidates none established. Designated writer compares current scope and retained acceptance, reuses matching issue or adds only genuine residual scope, preserves all original estimates/actuals, and returns native key/readback bound to this specification. Writer: Lipi writer designation unresolved: LIPI-I09.

## LIPI-23 — Prove repeated end-to-end autonomous commerce cycles

Demonstrate operation, owner steering and recovery separately from launch and revenue.

- Status: planned
- Owner: Suggested Lipi executor, admitted by Lipi coordinator
- Source: pri8771/lipi-standard-store
- Owned paths: `ops/autonomy/acceptance/`
- Evidence: LIPI-E02, LIPI-E03, LIPI-E04, LIPI-E06
- Dependencies: LIPI-14, LIPI-17, LIPI-18, LIPI-20, LIPI-21, LIPI-22, SH-15
- Inputs: LIPI-I03; LIPI-I10
- Effect gates: LIPI-G01, LIPI-G02, LIPI-G04, LIPI-G05, LIPI-G06, LIPI-G07, LIPI-G08
- Requirements: LIPI-O13, LIPI-A09, LIPI-A13, LIPI-A17, LIPI-A18

Implementation steps:

1. Freeze exact source/task/policy hashes and independent reviewer identity; execute an authorized live observation→decision→permitted action→receiving-system readback→evaluation→next-action cycle.
2. Repeat from the next due/event trigger without a new launch prompt. Exercise actual owner pause/resume/reprioritize plus crash after an accepted effect and restore without duplication; document model/tool/cost attribution.
3. Retain real7-day operations evidence when that window elapses. If there are no real customers/orders, prove permitted price/content/read observation routes but leave paid-order/delivery/revenue business acceptance open; never buy merely to manufacture sales.

Effect-specific gate applicability:

- LIPI-G01: External shop/provider read or private mapping change only: intended account, current scoped supported route and any required existing grant.
- LIPI-G02: Live retail price change only: existing conditional owner authority, current worst-case 20% math, exact before/after and required Jira/worklog admission; no new generic price approval.
- LIPI-G04: Sample/purchase/charged test/provider order or paid supplier action only: exact identities, total, currency, cap, expiry and owner grant.
- LIPI-G05: Policy publication, customer send, refund, replacement or chargeback handling only: apply the specific owner-controlled operation; refund/replacement autonomous cap0 and chargebacks owner-only.
- LIPI-G06: Public product/theme/catalog/shared-surface cutover only: exact independent review, owner launch grant, preimage and reversible rollback.
- LIPI-G07: Organic publication/lifecycle marketing effect only: actual account, exact content/cadence grant, live destination, consent where applicable; paid ads remain disabled.
- LIPI-G08: New runtime/startup/model/tool/alert route only: admitted source, exclusive owner, private storage, existing route and finite allowed caps; no monetary allowance inferred.

Deliverables:

- exact-source independent review
- two-cycle runtime receipts
- direction/restart evidence
- separate operating/business readiness report

Pass/fail checks:

- Each link uses actual event/action/destination IDs; synthetic fault fixtures are separately labeled.
- Independent reviewer reproduces source checks and reconciles every uncertain effect; unresolved repair after 2 attempts prevents autonomy acceptance.

Acceptance:

- Autonomous operation requires TWO completed real allowed observation-decision-action-verified-result-next-decision cycles, plus actual owner direction, recovery and bounded usage. A scheduled wait or second prepared decision is valid operation but cannot substitute for the second completed real cycle.
- Paid/fulfilled demand and profitability stay separate measured criteria; blocked effect does not become success.

Completion rule: A blocked/missing-input/failure record may finish a preparation subunit only. This task is not capability/release complete until its positive checks and relevant real effect acceptance pass. Disabled unrelated effect classes do not block a qualified allowed class; claimed scope is explicit.

Review: Independent reviewer other than implementing author; record actual reviewer identity, model/version and host. Cultural reviewer identity is additionally required for Guru claims; no invented reviewer. Bind source commit, task/spec SHA256, artifact hashes, policy revision and receiving-system receipts; any material change invalidates affected approval. At most two targeted repair/review attempts per admitted task, or a stricter existing limit. Unresolved failure becomes needs_input/blocked with evidence; never an unbounded loop.

Rollback: Stop new autonomous effects, retain read-only monitoring if allowed and hand exceptions to owner; restore qualified prior operator.

Next eligible action: Continue the accepted finite experiment queue; reopen failed gates with targeted tasks, not a fresh inventory.

Planning estimate: 4–8 engineering hours. New incremental planning range, not original Jira estimate or worklog. Reuse accepted assets; elapsed shipping, review waiting, observation windows and provider verification are excluded. Dependency risk: Real authorized effects, reviewer and observation windows.

Jira: actual key null. Candidates LIPI-86, LIPI-87. Designated writer compares current scope and retained acceptance, reuses matching issue or adds only genuine residual scope, preserves all original estimates/actuals, and returns native key/readback bound to this specification. Writer: Lipi writer designation unresolved: LIPI-I09.

## LIPI-24 — Evaluate India-to-US apparel as a separate supplier experiment

Test the owner supplier hypothesis without assuming Printful proves India sourcing or replacing the catalog.

- Status: planned
- Owner: Suggested Lipi executor, admitted by Lipi coordinator
- Source: pri8771/lipi-standard-store
- Owned paths: `ops/autonomy/supplier-hypothesis/`
- Evidence: LIPI-E02, LIPI-E04, LIPI-E06
- Dependencies: LIPI-01
- Inputs: LIPI-I12
- Effect gates: LIPI-G04, LIPI-G09
- Requirements: LIPI-O14

Implementation steps:

1. Create a supplier comparison for an actual specific garment and US delivery route: manufacturer identity, provenance/material/size claims, design rights, MOQ/customization, capacity, packaging, delivery/tracking, returns address and defect terms; mark unavailable facts unknown.
2. Use dated primary supplier/carrier/customs evidence to estimate full landed economics, FX, freight/duties/fees/returns and cash exposure separately from current Printful formulas. Obtain qualified owner/professional determinations for legal/import declarations; no outreach or purchase without exact authority.
3. Predeclare one sample/pilot with budget and physical/delivery/20%-floor acceptance; compare with current capsule. After actual approved sample, accept/narrow/reject route and schedule next evidence collection; do not change English-first brand/catalog claims absent new owner decision.

Effect-specific gate applicability:

- LIPI-G04: Sample/purchase/charged test/provider order or paid supplier action only: exact identities, total, currency, cap, expiry and owner grant.
- LIPI-G09: Supplier contact/import/pilot/catalog expansion only: exact outreach/purchase/import declaration authority and owner-approved scope; offline research is independent.

Deliverables:

- supplier hypothesis and evidence matrix
- landed-cost sensitivity model
- concrete sample/pilot decision and next experiment

Pass/fail checks:

- A Printful mapping cannot populate an India supplier claim.
- Unknown duties/return costs or missing provenance prevent profitability/US-delivery promise.

Acceptance:

- Specific supplier/product route has evidence or a precise collection task for each gap.
- Offline research/model is useful now; contact, purchase/import and catalog changes remain separate gates with receipts.

Completion rule: A blocked/missing-input/failure record may finish a preparation subunit only. This task is not capability/release complete until its positive checks and relevant real effect acceptance pass. Disabled unrelated effect classes do not block a qualified allowed class; claimed scope is explicit.

Review: Independent reviewer other than implementing author; record actual reviewer identity, model/version and host. Cultural reviewer identity is additionally required for Guru claims; no invented reviewer. Bind source commit, task/spec SHA256, artifact hashes, policy revision and receiving-system receipts; any material change invalidates affected approval. At most two targeted repair/review attempts per admitted task, or a stricter existing limit. Unresolved failure becomes needs_input/blocked with evidence; never an unbounded loop.

Rollback: Archive rejected hypothesis while preserving evidence; no existing capsule or provider account change.

Next eligible action: Run approved bounded supplier pilot or continue current capsule and recheck a named missing fact only when evidence can change.

Planning estimate: 5–10 engineering hours. New incremental planning range, not original Jira estimate or worklog. Reuse accepted assets; elapsed shipping, review waiting, observation windows and provider verification are excluded. Dependency risk: Supplier quotes, legal/import review and real sample lead time.

Jira: actual key null. Candidates none established. Designated writer compares current scope and retained acceptance, reuses matching issue or adds only genuine residual scope, preserves all original estimates/actuals, and returns native key/readback bound to this specification. Writer: Lipi writer designation unresolved: LIPI-I09.
