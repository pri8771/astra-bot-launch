# Claude Mac — first-three runtime and Windows-readiness batch

## Deliverable and boundaries

Produce a compact, implementation-ready shared-runtime handoff that lets OPO, Wait How Big and CommerceLint proceed through their existing product routes with host-local source, durable evidence and controlled ownership. After exact native Jira admission, implement only the small missing shared boundary demonstrated necessary for those launches; verify it with offline tests and return an isolated candidate. Do not build a replacement platform or make migration a prerequisite for the three product teams.

One cohesive Claude author session; maximum six hours, ending early when the finite queue is complete. Use the configured model/effort supplied at launch; do not silently select another model. Conserve Anthropic capacity: no delegate-model/reviewer loops, broad chat replay, whole-repository ingestion, repeated unchanged scans, idle reasoning or paid fallback. Read a small source packet per work unit, reuse accepted evidence and reserve one final review packet for root. At most two repair attempts across the batch; exhausted quota or a repeated identical blocker ends the dependent work with evidence.

Root owns canonical files, integration, deployments and launches. Cursor Windows owns CommerceLint, Cursor Mac owns OPO plus Jira-outbox coordination, Antigravity Mac owns WHB content/account qualification, and Antigravity Windows owns independent readiness QA. No edits to product code/content, their working trees, Pri 2.0, Digital Temple, Kai runtime/configuration, credentials, live ledgers, schedules or service allowances.

## Source packet and established facts

Start at `/Users/pchordia/Documents/ChatGPT/Astra/CONTINUE_HERE.md`; read it fully, then only relevant portions of `planning/bots-focus-2026-09-12/PROPOSED_OPERATING_PLAN.md`, `ACCOUNTS_CHANNELS_AND_HOSTS.md`, `WINDOWS_PRIMARY_READINESS.md`, `AUTONOMOUS_EXPERIMENTS.md`, `JIRA_EXPERIMENT_HANDOFF.md` and `EXPERIMENT_TEMPLATE.md`. Root alone updates these canonical documents. Newer owner steering launches the first three in parallel.

For each code question, read the named module and its directly corresponding tests; use bounded symbol searches instead of recursive source dumps:

- `tooling/manual_dispatch/README.md`, `manual_dispatch.py`, `run_queue.py` and relevant receipt/finalization tests.
- `tooling/dispatch_service/README.md`, `engine.py`, `supervisor.py` and relevant platform/claim tests.
- `tooling/review_controller/FINITE_RUN.md` and the exact receipt/admission validation functions needed; do not invoke the resident dispatcher, review runner or its model/Jira effects.

Known limits: the manual ledger is cooperative and single-Mac; `run_queue.py` uses `os.killpg`, while dispatch engine/supervisor import `fcntl` and use POSIX process controls. Native Windows supervised execution is **not qualified**. No working WSL distro has been established. Do not solve this by copying the Mac runtime, replacing the platform, opening a second authoritative ledger or weakening cleanup/fencing.

Windows BOTS source was verified at `D:/Astra/repos/bots`, clean `main`, SHA `d5b3b7f399b8d3e3ba4b926d0d89f30aacac6e11`; Python 3.11.9, Node 24.19.0, Ollama 0.33.2. Git read/clone works; gh push/PR identity remains unverified. Newer smoke evidence supersedes the older “download pending” note: `qwen3.5:4b` passed **two synthetic cases**, 122 input/22 output tokens total; `ollama ps` showed 100% GPU. Receipt: `D:/Astra/pilots/local-smoke-20260912-1812`. This is not business judgment, fleet, worker-recovery or Mac-independent qualification. Do not repeat it without a specific evidence gap.

## Ordered work units

1. **Freeze ownership and admission request.** Capture current Astra SHA/status and identify exactly which shared paths are already owned. Propose only needed changes against existing BOTS-90/92/94/100/111/112/148 associations; these are search pointers, not blanket admission. Write `ADMISSION_REQUEST.json` naming exact keys, acceptance, scope, predecessor evidence, proposed files, resources and one candidate source revision. Mac Cursor coordination relays this unchanged to sole Jira writer `6046ab83-8402-47e3-b832-f4fe478da7f1`. The writer must verify current native mappings, fully specify any changed task and return readback before product/runtime coding. No worker writes Jira or creates a competing writer. If admission is unavailable, finish units 2–4 as readiness artifacts and return; no indefinite waiting.
2. **Gap map tied to first launches.** For OPO, WHB and CommerceLint identify existing execution route, account/action boundary, source/receipt storage and the minimum missing common contract. Consume product-worker handoffs without editing their deliverables. Classify each fact verified/reported/unknown. Deliver `FIRST_THREE_GAPS.md`, separating launch blockers from optional infrastructure. Pending domain or publishing eligibility becomes one scoped `needs_input` record, never a guessed answer or global stop.
3. **One execution/evidence contract.** Map existing structures to mission/task/job identity, admitted action and input digest, source revision, host, attempt/role, timeout, deduplication/effect key, ownership/fence, result and uncertainty. Define `needs_input`, blocked, review-ready and effect-unknown behavior using existing mechanisms. Record model, effort, machine, input/output/cached/thinking counts and cost only when observed; session totals are counted once. Do not derive billing from a subscription percentage or allocate unknown shared usage to tasks.
4. **Windows transfer and qualification plan.** Produce `WINDOWS_HANDOFF.md`: independent local clones/worktrees, exact remotes/SHAs, hash-verified source/receipt bundle, private host-local credentials by reference, fixed prerequisite checks and one finite Windows validation command list. Windows output must reside on Windows independently of Mac availability. No shared SMB working tree, copied browser profile/token cache, remote Mac runtime dependency, WSL installation, reboot, service setup or model pull. Explicitly mark unsupported native process control and how a later admitted worker must prove cleanup, restart and uncertain-effect reconciliation.
5. **Implement the smallest admitted gap.** Only after `ADMISSION_READBACK.json` is verified, create a new worktree from root's verified retained source; never write the dirty canonical checkout. Bind an exact file allowlist in `OWNERSHIP.md`. Prefer a narrow OS-capability preflight that refuses unsupported execution, or portable receipt/input validation needed by all three routes. Reuse existing interfaces; no replacement scheduler, database migration, broad provider abstraction or platform rewrite. If the required change exceeds the admitted slice, preserve the design and request an explicit scope revision through the writer; continue independent admitted work.
6. **Exercise meaningful failure cases.** In isolated temporary data test malformed or mismatched admission/source hashes; duplicate event identity; wrong mission/host; unsupported Windows process controls; missing required local artifact; oversized or timed-out result; unknown effect after interruption; absent usage fields; and stale-owner takeover denial where the selected change touches ownership. Use fake executors/adapters; no real accounts, external effects, model calls or live ledger mutations. Run only directly affected regressions, preserving first failures. Tests that merely assert the implementation text are insufficient.
7. **Produce the Windows acceptance bundle.** Export exact source/diff and bounded test inputs, manifest hashes, expected observations and receipt schema for root to assign to a Windows worker. Mac-generated output cannot prove Windows execution. Actual Windows acceptance must show local source, process/results, receiving artifact hashes and recovery evidence with Mac disconnected where required. Do not claim that pass before the corresponding Windows receipt exists.
8. **Freeze and return.** Pin final SHA/tree/diff and task traceability, summarize unresolved cases and produce a compact external review packet. Root arranges independent review and integration; do not invoke another Claude model to review this batch. No Done while admission, review, integration, external action or host acceptance remains missing.

## Return contract

Own only `/Users/pchordia/Documents/ChatGPT/Astra/planning/windows-primary-wave-2026-09-12/returns/claude-mac-runtime/` and the newly admitted isolated worktree. Choose a unique return sibling if occupied. Include `SESSION_RETURN.md`, `OWNERSHIP.md`, admission request/readback, gap map, contract, Windows handoff/bundle manifest, `TEST_RESULTS.json`, `EVIDENCE_INDEX.json`, `REVIEW_PACKET.md`, `JIRA_OUTBOX.jsonl` and `USAGE.json`. Every task needs an honest disposition, exact evidence and next action. Record each author/repair attempt, machine and observed model; unknown usage stays null with a reason. Commit only owned admitted changes with real task keys if root's launch packet permits commits; never push, merge, publish or activate a service. Root stages/integrates documentation and updates continuity.
