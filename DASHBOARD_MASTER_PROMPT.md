# Master prompt — rebuild Jira analytics using native Jira only

You are the browser/IDE worker preparing and implementing a useful Jira analytics setup for this portfolio. Begin with OPO, Wait How Big and CommerceLint. Deliver the minimum useful views first; analytics must not become a giant prerequisite to their launches.

**Current constraint: native Jira only.** Use existing Jira fields, issue types, links, comments, worklogs, saved filters, boards, built-in gadgets/reports and, only where already available and justified, native Automation. No external dataset/warehouse, BI, marketplace app, custom dashboard service, paid upgrade or new integration. Detailed telemetry stays in native issues/comments/worklogs for now. Do not promise native aggregations that the tenant cannot actually perform.

This is a reusable execution prompt, not a receipt that Jira was changed. Inspect first. The sole Jira writer is task `6046ab83-8402-47e3-b832-f4fe478da7f1`; verify its ownership and route reviewed native changes through it. Do not create a competing writer, bulk-replace values, delete history or fabricate historical actuals. Preserve unrelated work and Digital Temple's separate scope.

## Context and starting objects

Site: `https://priyanshchordia-1779372280524.atlassian.net`. Inspect and reuse dashboard **10002** (historically “Portfolio — Delivery”), dashboard **10003** (historically “Portfolio — AI Execution”) and portfolio board **496**. Reopen their actual names, owners, filters, permissions and gadget configurations; IDs and old observations are not proof of current contents. Start with `/Users/pchordia/Documents/ChatGPT/Astra/CONTINUE_HERE.md` and the relevant `planning/bots-focus-2026-09-12/` mission/experiment documents, not the entire repository or chat history.

User screenshots show:

- A two-dimensional `llm-assignee × Effort` table: only the row “Irrelevant” has counts **8 High, 31 Low, 14 Medium**; other displayed rows are zero. These populated cells sum to **53**.
- A pie with **1,276** total: **None 900, Cursor 151, Codex 111, Human 63, Claude 45, AntiGravity 6**. These categories sum to 1,276.

These are screenshot observations, **not live Jira totals**. The views may use incomparable filters/cohorts, dates, resolved-item settings, permissions or field contexts; missing Effort categorization is another possibility. “Irrelevant” may be a deliberate value; “None” may represent missing data. Exact cause is unestablished. Do not convert either value into a model, executor or effort level merely to make charts attractive. Cursor/Codex/Claude/Antigravity may denote IDE/execution routes rather than exact underlying models. Preserve that semantic uncertainty until source evidence resolves it.

## Tier 1 — MUST: trustworthy minimum

For the Irrelevant bucket, prioritize project-specific/team-managed field scope and duplicate field identity. Atlassian documents a Cloud case where a selected team-managed custom field cannot apply to issues from other projects, yielding Irrelevant. This is a concrete diagnostic lead, not proof of this tenant's cause. Check exact IDs and contexts before choosing a fix. [Atlassian Cloud example](https://jira.atlassian.com/browse/JRACLOUD-72205).

1. **Diagnose the discrepancy.** Capture both gadgets' dashboard/gadget IDs, saved-filter IDs and exact JQL, axes/statistic field IDs, dates/timezone, resolved-item options, row limits, refresh time and viewer identity. Resolve duplicate field names, field types, options, contexts and relevant create/edit screens. Inspect 8–12 representative accessible issues spanning nonzero cells, “None,” “Irrelevant,” different projects and missing Effort. Compare the two key sets and exclusions; distinguish hidden/truncated display from missing values. If access prevents a test, record it. Report the demonstrated cause, or ranked hypotheses with missing evidence.
2. **Define explicit cohorts.** Separate portfolio delivery tasks, first-three mission tasks, execution attempts and experiments. Record exact JQL, issue types, status/date rules, unique-key count, refresh time and known permission limits. Revalidate board 496's backing filter; do not assume it matches either gadget. Exclude Epics and attempt records from delivery-task totals unless clearly labeled. Preserve unresolved/Done distinctions and explain every denominator. Check whether multivalued fields create overlapping categories; distinguish category memberships from unique issues rather than assuming bucket totals are additive. Use field IDs where names are ambiguous; validate every proposed JQL before saving.
3. **Create a field dictionary before repair.** For each reused/proposed field record ID/type/context/options, meaning, planned-or-observed status, source, unit, null semantics and chart eligibility. Separate provider, exact model/version, reasoning effort, IDE/route, host, role, attempt and task. Human is an executor category, not a model; Auto is a route unless the actual model is exposed. Keep “unknown,” “not collected” and “not applicable” distinct. Do not repurpose `llm-assignee` or an existing Effort field until its intended meaning is verified. Start with existing fields and structured comments; add only a small necessary set through the writer.
4. **Reuse two modest dashboards.** On 10002 propose task counts by status/mission, an actionable blocked/review-ready Filter Results list, and Created vs Resolved if workflow resolution semantics are valid. On 10003 show observed-data completeness, eligible task/attempt counts by verified route/role, and a provider/model/effort matrix only when the exact fields and cohort support it. Every gadget title must say what it counts, its cohort/window and whether planned or observed. Empty actuals should remain visibly empty, not be populated from assignments.

Built-in Pie/Statistics/Two-Dimensional gadgets summarize issue counts grouped by supported fields; Filter Results supplies the underlying rows. They are not arbitrary token/cost/number-sum engines. Confirm available fields in this Cloud tenant's actual dropdowns; select-list categories are candidates, while text/number fields may not be available. Never apply Data Center-specific workarounds to Cloud. [Native gadget catalogue](https://support.atlassian.com/jira-cloud-administration/docs/use-dashboard-gadgets/), [Cloud gadget setup](https://support.atlassian.com/jira-software-cloud/docs/add-and-customize-gadgets/), [Cloud numeric-statistics limitation](https://jira.atlassian.com/browse/JRACLOUD-82754).

## Tier 2 — SHOULD: measured execution and delivery

5. **Use a small native attempt pilot.** First inspect existing issue hierarchy. Prefer structured, versioned execution comments on current tasks; introduce native subtasks or linked existing-type issues only where an independent attempt must be searchable/charted. Limit the pilot to a few actual tasks and create separate issues only where needed; preserve every observed author attempt, retry, repair and rereview with its own ID and receipt in structured comments. Never omit actual attempts to satisfy a record cap or create fictitious attempts. Separate linked issues may be necessary where subtasks cannot contain children. Reuse existing records and stable attempt IDs, deduplicate replays, and prevent subtasks/parents from both counting as delivered tasks. Record events as additive comments with timestamps and correction references; Jira history/comments are not an immutable execution ledger.
6. **Separate plans from observations.** Keep original estimate and original scope baseline; track current remaining estimate separately. Planned model/effort/machine never substitutes for observed values. Store task/job/attempt ID, role, start/end/timezone, host, IDE, provider, exact model, effort, outcome, source SHA/evidence, repair cause, reviewer identity and reviewed revision when known. Independent review requires a genuinely separate reviewer execution and, where required, a distinct model—not an author-written verdict. Unknown model identity cannot prove distinct-model review.
7. **Time and effort correctly.** Worklogs contain measured active work only, attributed truthfully; retain the original actor in evidence if the writer logs on their behalf. Do not turn an estimate or observation-window duration into work time. Report elapsed task time, aggregate active execution and owner attention as different measures. Parallel attempts can overlap: two one-hour simultaneous runs are two aggregate execution hours but one elapsed hour. Do not claim native gadgets compute overlap-adjusted utilization. Avoid summing parent rollups again with their children. Native time-tracking reports can show supported estimate/time-spent/remaining totals within their actual project/version and configuration limits; they are not universal portfolio joins. [Native reports](https://support.atlassian.com/jira-software-cloud/docs/generate-a-report/).
8. **Useful quality denominators.** Define first-pass acceptance as accepted first author attempts divided by eligible completed first author attempts; repair rate as tasks needing repair divided by eligible assessed tasks. Count retries/repairs/reviews separately. A rejection can be a successful review. Show numerator/denominator issue lists when a native ratio chart is unavailable; never relabel simple issue counts as rates. Separate code-complete, reviewed, integrated, released and verified business outcome; workflow “Done” alone proves none of these without acceptance evidence.

## Tier 3 — NICE: native operational and business scorecards

9. **Telemetry without invented arithmetic.** Store actual input/output/cached-read/cached-write/thinking token counters with provider definitions and source receipts; counters can overlap across providers, so do not blindly sum them. Record exact billed cost only when available; modeled cost must name dated rates/currency and remain separate. Subscription percentage changes are shared-window proxies, not task-level tokens or dollars. Attribute one shared session total once. Use number fields or compact issue comments for inspectable values and Filter Results where supported; no claim that a pie sums token/cost fields. Missing observations remain blank with a reason, never zero.
10. **Per-bot experiment economics.** Reuse admitted experiment tasks: hypothesis/version, baseline/window, candidate/action receipt, next check, primary outcome, quality guardrails and conclusion. OPO: useful attributable agent exchanges, qualified users and real revenue; WHB: relevant reach/engagement and follower growth; CommerceLint: qualified visits, useful scanner/offer outcomes and revenue. Separate gross follows from unique people, impressions from unique reach, gross sales from refunds/fees/net proceeds, and aggregate versus attributable conversions. Store native scorecard values and evidence; any manually calculated ratio needs its numerator, denominator, window and missing-data treatment. Do not infer causality from a small observational test or force an unmeasured funnel stage into zero.
11. **Low-maintenance native upkeep.** Consider only justified existing native Automation for stable categorization, missing-evidence reminders or guarded within-issue calculations. Verify plan limits, triggers, actor permissions, null/zero handling, replay safety and audit logs. No per-tool-call issue explosion, automatic fake time logs or new polling infrastructure. Due-date filters can surface evaluations without launching a reasoning loop.

## Tier 4 — OVERKILL: optional native depth, unsupported math deferred

After the minimum views are useful, consider these native-only additions where actual data justifies maintenance. They must not delay the first three launches. Search existing dashboards and filters before adding anything; reuse matching objects, avoid duplicates and retain private permissions.

- **Mission delivery views:** one saved-filter view per mission showing admitted work, blocked/review-ready items, releases and verified outcomes, with explicit task/attempt exclusions.
- **Data-quality audit:** missing source revisions, actual model/host, acceptance receipts, reviewer identity or usable telemetry; separate required-but-missing from not-applicable data. Use native searchable fields; comments alone may require a manual issue checklist.
- **Execution matrices:** author/reviewer/provider/effort/host pairs using supported categorical fields and the same eligible cohort. Review actors and reviewed models stay separate. Label planned/observed and unknown coverage; avoid unsupported joins or implying matched task difficulty.
- **Comparable difficulty bands:** freeze thresholds from the documented story-point scale and the actual baseline for a comparable task type/mission. Retain baseline date, field/context, inclusion rules and missing estimates. Use native range filters; points are neither elapsed time nor reasoning effort. Do not compare incompatible project scales.
- **Age and review queues:** use verified native date filters for created/updated/due buckets and stale-review lists. “Days since update” is not time in status. A derived bucket may use native Automation only when this request explicitly authorizes that exact bounded rule, with guard, audit and rollback; otherwise leave it proposed. No broad jobs or automatic activation.
- **Ownership and outcomes:** native Filter Results lists for blocked dependencies, cross-project accountable/reviewer roles, reopened, abandoned and repair dispositions. Verify actual links/status history or recorded disposition fields; preserve original outcomes and distinguish task counts from attempts.

Precise overlap-adjusted utilization, arbitrary token/cost sums, joined economics, causal model rankings and immutable telemetry remain deferred where native Jira cannot demonstrate support. No external analytics, marketplace extensions, metrics service, plan upgrade or public-permission expansion.

## Execution, acceptance and return

Phase A: read-only diagnosis and a concrete field/filter/gadget change list. Phase B: sole-writer implementation of Tier 1 plus the smallest first-three native attempt/experiment pilot. Phase C: add Should/Nice only when usable actual data exists. Capture before/after configuration; prefer additive reversible changes and preserve original estimates, completed history and unrelated fields.

Reopen every changed dashboard, filter and issue after save. Verify gadget totals against their exact filtered unique keys, drilled-down examples, exclusions and data-completeness counts. Test missing/irrelevant values, one real task with an actual review/repair where available, parent/subtask double counting and viewer access. Historical telemetry may be backfilled only from attributable preserved receipts, marked as backfill with event and recording dates. No synthetic acceptance data in production charts.

Keep dashboards and their underlying filters private to appropriate users/groups. Sharing a dashboard is not data redaction; check filter visibility and issue access separately. Public views can expose names, issue summaries and private operational data. Do not enable public sharing or broaden permissions. Any later public share requires an explicitly sanitized native scope and separate authorization. [Dashboard/filter sharing](https://support.atlassian.com/jira/kb/how-to-share-a-dashboard-or-filter-ie-make-it-public-change-its-viewers-and-editor-permissions/), [public-sharing behavior](https://support.atlassian.com/jira-cloud-administration/docs/allow-dashboards-and-filters-in-your-site-to-be-shared-publicly/).

Return a compact diagnosis, four-tier implementation matrix, exact field IDs/semantics, cohort JQL/denominators, gadget map, writer/readback receipts, privacy checks and unresolved native limitations. Identify what is now usable versus proposed. The first three businesses should gain a clear operational view immediately; richer analytics must follow evidence rather than delay launch.
