# First three bot launches — shared worker context

Owner launch priority, 12 September 2026: One Person Ops, Wait How Big and CommerceLint first. Latest host direction: R730 is the central hub for Kai/local bots; Windows is fallback and a development/inference worker; the Intel i9 Mac is staging and secondary fallback. This supersedes earlier Windows-primary bot-hub wording; migration is not complete. The owner also selected Kai, Pri 2.0 and Lipi for the current focused plans; read `../kai-pri-lipi-focus-2026-09-12/README.md` locally or `focus/kai-pri-lipi/README.md` in the cloud library. Launchable products take priority over a general platform rewrite. A running IDE is not a launched product.

| Stream | Owned work | Packet |
|---|---|---|
| Cursor Windows | CommerceLint source, narrow release improvement, qualified release | CURSOR_WINDOWS.md |
| Cursor Mac | OPO product and task/outbox coordination | CURSOR_MAC.md |
| Antigravity Mac | Small WHB content/source/account/readback slice | ANTIGRAVITY_MAC.md |
| Antigravity Windows | Small independent readiness checks | ANTIGRAVITY_WINDOWS.md |
| Claude Mac | Minimum shared runtime/readiness contract | CLAUDE_MAC.md |

Cursor Auto and Multitask are requested. Verify the actual UI/CLI capabilities; a request to use Multitask is not proof it was enabled. Children need distinct files/worktrees and outputs. Parent coordinates within its lane; root owns canonical continuity, cross-lane integration and source migrations. Do not rewrite another worker's artifacts.

Root owns `CONTINUE_HERE.md`, this packet directory outside private returns, and shared planning files. Preserve dirty checkouts. Existing sole Jira writer is Cursor task `6046ab83-8402-47e3-b832-f4fe478da7f1`; Cursor Mac should inspect/reuse it through available native task tools and relay exact outboxes. Never create a competing writer merely because the old task is inaccessible. Planning/readiness artifacts can proceed while native task fields are reconciled; implementation/experiments need exact specification/readback first. No fabricated Jira keys, actuals, reviews or completion.

Latest additions: canonical source/instructions must be cloud-backed, with host-local clones and separate credentials. Existing product remotes should be reused. OPO's preserved source is now privately cloud-backed at pri8771/one-person-ops; read CLOUD_PUBLICATION.md for its exact snapshot and limits. Do not push dirty trees or secrets blindly. **Jira analytics are native-only for now:** no marketplace add-ons, external BI, new metrics service or invented aggregates. Dashboard work is a separate prompt and must not block the first launches.

Two no-tool local Qwen cases passed on Windows; this is not approval of autonomous Jira mutation or Windows unattended recovery. Use deterministic validation and one qualified writer; weak models may draft bounded proposals. Unknown telemetry stays null. Preserve original estimates. Every source commit references real task keys. Independent reviews identify the reviewer/model and exact candidate. Counts of assignments are not measured model success.

Unraid share mapping and Plex assistance are queued after the first three. Do not change servers, networking, media, Pri 2.0, Digital Temple, Retell, service budgets or inherited schedules in this wave. Account-specific resources and public destinations must be verified; ask once for exact missing inputs and continue independent work. Root will not infer a domain from the partial name `shivangchordia`.

Record prompt submission, actual executor/model/machine, task handles, source and return paths, failure or completion separately. Update only your private return as meaningful work occurs; no idle model polling. Do not duplicate a session with an existing verified active receipt.
