# Session router — LEAD-048 — LIVE V1.7 ONLY

## Fable: sole integrator, RELEASED_AWAITING_WORKER_ACK

Branch: `fable/social-bots-v23-fasttrack-20260921` (historical name, not scope).
Assignment: `social-bots/delivery/V17_LIVE.md`.
Ceiling: operational V1.7. After submission await acceptance; after acceptance no V1.8+ development without a fresh owner request. This replaces every previous continue-to-V2.3/V3.0 direction.

Fable owns shared recovery/runtime integration necessary through V1.7, not future strategy/planner/specialist or portfolio work. Preserve new/local work and check shared-file ownership at handoff. Reuse old components only where required and safe; no artificial future dependencies.

## Cursor / legacy implementation: PARKED_SAFE_HANDOFF

No new overlapping source. Preserve/push existing local changes with honest reports so the integrator can consume them. An instruction is not a claim that a process stopped or acknowledged it.

## QA: independent review-only

Review actual <=V1.7 candidate behavior and required negative/live evidence. No runtime source edits or unauthorized model calls. Do not make unused future specialist acceptance a prerequisite for live V1.7. Only ChatGPT accepts artifacts/milestones.

## Operation

One real SESSION_ONCE per fresh session; no periodic heartbeat or repeated heartbeat on resume. Invocation completion and lease renewal are separate. Development stopping at V1.7 neither grants unlimited operation nor automatically disables an otherwise authorized V1.7 service. Respect run-specific grants and their expiry.
