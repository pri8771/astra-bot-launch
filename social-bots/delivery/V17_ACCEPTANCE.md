# V1.7 live exit criteria — LEAD-048

Scope-only refinement. No runtime test or milestone was accepted in this lead turn. MILESTONE_MANIFEST.md and each active artifact card remain authoritative for content; this file specifies the integrated finish line and does not waive predecessors.

## Product capabilities to demonstrate

| Milestone | Required real evidence |
|---|---|
| V0.4 | Existing authorized canary retained; fixed new persona/evidence comparison batch only under fresh explicit approval + lead manifest; genuine provider receipts and predefined material divergence, failures preserved. |
| V0.5 | Current decision-relevant captured bytes -> material claims -> actual support/reviewer events -> final rendered content hash; factual/voice/platform and required cultural gates work in the normal pipeline. |
| V0.6 | social-a, social-b and social-c each complete their real adaptive unpublished loop; prospective experiment and research/review learning, no fabricated publication/performance. |
| V0.7 | Actual native scheduler on verified persistent owner-controlled host; at least three distinct firings, real contention and crash/restart/stale takeover; two real engineering-artifact -> lead -> later-worker cycles with no owner task relay. |
| V0.8 | Fresh identity/route/capability/analytics verification for the required X, Instagram, TikTok, Reddit and Facebook routes. Unavailable routes remain exact blockers, not invented compatibility. |
| V0.9 | One explicitly authorized bounded public canary per general persona; exact destination/content readback/permalink and real compatible observation windows; UNCERTAIN reconciled before retry. |
| V1.0 | All three bots run the ordinary scheduled production loop with multiple opportunities each; publication/measurement/learning/next decisions within granted limits; independence from SwarmAI and owner-gate behavior proven. |
| V1.1 | Supported process/network/provider/storage failures recover or safely block; real restart/lease/duplicate prevention evidence and a practical stop/recovery runbook. |
| V1.2 | Actual route availability plus real evidence affects platform choice; NO_PLATFORM is valid; a choice does not create account authority. |
| V1.3 | Real raw analytics -> normalized observations with source/window/unit/snapshot-delta semantics; missing is not zero and duplicate snapshots are not double-counted. |
| V1.4 | Those real observations update persona-scoped hypotheses and appear in later production reasoning; confidence/contradiction/decay correctly handle missing or stale evidence. |
| V1.5 | A real verified intervention and completed compatible window feed honest success/failure/inconclusive/stopped closeout and later learning; no borrowed cross-persona baseline or fabricated business success. |
| V1.6 | Actual source-supported content becomes materially platform-native reviewed output, with idea/repurpose lineage and repetition control exercised in production. |
| V1.7 | Real permitted community/thread observations -> scoped conversation memory -> observe/ignore/reply-draft decision -> reviewed, correctly bound candidate where appropriate -> persisted outcome and next check. Independent integrated review closes SB-V17-003. |

No product specialist, V2 strategy, V1.8 cultural rollout, V1.9 soak or V3 brand feature is required for this campaign. Keep required V0.5 cultural-review cases; do not remove correctness controls on cultural content already in scope.

## Bounded integrated run

Before execution, pin the candidate SHA/config and freeze the host, bot/account route mapping, grants, stage call budgets, observation schedule and numeric success predicates. Default proposed continuity profile is 24h with multiple scheduled opportunities for every bot; the lead fixes or replaces it before the run based on actual scheduling and granted budget. It is a test profile, not a completion-time promise. Do not import the deferred V1.9 72h requirement. Platform observation windows may exceed the continuity profile; do not truncate them to meet a deadline.

Use the same ordinary runner/entrypoints in rehearsal and real operation, changing only permitted configuration, data and provider bindings. Show all three bots served without first-bot starvation. Reuse compatible prior real evidence where its candidate/environment/meaning is still valid and record the exact reuse mapping. No need to publish on every bot-platform combination merely to demonstrate routing.

Community acceptance uses actual authorized read-only data and reviewed reply candidates; it does not silently authorize sending replies. Do not create fake engagement, fake comments, fake reviewer identities or traffic to satisfy a test. Insufficient real input is BLOCKED_DATA. A legitimate ignore decision is safe but cannot alone prove the candidate/review generation capability; use an appropriate real observed input when available.

## Negative controls before scarce live calls

Direct-library no-grant dispatch; lying fixture/provider metadata; zero/exhausted shared call budget; concurrent last-slot/reentrant calls; stale/revoked grant; wrong account/persona/thread; material content edited after review; source prompt injection; cross-persona memory; repeated effect attempt; uncertain response; killed worker and lost lease; incomplete or replayed observation data. Use harmless sentinels and isolated storage. Do not invoke a real model or public action to test refusal.

If unused future modules remain in the checkout, keep them unreachable from the V1.7 dispatcher. A narrow test must show out-of-scope strategy/planner/specialist entrypoints are not selected. Preserve the code and report unresolved deferred defects; do not widen this campaign into finishing them.

## Final packet

Submit an index mapping every required <=V1.7 artifact to exact code/evidence SHA, command, run ID, real environment and result. Report discovered/pass/fail/error/skip counts separately. Bind raw source bytes, final reviewed payload, provider attempts, scheduler/job history, destination readback and analytics windows to the claim they support. Separate OFFLINE_FIXTURE, REAL_PROCESS, NATIVE_SCHEDULER, LIVE_SOURCE, LIVE_MODEL, LIVE_ACCOUNT, LIVE_PUBLIC_EFFECT and LIVE_ANALYTICS even when a run is mixed.

Include an independent reviewer run on the pinned candidate; a second summary by the implementer is not independent execution. Show basic safe stop, restart/restore and no blind duplicated effects. Record actual active model/public budgets and their expiry. No new test pass, worker heartbeat or acceptance decision is inferred from a lead's assignment commit.

Finish states: READY_FOR_LEAD_REVIEW, FAIL, INCONCLUSIVE, BLOCKED_AUTHORIZATION, BLOCKED_HOST, BLOCKED_ACCOUNT or BLOCKED_DATA. Workers cannot mark ACCEPTED. Once ChatGPT accepts live V1.7, feature work ends. Future artifacts stay parked until a new owner request. An already authorized V1.7 service may continue only within its existing permission; development stopping does not grant or revoke unrelated service authority.
