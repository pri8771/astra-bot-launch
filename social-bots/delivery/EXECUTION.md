# Delivery execution — LEAD-047

## 1. Reconcile once, then build

FINAL_RUN.md is the current release. It supersedes older pause/new-files-only instructions for Fable at the safe handoff. TASKS.json contains C01–C31 closure cards; GATES.json contains test design; neither is a live-action grant or a worker-editable acceptance ledger.

One pinned combined candidate is the unit of testing. Preserve current work; fetch the canonical and worker branches; record exact source refs and new material differences. Do not confuse a lead acknowledgement commit with new implementation. Run the supplied repository-aware validator. Resolve the ten RECONCILIATION records against latest evidence. No broad new roadmap or acceptance weakening is needed.

## 2. Ownership

Fable is sole active integrator for this campaign, including formerly Cursor-owned recovery paths. Cursor/legacy implementation lanes are parked at a safe checkpoint. Preserve any newly arrived source first; do not overwrite active unpushed work or force-push another branch. Independent QA may reproduce tests and submit findings, but a historically named sleeping lane is not a reason to stop all safe engineering. Neither the implementer nor a subagent may self-accept milestones. ChatGPT remains lead.

## 3. Sequence and provisional build use

First: reproduce and fix R07-041 and the specialist accounting/label/integrity failures in reviews/LEAD047_AUDIT.md. Then consolidate the needed Intelligence producer modules onto the same candidate, fix persistence/review semantics, implement missing S20/S21/S22 functionality and integrate H1–H4. Reuse S23 and strategy-store work rather than rebuild it.

Lead explicitly permits code-only consolidation and provisional use of submitted interfaces inside isolated offline integration tests after the boundary regression passes on that candidate. This is not operational acceptance of R07-041, Intelligence or any downstream artifact. The previous dependency on accepted R07-041 remains binding for operational use; it no longer forces all safe development to idle awaiting a separate chat.

A Cxx closure card does not mean the feature behind it already exists. If it depends on unfinished SB-S20/S21/S22 or later feature slices, implement those small packets before executing the closure proof. Keep one bounded change/commit where practical. Never report a scaffold as an implementation.

## 4. Task and evidence rules

Run focused and adversarial tests for each change; run the full relevant suite at integration checkpoints and the complete candidate before submission. Count discovered, passed, failed, errors and skipped separately. Preserve the code SHA followed by a separate evidence commit; do not repeatedly amend a source commit merely to reference its own future SHA.

Persist task status as worker reports in an execution ledger, not by turning planning-only TASKS.json rows into ACCEPTED. Rehearsal evidence for SB-S20-007 is a separate result; it cannot close the LIVE requirement. Current module tests do not prove use by the production runner.

## 5. Real-world campaign

Rehearse through ordinary entrypoints with providers/network/effects unavailable. Then run only those real scenarios for which actual host/account/model/public/data gates are satisfied. A bot cycle is not evidence that a coding worker implemented an artifact. A comment containing lead text is not evidence of lead execution.

One fresh session gets one SESSION_ONCE heartbeat. Existing resumed session IDs are reused without another heartbeat. Locks/fences may renew independently. Do not keep a chat awake to manufacture recurring liveness. Use installed/verified scheduling only within the permitted host and autonomous-call budget; an instruction file itself does not wake a model.

## 6. Finish and continue

Priority is an actually integrated V2.3, followed by the existing V3.0 path. When external authority/data blocks live proof, complete remaining released safe code, negative controls, packaging and exact next-action manifests. Do not weaken positive demonstrations: a justified HOLD is safe behavior but cannot replace the required real measured strategy-change demonstration.

At each handoff, give exact candidate/evidence SHA, test counts, production-entrypoint trace, missing gate IDs and next executable action. Finish as READY_FOR_LEAD_REVIEW or BLOCKED with precise predicates, never a pretend accepted version. Runtime work does not continue after the process stops unless an actual authorized scheduler starts it.
