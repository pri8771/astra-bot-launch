# Version exit matrix

The complete 27 machine-readable rows are in GATES.json (version, predecessor, required artifacts, real evidence scopes, scenario, proof and external gates). The expanded per-stage failure rules remain in `../next-round/LIVE_PROOF_MATRIX.md`. This avoids maintaining a third separate list of milestone truth.

## V0.4

Bounded authorized five-case real adaptive comparison on frozen decision-relevant evidence. Preserve original failures and exact inputs. No extra call is granted by this matrix. Evidence proves only the observed cases, not universal statistical causal reliability.

## V0.5–V0.6

Final-content factual/platform/cultural review on actual captured sources. Each general bot completes the normal unpublished loop with actual adaptive reasoning when required, attributable review, prospective experiment, research learning and next schedule. No fabricated baseline or audience performance. NO_ACTION alone cannot prove the creation/review stages.

## V0.7

Actual native scheduler sessions on a verified owner-controlled persistent host, actual no-overlap/crash/restart, and actual development artifact work across two lead/worker cycles without owner task relay. At least three scheduler-fired bounded invocations across at least two real intervals; each completed start gets one SESSION_ONCE heartbeat. A manual CLI loop, job template or kept-open chat is not recurring scheduler proof.

## V0.8–V0.9

Verify all required account routes using supported current capabilities. Unsupported routes remain blockers, not silently accepted or dropped. Public canaries require separate explicit persona/account/content/count grants, real readback/permalink and compatible measurements. UNCERTAIN attempts reconcile before resend. No metric fabrication or engagement manipulation.

## V1.0–V1.8

Use the exact rows in GATES.json and existing artifact packets. Continuous operation, recovery, real route-aware selection, analytics semantics, audience memory, experiment closeout, factual content lineage, community reads and both cultural personas must each have the specific real production trace they claim. Legitimate abstention is safe behavior but does not prove an unused capability.

## V1.9

Freeze the supported-host stabilization/restore profile before running it. Proposed defaults include 72h for stability and 24h for initial continuity; these are design profiles, not time estimates or authorization. Report scheduled/attempted/completed/missed denominators and all outages/skips. Reuse genuinely compatible evidence without relabeling an earlier environment as the current one.

## V2.0–V2.3

GATES.json's HOLD language describes correct negative behavior. It DOES NOT waive the original positive real measured strategy-change requirement. The real V2.3 mission uses an owner-relevant goal, the production H1–H4 path, at least two distinct specialist roles, bounded real inputs/providers where claimed, safe output adoption and later parent consumption. An integrated fixture rehearsal closes engineering predicates only. All original operational predecessor gates remain.

## V2.4–V3.0

Use actual available long-term data, adequate aggregate samples, genuine temporal trend observations, executed cross-platform plans and consumed resource reservations. Insufficient data stays a blocker. Self-evaluation generates reviewed proposals, not self-approved changes. Final V3.0 needs actual concurrent scoped brands and the complete integrated acceptance packet, not simply new classes and schemas.

Each packet must bind code/config/schema/input/output hashes, environment, command/invocation, actual times, grants, attempt/verification state and independent review. No value named live=true grants acceptance. Workers submit; the lead accepts.
