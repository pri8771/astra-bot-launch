# LEAD-051 — Social Bots V1.7 candidate audit

Timestamp: 2026-09-21 21:55 ET / 2026-09-22T01:55Z
Canonical branch: `chatgpt/social-bots-plan-20260920`
Official phase: **V0.4.x / V0.4 in progress**
Current execution ceiling: **LIVE V1.7 only, then hard stop**

## Material changes since LEAD-050

Fable is no longer stale. Worker session `s-20260921T211438Z-d5589881` ingested the LEAD-048/050 V1.7-only scope and pushed signed material work through `af3fded92eaba5e68c8088737b68c6f043e40a5e`.

Verified worker submissions include:
- `12c807e970b4d7a8f92a7d5db7b7b3a3856c46d6` — shared pre-dispatch authorization/budget gate; worker reports 559 discovered / 557 passed / 2 skipped.
- `b5fd038` — due-work rotation / first-bot starvation repair; worker reports 569 discovered / 567 passed / 2 skipped.
- `c4d31fee669d6ad28b514792bf710853d2e3d53c` — final-content review binding plus prospective experiment semantics; worker reports 678 passed / 2 skipped.
- `3da19a9c2a1d493c60a940eac344c92d06c7c6ab` — selective V1.7 consolidation of Intelligence producers; future V2.0 growth modules excluded.
- `af3fded92eaba5e68c8088737b68c6f043e40a5e` — read-only V1.7 host/provider/account preflight and gate dossier.

Mac Acceptance still has no fresh independent post-LEAD-050 result and is **STALE / ACTION REQUIRED**.

## Lead source finding — SB-R07-041 remains CHANGES_REQUIRED

`12c807e` materially closes the raw registered-callable and real Claude CLI pre-dispatch gaps: live routes re-authorize a canonical scoped manifest and reserve a durable `O_CREAT|O_EXCL` budget slot before invocation; reentry and shared-budget cases are covered by focused engineering tests.

However, the current policy-owned engineering seams still create a caller-controlled no-grant execution capability:

- `model_dispatch.classify()` exempts exact `reasoning.EngineeringStub` objects.
- `model_dispatch.dispatch()` executes `ENGINEERING_STUB` and CLI `NOT_LIVE` injected-runner seams when no dispatch scope exists.
- `EngineeringStub(fn)` accepts any callable, while `ClaudeCodeReasoningProvider(runner=...)` accepts an arbitrary injected runner. Therefore a direct-library caller can wrap a network/model-capable callable in an engineering seam and execute it by omitting scope/manifest.
- The current test suite explicitly proves that a declared EngineeringStub runs outside production with no scope.

This fails the V1.7 negative-control requirement against caller-controlled fixture/capability bypass. The repair is zero-live: no-scope must fail closed for arbitrary callable execution; engineering seams may run only under an explicit policy-owned engineering scope such as `allow_engineering_stubs=True`; production scopes must continue to refuse them.

Required independent Acceptance checks after repair: harmless EngineeringStub sentinel and injected-runner wrapper both refused with no scope, allowed only under explicit engineering scope, refused under production scope; raw/live routes remain manifest-gated; no real inference.

## Other submitted work

`b5fd038`, `c4d31fee...`, `3da19a9c...` and `af3fded...` remain **submitted engineering evidence** pending independent review. No artifact or version is promoted from worker self-report.

The C09 dossier is a useful honest blocker packet: the Fable environment is an ephemeral Linux VM, not a persistent owner-controlled scheduler host; social-platform outbound routes are blocked by the environment; no canonical live authorization manifest exists; account registry/cultural reviewer inputs are absent. These facts block operational evidence rather than satisfying it.

## Lane disposition

- **Fable Integrator: ACTIVE.** Fix R07-041 seam first, then continue only zero-live/non-overlapping V1.7 integration within the hard ceiling.
- **Mac Acceptance: STALE / ACTION REQUIRED.** Independent review-only; no runtime edits. Audit repaired R07-041, then R07-071, due-rotation and C05/C06/C07 negative controls.
- **Cursor/Core/Intelligence: PARKED / evidence-only.** No overlapping source ownership.
- **V0.4 canary: FROZEN / evidence preservation.** No further model call.
- **worker-pc: OUTSIDE CRITICAL PATH** until private-repo clone/auth is demonstrably healthy.

## Authority and milestone truth

Setup PR #1 remains open/unmerged; canonical branch does not change.

Official version remains **V0.4.x**. `SB-V04-002` and `SB-V04-004` remain blocked on fresh explicit owner authorization for controlled causal divergence, and `SB-EVD-002` remains withheld. The previously authorized exactly-one canary call is consumed; no additional Claude/adaptive/product-model call, public effect, account mutation, spend, destructive action, credential exposure, fabricated evidence, engagement manipulation, main/public release, or SwarmAI dependency is authorized.

The heartbeat contract is **ONE SESSION = ONE HEARTBEAT**; the earlier FAST_5M/SOAK experiment is superseded. Fable's resumed session correctly does not emit a second heartbeat solely because of restore/compaction.

## Next critical action

1. Fable repairs the R07-041 engineering-seam no-scope bypass with zero live calls and submits exact tests/evidence.
2. Mac Acceptance independently audits that repaired boundary and executes the currently submitted V1.7 negative/control tests.
3. Only after independent review does the lead reconcile artifact statuses. Operational V1.7 still requires the real evidence chain in `delivery/V17_ACCEPTANCE.md` and remains blocked on host/account/model/public/measurement gates as applicable.
