# LEAD-041 — Social Bots recovery lead review

Timestamp: 2026-09-21T18:52:07Z  
Canonical branch: `chatgpt/social-bots-plan-20260920`  
Official phase: **V0.4.x / V0.4 in progress**

## Disposition

No artifact or milestone is newly accepted or rejected in this review. V0.3 remains accepted/closed. V0.4 remains open because `SB-V04-002` and `SB-V04-004` are blocked on fresh owner authorization for controlled causal-divergence evidence and `SB-EVD-002` remains withheld.

No live Claude/adaptive/model call, new spend, public effect, destructive action, or credential-bearing action is authorized.

## Issue #3 delta

Since the preceding review window, Issue #3 contains Core's submission for source `a73b7b58de8f3669795b81637bff55247d67943c`. The submission reports no model call, identifies real authorization-path defects in the prior design, moves the authorization refusal to the actual provider spawn point, and reports `312 passed, 1 skipped`. No fresh Intelligence or Acceptance implementation submission superseding canonical state was observed in the review window.

Heartbeat/session comments remain liveness/visibility only and do not self-accept artifacts.

## Material source finding

LEAD-040 created `cursor/social-bots-recovery-v07-20260921` from `claude/quirky-shannon-t1377u@918c42e...`, but the verified Core descendant advanced immediately afterward.

The materially important descendant is:

- `a73b7b58de8f3669795b81637bff55247d67943c`: moves the no-live authorization-manifest refusal to `ClaudeCodeReasoningProvider`'s real process-spawn boundary so alternate entrypoints cannot bypass the gate. Injected runners remain test/fixture seams. Worker evidence reports `312 passed, 1 skipped`; no live model call occurred.
- Report/progress corrections then continue through `2f14a5cb08c9019fd174c1f54ecda130fa9308d4` without a later material source change.

Starting recovery from `918c42e...` would therefore have been a stale safety baseline.

## Lead action: recovery baseline repaired

I fast-forwarded `cursor/social-bots-recovery-v07-20260921` from `918c42e...` to the verified descendant history through `2f14a5c...` before adding new lead instructions. This was a fast-forward only; no worker branch was auto-merged into a release branch.

The recovery lane is now assigned:
1. `SB-R07-071` — atomic cross-process `SESSION_ONCE` uniqueness, including an adversarial race regression.
2. `SB-R07-041` — audit/retain the inherited provider-spawn authorization hardening.
3. `SB-R07-044` — divergence verifier.
4. `SB-R07-072` — persistent-host preflight.
5. Continue dependency-ready no-live recovery artifacts.

At review cutoff, the recovery lane is **ASSIGNED / NOT YET EVIDENCED RUNNING** after the LEAD-041 assignment. A lead instruction commit is not worker progress.

## Lane classification

### Cursor Recovery — ASSIGNED / NOT YET EVIDENCED RUNNING

Primary implementation branch: `cursor/social-bots-recovery-v07-20260921`.

Immediate blocker/task is `SB-R07-071`: inherited `SESSION_ONCE` duplicate protection performs duplicate check and append as separate operations and therefore does not guarantee uniqueness across concurrent processes.

### Legacy Core — PARKED / evidence-only

Branch: `claude/social-bots-windows-core-host`.

The useful post-LEAD-040 descendant work has been transferred into the recovery branch. Core must not continue overlapping implementation unless a later canonical review assigns a separate non-overlapping task.

### Intelligence — PARKED / evidence-only

Branch: `claude/social-bots-intelligence-repair-v2`.

`SB-V15-001` remains `CHANGES_REQUIRED`; no status promotion is made. The branch is parked to avoid duplicate implementation ownership during consolidated recovery.

### Acceptance — REVIEW-ONLY STANDBY

Branch: `claude/social-bots-mac-qa-control`.

No runtime source edits. When `SB-R07-071` lands, Acceptance must independently execute the cross-process duplicate-session race and verify exactly one durable record wins. It then audits `SB-R07-041` when explicitly assigned.

### V0.4 canary — FROZEN / evidence preservation

Branch: `claude/social-bots-v04-live-canary`.

The first authorized canary remains accepted as `SB-V04-005`; the later historical duplicate call remains excluded. No additional model call is authorized.

### worker-pc — OUTSIDE CRITICAL PATH

Do not redispatch Social Bots until private-repository clone/auth for `pri8771/astra-bot-launch` is demonstrably fixed.

## Artifact truth

`ARTIFACT_INDEX.json` requires no status change in LEAD-041. Existing accepted / changes-required / blocked / withheld dispositions remain canonical.

Priority Zero remains the V0.4 controlled empirical-divergence gate. The planned five-call existing-subscription batch is **not authorized**. It requires fresh explicit owner authorization plus a matching canonical lead authorization manifest before any provider process can spawn.

## Coordination updates made

- Reconciled `STATE.json` to LEAD-041 and the recovery transfer.
- Reconciled `WORK_QUEUE.md` to the current inherited source and ordered recovery artifacts.
- Reconciled `SESSION_ROUTER.md` to one primary implementation lane plus independent review-only Acceptance.
- Reconciled `WORKER_PERFORMANCE.md` with the post-LEAD-040 Core safety hardening and current lane topology.
- Updated Cursor Recovery instructions/ack.
- Parked legacy Core and Intelligence source ownership.
- Moved Acceptance to independent review-only standby.
- Reaffirmed canary evidence freeze.

## Next critical action

Cursor Recovery must implement and submit `SB-R07-071` with exact source SHA, focused/full tests, and adversarial concurrency evidence. Acceptance must independently execute that race before lead acceptance. No live model call is involved or authorized.
