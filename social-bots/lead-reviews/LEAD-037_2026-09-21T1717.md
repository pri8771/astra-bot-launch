# LEAD-037 — V0.4 empirical-gate decision

Evidence cutoff: Issue #3 through the Acceptance FAST_5M #5 comment at approximately 2026-09-21T17:16Z.

## Decision

**Do not weaken the V0.4 acceptance contract and do not use synthetic/replayed receipts as proof of adaptive causal divergence.**

The cleanest legitimate disposition under the current owner authorization is:

- SB-V04-002 -> **BLOCKED** on fresh explicit owner authorization for a bounded real adaptive divergence batch.
- SB-V04-004 -> **BLOCKED** on the same empirical execution.
- SB-EVD-002 remains **WITHHELD**.
- V0.4 remains **IN PROGRESS**.
- No additional Claude CLI/adaptive/model call is authorized.

This is a blocker classification change, not acceptance.

The detailed execution design is now canonical in `social-bots/V04_DIVERGENCE_ACCEPTANCE_PLAN.md`.

## Evidence reviewed

### Canonical
- STATE.json / ARTIFACT_INDEX.json / MILESTONE_MANIFEST.md / VERSION_ROADMAP.md / WORK_QUEUE.md / SESSION_ROUTER.md.
- RESET_EXECUTION_20260921.md and HEARTBEAT_ASSIGNMENT_PROTOCOL.md.
- SB-V04-002, SB-V04-004 and SB-EVD-002 artifact packets.
- LEAD-036.
- latest relevant AGENT_MESSAGES tail.

### Worker branches
- Core current branch head: `aa46f6abec50d3ae2633918ed844c46f793c7e41`; latest verified worker implementation remains `74a357d51a26a482d4957561ef194353fff5d24e` / `a19046d...`.
- Intelligence current branch head: `84781ac9d85821c99975e8f00fe0dac41b912cf5`; latest verified worker implementation remains `205295531e7755a5045fb1e458d3964d986edd56`.
- Acceptance current branch head: `7ba3ba2ae8d2cf0765bc07f8795e20b7f28fa2c8`; accepted worker evidence remains `72e380bda71561429154e67bc07636b89143a488`.
- Live-canary branch remains `096d20b9062f3f64db080a9687a19aff9cf00b4c` and is frozen.

The current branch-head commits above the verified worker evidence are lead-control updates, not new implementation submissions.

## V0.4 finding

Core's repair is engineering-useful:
- single-variable persona/evidence test design is repaired;
- adaptive receipt replay is fail-closed;
- schema/policy/no-public-effect invariants are useful regression coverage.

But the synthetic receipts are authored test inputs. Replaying one real canary receipt through an `adaptive=true` seam proves integration behavior, not that a live model would causally change output for a different persona/evidence input.

The first authorized canary proves exactly one genuine adaptive response. It cannot establish the required counterfactuals.

The later second live call is excluded because it exceeded the owner's exactly-one authorization. It is not used even where its evidence happens to be technically interesting.

## Future empirical batch

The conservative clean batch is five live adaptive invocations after a new explicit owner authorization:
- social-a / E1 baseline;
- social-b / same E1;
- social-c / same E1;
- one Primandir cultural workspace / same E1;
- social-a / materially different E2.

This yields three persona-only comparisons and one evidence-only comparison while holding the other variables constant.

No call is authorized now. Prepare-only code may be built now and must fail closed without a lead-created authorization manifest.

## Lane assignments

### CORE
1. No live model/provider calls.
2. Build the prepare-only divergence matrix/context-hash/prompt-hash/isolation harness described in V04_DIVERGENCE_ACCEPTANCE_PLAN.md.
3. Add a hard authorization-manifest + call-budget gate, but do not create an authorization manifest and do not exercise the live path.
4. After the prepare-only work is submitted, proceed to dependency-ready **SB-V07-001** host worker/runbook work: OS-level scheduling, one-task claim, invocation receipts, no-overlap and heartbeat durability. Tests must not invoke a live model.
5. Do not claim V0.4 acceptance.

### INTELLIGENCE
1. Finish only the already-assigned SB-V15-001 repair: remove/private/rename ordinary whole-runtime `load` / `load_all` aliases.
2. Add structural regression proving normal production APIs cannot enumerate another persona.
3. Run tests, submit and stop for lead audit.

### ACCEPTANCE / QA
1. No model calls.
2. Independently review new Core/Intelligence submissions when they land.
3. Prepare V0.7 host/heartbeat acceptance checks without editing Core/Intelligence runtime source.
4. Heartbeat transport must not prevent durable local heartbeat logging; this is a control/tooling defect to be covered in V0.7 work.

### LIVE CANARY
Frozen. Evidence preservation only.

## Heartbeat truth

Issue #3 has useful live comments, including Acceptance FAST_5M comments through #5 and Intelligence/Core progress comments.

However the contract says the reset soak is proven by durable `HEARTBEAT_LOG.jsonl` records. Current branch logs still contain no reset-epoch FAST_5M records. Therefore the independently verified durable reset soak remains **0 intervals** for Core, Intelligence and Acceptance.

This does not block engineering. The V0.7 host/heartbeat work will repair the transport coupling so missing `gh` cannot prevent durable heartbeat recording.

## Product-control gap found

MILESTONE_MANIFEST lists V0.5/V0.6/V0.7 artifacts that are absent from ARTIFACT_INDEX. This is a registry completeness defect, not a runtime defect. The lead will add the missing planned registry entries so future parallel work is artifact-addressable.

## Safety

No public social effects, paid API/PAYG/new spend, credentials/secrets, destructive actions, fabricated operational evidence, engagement manipulation or SwarmAI dependency.
