# LEAD-028 — 2026-09-21 04:51 ET

## Done

- Rechecked setup PR #1; it remains open/unmerged, so canonical coordination remains `chatgpt/social-bots-plan-20260920`.
- Audited new signed Windows Core submission `76e96dde4677346fd5b40c8cba4988f6e4c64fee` rather than accepting the worker report alone.
- Re-audited the current V0.4 production entrypoint and provider resolution path.
- Reviewed Mac QA and Intelligence heartbeat/activity truth from durable worker evidence.
- Rechecked the dedicated V0.4 canary branch and `worker-pc` result state.
- Added the missing canonical `SB-V04-004` artifact contract with isolated-variable and adaptive-provider acceptance criteria.

## Evidence / findings

### Core / SB-V04-004 — worker submission not accepted

Core `76e96dde...` adds six divergence tests and honestly labels them engineering-only. The full worker report claims 136 passing tests, but source inspection finds two acceptance-design defects:

1. `test_same_evidence_different_personas_diverge` does **not** use the same evidence: it runs the first persona against `https://example.org/shared` and the second against `https://example.org/shared2`. Persona and evidence both change, so runtime divergence cannot be attributed to persona.
2. `test_same_persona_different_evidence_diverges` does not hold the persona/runtime context constant, so it likewise cannot isolate evidence as the causal variable.

The suite also forces `SBOTS_REASONING=contextual`. `ContextualReasoningProvider` truthfully reports `adaptive=False`; therefore these tests are useful deterministic engineering regressions but cannot satisfy the adaptive behavior required by V0.4 / `SB-V04-002`.

`test_persona_divergence_at_proposal_level` is useful supplemental evidence because it better holds evidence constant, but it still uses the non-adaptive provider.

Disposition: the worker attempt is **CHANGES REQUIRED / not promoted to accepted submission**. Canonical index remains conservative until the repaired packet is submitted. New packet: `artifact-packets/SB-V04-004.md`.

### Core / SB-V04-001 — source posture materially improved, no status promotion this review

Current production launcher `social-bots/bin/run_worker.py` defaults to `require_adaptive=True` unless the explicitly diagnostic `SBOTS_WORKER_ALLOW_DETERMINISTIC` override is set. `runtime/worker.py` documents that production passes this posture into the decision cycle. `runtime/reasoning.py` refuses non-adaptive providers when adaptive reasoning is required. This closes the previously identified silent deterministic-default concern at the production launcher.

I am not promoting `SB-V04-001` in this review because V0.3 predecessor acceptance remains open and the integrated real adaptive route is still awaiting the mandatory canary. Preserve the repaired fail-closed posture.

### SB-V04-002 / SB-V04-003

- `SB-V04-002`: Claude Code adaptive route exists, but all accepted successful outputs remain test/injected evidence. No real subscription-authenticated provider invocation has been accepted. Remains CHANGES REQUIRED.
- `SB-V04-003`: deterministic authority boundary remains source-positive but predecessor/dependency gated. Remains BLOCKED.

### V0.3 gate

No new independent V03-004 execution report exists. Core source remains stable at final V03 implementation `796d4e390bd135167e5de2ff8f586bc07ac7f370` with final prepared evidence `436787b0a63fdae0e89c224a54054607e32b5187` (130 tests OK). `SB-V03-005` remains ACCEPTED. `SB-V03-004` remains held only for packet-required independent execution; V03-006 remains blocked on that gate/final lead reconciliation.

### Mac QA

Mac QA remains hourly-authorized, but no new worker-generated QA report or heartbeat appears after seq10 (`03:57:57Z`). Lead-only assignments do not count as worker liveness. The independent V03-004 execution remains overdue and is still the V0.3 gating QA task.

### Intelligence

No worker source/heartbeat commit has appeared after seq7 (`04:10:36Z`). The prior `03:40:08Z -> 04:10:36Z` interval is ~30.5 minutes, so Intelligence still lacks three consecutive ~15-minute durable intervals and hourly remains unauthorized. More importantly, `SB-V05-001` HTTPS and `SB-V15-001` persona experiment repairs remain unsubmitted. Heartbeat must stay background-only.

### Priority Zero canary

`claude/social-bots-v04-live-canary` still has no worker-generated `SB-V04-005` evidence. There is no accepted real current-source receipt, existing-subscription Claude Code provider invocation, validated proposal, persisted local decision, or zero-public-effect canary chain. Priority Zero remains stalled.

### worker-pc

`socialbots-v03-repair-audit-20260921-01` failed at repository clone before Claude/tests. It contributes zero acceptance evidence. Do not redispatch Social Bots until clone/auth access to `pri8771/astra-bot-launch` is fixed; do not weaken private-repository controls.

## Next

### Windows Core

1. Preserve final V0.3 source/evidence; do not churn V03 without a concrete QA finding.
2. Repair `SB-V04-004` test design:
   - same exact evidence/state/objective, vary only persona/workspace;
   - same exact persona/state/objective, vary only evidence;
   - retain deterministic contextual tests only as supplemental diagnostics;
   - create an acceptance seam for the real adaptive/canary proposal receipt without invoking another unauthorized model call.
3. Preserve the V04-001 adaptive-required production default and V04-003 deterministic authority wall.
4. Do not execute the real canary from the Core lane.

### Intelligence

1. Implement `SB-V05-001` real pinned-IP HTTPS TLS/SNI/certificate path now.
2. Then implement `SB-V15-001` authoritative bot+persona experiment persistence/read APIs.
3. Keep heartbeat prospective/background; hourly remains unauthorized.

### Mac QA

1. Resume hourly coordination heartbeat.
2. Independently execute current final V03-004 lifecycle regressions against implementation `796d4e3...` and report exact commands/results.
3. No Core runtime edits.
4. Continue integration/CI/V2 harness only after the gating report is produced.

### Live canary

Execute `SB-V04-005` immediately on a genuinely subscription-authenticated local Claude Code host, or submit a truthful host/auth blocker. No heartbeat prerequisite. No API/PAYG/new spend. Zero public effect.

## Blockers

- V0.3: missing independent V03-004 execution.
- V0.4: mandatory real `SB-V04-005` canary absent; V04-004 worker test design not acceptance-ready.
- Intelligence: V05/V15 implementation lane stale.
- Mac QA: independent QA lane stale.
- worker-pc: private-repository clone/auth failure.

## Source refs

- Canonical: `chatgpt/social-bots-plan-20260920`.
- Core new signed worker commit: `76e96dde4677346fd5b40c8cba4988f6e4c64fee`.
- Final V03 implementation/evidence: `796d4e390bd135167e5de2ff8f586bc07ac7f370` / `436787b0a63fdae0e89c224a54054607e32b5187`.
- Core test: `social-bots/tests/test_v04_divergence_acceptance.py`.
- Production launcher: `social-bots/bin/run_worker.py`.
- Reasoning path: `social-bots/runtime/reasoning.py`.
- New contract: `social-bots/artifact-packets/SB-V04-004.md`.
- Remote-worker result: `pri8771/remote-workers/results/socialbots-v03-repair-audit-20260921-01.json`.
