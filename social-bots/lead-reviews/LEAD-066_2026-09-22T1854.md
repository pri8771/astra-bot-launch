# LEAD-066 — PR17 suitability / local Mac inference preparation

Review time: 2026-09-22T18:54:00Z

Official product remains **V0.4.x**. Current owner target/hard stop for Bots is **accepted LIVE V1.7**. New bounded implementation work is Codex-direct; no Fable dispatch.

## PR17

PR17 exact `da53159704e3e1dfefb8d7e4d2518fdc889317fb` / tree `32a7e6c18f2fea4e6f6f32b5d7ae65af56f18e6d`.

**Capture-integrity verdict: ACCEPTED.** E1/E2 raw bytes, receipts, HTTP 200/trusted live-capture provenance, byte counts and hashes are internally consistent.

**Final divergence-input suitability: REWORK_REQUIRED.**

- E2 TikTok, published 2026-09-14, is sufficiently current and directly relevant to creator-led social content strategy.
- E1 Meta Reels India, published 2026-06-04, is product-relevant but too old for the intended September “current evidence changes reasoning” acceptance comparison.
- Both receipts show `extraction_status=not_attempted`. Their model-facing signal summaries are not yet bound to an accepted extraction result derived from the frozen raw capture.

Keep E2. Replace E1 with a materially distinct public Social Bots-domain source published within 30 days of capture unless a later lead explicitly accepts a longer-lived source. For both sources, create deterministic extraction/signal-lineage records bound to raw SHA, receipt SHA/id, extraction implementation/version, extracted-text digest, signal digest, URL and publication timestamp.

Do not rebuild the final P0/P1/P2/P3/E0 matrix until the corrected pair returns for lead suitability review.

## Local Mac provider — engineering release only

Owner selected the local Mac for the first bounded proposal experiment. Existing local inventory includes Ollama on loopback and local models including `qwen3.5:9b`.

Codex may implement a minimal LOCAL provider adapter from accepted binding source `fec97738ec0e9407415f60228f7c3938613396c3` (tree `43e80b92d8ae559db55a41ed33329695e6fd03fc`). If worker-runtime ancestry needs PR15 `9de61f6...`, compose it explicitly; do not silently drop accepted repairs.

Allowed production surface:
- `social-bots/runtime/authorization.py`
- `social-bots/runtime/divergence_prepare.py`
- new minimal `social-bots/runtime/reasoning_local.py`
- `social-bots/runtime/model_dispatch.py` only if required for truthful LOCAL classification
- focused tests.

Contract:
- provider mode distinct from Claude, e.g. `ollama-local`;
- endpoint loopback-only, canonical `127.0.0.1:11434`;
- no LAN/WAN endpoint, remote fallback, OpenRouter/API fallback, model download/pull, daemon start/restart, or fallback model;
- exact endpoint + model + config included in PR16 execution binding;
- initial engineering target model `qwen3.5:9b`;
- same P0/P1/P2/P3/E0 structure;
- exactly five durable attempts, pre-consumed, no retries, no sixth attempt;
- failures consume their attempt truthfully;
- no public/account/effect tools.

This releases adapter/tests only. **No real Ollama inference is authorized yet.**

Codex direct owns implementation. One bounded non-implementer Codex reviewer may recommend only; ChatGPT remains formal acceptor.

## Fail-fast diagnostic release

Read-only source review indicates the five-case batch may continue after `provider_unavailable`, `invalid_proposal` or `provider_exception`.

Codex may reproduce this offline only. If case N failure currently permits N+1 reservation/execution, return the reproduction. No source edit is released yet.

Intended contract if confirmed: first non-`proposal_received` stops the batch; failed slot stays consumed; later slots remain unconsumed; no retry.

## Held / authority

Retry/backoff implementation remains HELD. SB-R07-073/074 remain PLANNED. No duplicate SESSION_ONCE/watcher. No live local inference, Claude/model fallback, public action, account mutation, scheduler/host action, spend, merge or deployment is authorized.
