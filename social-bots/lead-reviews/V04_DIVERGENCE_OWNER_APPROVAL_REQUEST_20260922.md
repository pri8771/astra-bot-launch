# V0.4 five-call controlled-divergence owner approval request — PREPARATION ONLY

Status: **DORMANT / NOT AUTHORIZED**

Historical authority:
- The prior exactly-one existing-subscription model-call authorization was consumed by the accepted first V0.4 canary.
- The later duplicate call exceeded that grant and remains permanently excluded.
- No additional live adaptive/model call is currently authorized.

## Exact owner approval text

> Authorize one new bounded V0.4 controlled-divergence batch of **at most 5 live adaptive Claude Code subscription calls**, with **zero new spend**, for SB-V04-002/SB-V04-004 acceptance only. Use the existing subscription-authenticated Claude Code path with `ANTHROPIC_API_KEY` absent, no injected runner, no effect tools, no account mutation, no public post/message/purchase/destructive action, and no PAYG/API fallback. Execute one fixed matrix: social-a/social-b/social-c/cultural-primandir-atman on frozen evidence E1, then social-a on materially different frozen evidence E2. Hold objective/state/policy/provider configuration constant except the declared single variable. Maximum 5 calls, no retries. On provider/schema/authorization failure, stop and preserve the failure. Authorization expires 2 hours after the lead activates the canonical manifest. This authorization is new and does not revive or modify the consumed prior one-call grant.

## Planned matrix

- P0: social-a + E1
- P1: social-b + E1
- P2: social-c + E1
- P3: cultural-primandir-atman + E1
- E0: social-a + E2

E1 is byte/hash frozen and identical for P0–P3. E2 is materially different and byte/hash frozen. Objective, state, policy posture, allowed actions, provider version/model configuration and prompt structure remain fixed except for the declared persona/evidence variable.

## Execution constraints after explicit approval only

- designated executor: one Codex direct isolated V0.4 divergence lane;
- source pinned by the lead before activation (current prepared baseline: `8c86898d1c6641adbf5c9884e1aa7ab2923b1af3`);
- existing Claude Code subscription route only;
- `ANTHROPIC_API_KEY` absent;
- no PAYG/API fallback;
- no retries;
- fail fast on first provider/schema/manifest execution error;
- no account/public/effect tools;
- no spend;
- durable call budget refuses call 6;
- all context/prompt bytes and hashes frozen before call 1;
- activation expires after 2 hours;
- completion/expiry grants no future batch.

This document is an approval request, not an authorization manifest.
