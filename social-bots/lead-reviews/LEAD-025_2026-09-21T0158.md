# LEAD-025 — 2026-09-21 01:58 EDT

## Scope

Fast-track lead review of Windows Core, Intelligence, Mac QA, the dedicated V0.4 canary lane, and `worker-pc`. Canonical coordination remains `chatgpt/social-bots-plan-20260920`; setup PR #1 is still open/unmerged.

Official version remains **V0.3.x**. This review does not promote V0.3 or V0.4.

## Windows Core — material progress, V0.3 still open

### SB-V03-004 — source repair materially closes LEAD-024 defect; keep CHANGES_REQUIRED pending independent execution

Core implementation `7e4345b041b59b9d1b1036dfea388cedf79b4d3d` changed `runtime/worker.py` so the finish/success receipt is written through `fence.fenced_commit`. The `FenceLost` handler now distinguishes loss before the cycle durable commit from loss after the cycle commit but before completion evidence, and the post-commit path writes a truthful failure with `candidate_succeeded=false` instead of a success receipt.

The committed adversarial regression `test_stale_owner_cannot_write_success_finish_receipt_after_takeover` forces takeover before the finish receipt and asserts that no finish receipt exists and the takeover worker owns the lease. Source-level inspection supports the repair.

Lead disposition remains `CHANGES_REQUIRED`, not because a new source defect is known, but because neither Mac QA nor `worker-pc` has independently executed the repaired branch yet. Preserve this implementation unless independent execution finds a defect.

### SB-V03-005 — CHANGES_REQUIRED

Core added useful production-path tests and correctly routed the deliberate runtime-wide `_reconcile` publish-queue read through `isolation.admin_all_records`. Scoped dedup and analytics aggregation also behave correctly in the new tests.

However, the artifact packet explicitly requires raw whole-runtime readers to be structurally admin/internal or equivalently separated from persona-facing production use. The implementation still exposes ordinary callable whole-runtime functions including `pipeline.publish_queue(bot)`, `analytics.events_for(bot)`, and `RuntimeState.content_history()`. The latter is documented as admin-only, but documentation/convention does not prevent a normal caller from bypassing the persona facade. `isolation.persona_publish_queue` and `isolation.persona_analytics` themselves filter the output of those ordinary raw functions.

Required next repair is narrow: keep the logical isolation model, but make raw whole-runtime enumeration explicitly admin/internal at the API boundary and route normal persona-facing reads through scoped functions. Add a regression proving the sanctioned persona-facing interface cannot enumerate another persona's private records by calling a raw runtime reader.

### SB-V03-006 — BLOCKED / PREPARED

Core regenerated the bundle at `b2083b8f1f47c04467e38fcf62c5da274c0b8f67` from implementation `7e4345b...` and reports 127 passing tests. The checked-in `FOCUSED_RESULTS.txt` records all focused suites plus `full: OK`; `MANIFEST.json` binds the evidence to `7e4345b...`.

The bundle is not final acceptance evidence because V03-001/V03-004/V03-005 are not all accepted. Also, the evidence directory does not contain a separate full-suite output/count artifact; the final regeneration should capture the exact full-suite command/output/count rather than only `full: OK` in the focused summary.

## Intelligence lane — stalled

No Claude worker source or heartbeat commit is visible after sequence 7 at `2026-09-21T04:10:36Z`. Durable heartbeat history remains seq5 `03:23:31Z`, seq6 `03:40:08Z`, seq7 `04:10:36Z`; the ~30m28s seq6→seq7 interval does not satisfy the required ~15-minute bootstrap, so hourly cadence remains unauthorized.

More importantly, assigned `SB-V05-001` HTTPS repair and `SB-V15-001` persona-scoped experiment boundary have not progressed. Heartbeat remains background observability; implementation must resume with V05 immediately, then V15.

## Mac QA — hourly-authorized but worker stale

Mac QA's bootstrap remains valid and hourly cadence remains authorized. The durable worker history ends at seq10 `2026-09-21T03:57:57Z`, and no worker-generated independent Core verification report has landed after that. Lead-only branch instruction commits do not count as QA liveness.

Update the QA target to the repaired Core SHA `7e4345b...`: independently execute the finish-receipt takeover regression and independently probe the remaining structural raw-reader/persona boundary. Do not edit Core source.

## Priority Zero — V0.4 live canary still stalled

`claude/social-bots-v04-live-canary` still has no Claude worker-generated canary commit or `SB-V04-005` evidence. Existing branch commits are lead assignments only.

Heartbeat is not a prerequisite. One bounded existing-subscription Claude Code invocation is authorized, with no API PAYG/new spend and zero public effect. The canary must either run on an actually authenticated local subscription host and submit the required real-source/provider/persistence evidence, or report a truthful authentication/host blocker. Until then, V0.4 is not claimable and `SB-EVD-002` remains blocked.

## worker-pc — second Social Bots attempt failed before audit

The earlier Social Bots task failed at private-repository visibility. This review retried with `socialbots-v03-repair-audit-20260921-01`. The run reached `worker-pc` and progressed past the prior private-repository precheck, but failed with `Repository clone failed` before Claude could inspect source or run tests. It contributes zero artifact evidence.

Do not dispatch another Social Bots task until worker-pc GitHub clone/auth access to `pri8771/astra-bot-launch` is actually repaired. The control plane itself is functioning and unrelated worker jobs can succeed, so the blocker is target-repository access rather than runner existence.

## Artifact dispositions

No artifact is newly ACCEPTED in LEAD-025.

- `SB-V03-004`: `CHANGES_REQUIRED` — repaired source looks correct; independent execution pending.
- `SB-V03-005`: `CHANGES_REQUIRED` — structural raw-reader/admin boundary still incomplete.
- `SB-V03-006`: `BLOCKED` / prepared evidence only.
- `SB-V05-001`, `SB-V15-001`: `CHANGES_REQUIRED`, implementation stalled.
- `SB-V04-005`: ready to execute but no worker submission; `SB-EVD-002` remains blocked.

## Next

1. Core: perform only the narrow V03-005 structural raw-reader/admin-boundary repair, then regenerate final V03-006 with exact full-suite evidence. Do not churn the V03-004 repair unless independent verification finds a defect.
2. Intelligence: implement V05-001 now, then V15-001; heartbeat is background only.
3. Mac QA: resume hourly worker heartbeat and independently verify repaired Core SHA `7e4345b...` plus the remaining V03-005 bypass condition.
4. Live canary: execute SB-V04-005 now from an authenticated local Claude Code subscription host or submit a truthful blocker.
5. worker-pc: hold further Social Bots dispatch until repository clone/auth is fixed.

## Safety / authority

No public posting/replies/messages, purchases, paid API/new spend, destructive actions, secrets, fabricated evidence, engagement manipulation, or SwarmAI dependency are authorized.

## 2026-09-21T01:58:00-04:00 — CHATGPT -> CLAUDE — LEAD-025

Done:
- Reviewed Core `7e4345b...` / `b2083b8...`, Intelligence/Mac-QA heartbeat truth, live-canary branch activity, and `worker-pc` result evidence.
- Verified the V03-004 finish-receipt repair in source and committed adversarial test; held final acceptance for independent execution.
- Kept V03-005 CHANGES_REQUIRED on the remaining structural raw whole-runtime reader/admin-boundary defect; kept V03-006 BLOCKED/PREPARED.
- Retried `worker-pc` with a real read-only V0.3 audit; the runner reached execution but repository clone failed before tests, so no artifact evidence was accepted.
- Updated canonical state/queue/performance/packets and refreshed Core, Intelligence, Mac-QA and canary lane instructions/acks.

Evidence:
- Core implementation `7e4345b041b59b9d1b1036dfea388cedf79b4d3d`; regenerated evidence commit `b2083b8f1f47c04467e38fcf62c5da274c0b8f67`.
- `runtime/worker.py`, `tests/test_fencing.py`, `tests/test_production_read_paths.py`, `runtime/isolation.py`, `runtime/pipeline.py`, `runtime/analytics.py`.
- `receipts/evidence/SB-V03-006/FOCUSED_RESULTS.txt` and `MANIFEST.json`.
- Intelligence durable seq5/6/7 and Mac-QA durable seq7/8/9/10 heartbeat logs.
- remote-workers task/result `socialbots-v03-repair-audit-20260921-01`, dispatch run `35566429231`.

Next:
- Core: narrow V03-005 structural admin/raw-reader repair -> final V03-006 regeneration with exact full-suite output/count.
- Intelligence: V05-001 source repair now -> V15-001; heartbeat background only.
- Mac QA: resume hourly worker heartbeat and independently execute/probe repaired Core `7e4345b...`.
- V0.4 canary: execute SB-V04-005 now on an authenticated local subscription host or submit a truthful BLOCKED report.
- worker-pc: no further Social Bots dispatch until astra-bot-launch clone/auth is fixed.

Blockers:
- V03-005 structural reader boundary; independent V03-004 execution; V03-006 final evidence regeneration.
- Intelligence and Mac-QA worker activity is stale.
- No real V0.4 canary worker execution exists.
- worker-pc cannot clone the target Social Bots repository.

Source refs:
- `lead-reviews/LEAD-025_2026-09-21T0158.md`
- `STATE.json`
- `WORK_QUEUE.md`
- `WORKER_PERFORMANCE.md`
- `artifact-packets/SB-V03-004.md`, `SB-V03-005.md`, `SB-V03-006.md`
- branch-local `SESSION_INSTRUCTIONS.md` and `LEAD_ACK.json` files.
