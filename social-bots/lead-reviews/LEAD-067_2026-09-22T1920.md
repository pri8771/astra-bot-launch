# LEAD-067 — PR18 LOCAL adapter verdict / fail-fast repair release

Review time: **2026-09-22T19:20:00Z**

Official product remains **V0.4.x**. Owner target/hard stop remains **accepted LIVE V1.7**. Codex-direct ownership remains in force; no Fable dispatch.

## PR #18 exact-SHA review

PR: https://github.com/pri8771/astra-bot-launch/pull/18

Candidate:
- source **`d8a211fc32802c59b07b1739bef47f9c551ed8dc`**
- tree **`169c386c471b813585e6f441d0be88fcf1ba0158`**
- accepted base **`fec97738ec0e9407415f60228f7c3938613396c3`**
- native packet/evidence **`5ce1001`**, `social-bots/lead-reviews/CODEX_LOCAL_ADAPTER_RETURN_20260922.md`.

### Reviewed LOCAL boundary

The candidate introduces a distinct `ollama-local` provider route and keeps it inside the existing production authorization / binding / dispatch / durable-budget path.

The critical cross-route defect found during implementation is closed:
- a LOCAL manifest cannot authorize the Claude CLI provider;
- a raw/spoofed callable using the LOCAL provider ID is refused;
- LOCAL dispatch requires the exact policy-owned `_LocalInvocation` callable type;
- the reviewed provider configuration must match exactly;
- the provider ID must match exactly;
- source/tree/matrix binding from PR16 remains required;
- local endpoint/model/config are part of the reviewed execution closure;
- endpoint is literal loopback `127.0.0.1:11434`;
- no redirect/proxy/remote fallback/model-pull/daemon-control path exists;
- pinned model name + digest are verified before generation;
- provider failures consume their durable attempt through the existing gate.

Initial local target:
- model `qwen3.5:9b`
- pinned digest `6488c96fa5faab64bb65cbd30d4289e20e6130ef535a93ef9a49f42eda893ea7`.

No real inference occurred.

### CI review

The candidate also ports the released read-only CI workflow:
- Python 3.11;
- `contents: read`;
- no secrets;
- artifact-index parse;
- optional artifact graph validator truthfully skipped when absent;
- actual Social Bots unit/regression suite executed;
- adaptive/provider routes hard-disabled;
- no deployment/publish/model action;
- no auto-merge.

Exact-head hosted workflow run **35773340055** completed successfully.

### Evidence

Native evidence reports:
- **12 LOCAL focused tests passed**;
- **784 full tests passed / 2 existing genuine-live skips**;
- Ruff/new-file formatting/diff checks clean;
- independent reviewer reproduced the cross-route defect before repair and the refusal afterward without network/model execution;
- exact-head hosted Python 3.11 CI success.

### Formal verdict

**PR #18 LOCAL provider adapter + read-only CI port ACCEPTED ENGINEERING at exact SHA `d8a211fc32802c59b07b1739bef47f9c551ed8dc`, tree `169c386c471b813585e6f441d0be88fcf1ba0158`.**

Acceptance is engineering-only. It does not authorize real local inference, rebuild the final V0.4 matrix, satisfy source suitability, promote V0.4, or create any public/scheduler/spend authority.

---

## Fail-fast diagnostic disposition

Native diagnostic:
- proposal **`b188f03`**
- `CODEX_MAC_LOCAL_PREFLIGHT_20260922.md`
- `evidence/codex-failfast-20260922/`.

Confirmed on exact accepted source `fec97738...`:
- P0 `provider_unavailable` -> all five cases still run; all five slots consumed;
- P0 `provider_exception` -> all five still run; all five consumed;
- P0 `invalid_proposal` -> all five still run; all five consumed;
- five valid proposals correctly run five cases.

This violates the prepared fail-fast batch contract.

### Bounded repair RELEASED

Codex may implement only this behavior change, preferably on top of accepted PR18 unless source ancestry requires an explicit composition candidate:

1. In `execute_batch`, append the result for case N.
2. If `result["outcome"] != "proposal_received"`, stop the batch immediately.
3. Preserve the failed case's already-consumed slot and truthful outcome.
4. Do not reserve, construct or invoke later cases.
5. Later case slots remain unconsumed.
6. Return explicit batch-stop evidence, including:
   - stopped case ID;
   - stopped outcome;
   - unattempted case IDs.
7. Valid five-case runs remain unchanged.
8. No retry branch.
9. No change to LOCAL/Claude binding, call-budget semantics, source suitability or public/effect authority.

Required offline red/green tests:
- first-case `provider_unavailable` => 1 consumed slot, 4 unattempted;
- first-case `provider_exception` => 1 consumed, 4 unattempted;
- first-case `invalid_proposal` => 1 consumed, 4 unattempted;
- failure at a middle case => preceding successes + failed slot consumed, later cases unconsumed;
- five `proposal_received` => all five consumed/results preserved;
- no network/model call in tests.

Worker: **Codex direct implementation**. Independent recommendation role: **Codex-Acceptance-Review** with no implementation edits. ChatGPT remains formal acceptor.

---

## Capture/extraction preparation — reaffirmed release

LEAD-066 capture correction remains released as **read-only public preparation**:
- keep E2;
- replace stale E1 with one materially distinct current public Social Bots-domain source;
- produce deterministic extraction/signal-lineage records for both E1 and E2, bound to exact frozen raw bytes and receipts;
- no final P0/P1/P2/P3/E0 matrix yet;
- no model call or manifest activation.

## Held / authority

- real local Ollama inference remains **NOT AUTHORIZED**;
- final matrix remains held pending corrected source suitability;
- retry/backoff implementation remains HELD;
- SB-R07-073/074 remain PLANNED;
- no Fable dispatch, duplicate SESSION_ONCE/watcher, public/account action, scheduler/host action, spend, merge or deployment.
