# LEAD-040 — V0.7 recovery audit and decomposition

Evidence cutoff: current GitHub branches through `claude/quirky-shannon-t1377u@918c42e2a4602589997a7c625d994e049f266d6d`.

## Verdict

The project is **not V0.7** and is behind the owner's target because required LIVE checkpoint evidence is missing.

No milestone status is promoted in this review.

## Code audit highlights

Positive:
- 918c42e hardens live-provider authorization and call accounting.
- worker_once, direction consumption, invocation receipts and scheduler assets exist.
- worker-reported full suite: 306 passed / 1 skipped.
- committed POSIX SIGKILL proof leaves incomplete receipt, refuses overlap under live lease, and recovers after TTL.

Not sufficient for V0.7:
- scheduler proof is explicitly a direct CLI/CCR engineering demo, not native OS-scheduler firings on an owner-controlled persistent host.
- V0.4, V0.5 and V0.6 are not sequentially accepted.
- two-cycle lead->later-worker autonomous direction consumption is not proven.

Concrete code defect:
- `runtime/session_heartbeat.py::emit` calls `find_session(session_id)`, then later appends to the target ledger. The uniqueness decision and durable append are not one atomic critical section, so concurrent processes with the same session id can both pass the check. Sequential tests do not close this race.

## Recovery decision

Create one Cursor recovery lane from the best current source, and decompose work into small SB-R07-* artifacts. Separate engineering artifacts from LIVE-evidence artifacts.

Implementation branch created:
`cursor/social-bots-recovery-v07-20260921` from `918c42e...`.

See `RECOVERY_TO_V07.md`.

## Authority

No live adaptive/model call, public effect, PAYG/new spend, credentials exposure or destructive external action is authorized by this audit.
