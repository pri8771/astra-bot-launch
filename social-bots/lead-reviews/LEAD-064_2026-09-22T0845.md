# LEAD-064 — PR15 reconciliation verdict / V0.4 matrix-binding REWORK / evidence-suitability judgment

Review time: **2026-09-22T08:45:00Z**

Canonical coordination remains `chatgpt/social-bots-plan-20260920`. Official product phase remains **V0.4.x / V0.4 in progress**. Owner target remains **LIVE V1.3 with genuine tests**.

## PR #15 exact-SHA review — persistent reconciliation gate

Candidate:
- source **`9de61f6c8db9d06b11e9c71a8f84b2f7802633c7`**
- tree **`be23e6784c23db399db423f918ca4df912f2f252`**
- base accepted composition **`8c86898d1c6641adbf5c9884e1aa7ab2923b1af3`**
- native evidence **`e6acced`**, `CODEX_RECONCILIATION_REPAIR_20260922.md`.

The repair matches LEAD-063:
- every bounded unit runs the existing read-only reconciliation before `decision.run_cycle`;
- literal `safe is True` is required to proceed;
- unsafe returns `blocked_reconciliation_unsafe`;
- no decision cycle/new candidate/experiment/content/queue/analytics effect occurs;
- no normal finish receipt is written;
- a truthful failure receipt + blocked heartbeat are written;
- only the current worker's owned lease is released;
- stale takeover metadata remains preserved;
- a fresh subsequent process against unchanged unsafe queue reconciles again and remains blocked despite `reconcile_required=false`;
- the unsafe queue bytes are not repaired or rewritten;
- no retry policy/effect journal scope was added.

Evidence reports:
- accepted-base red: **8 failures**;
- focused gate: **3 passed**;
- existing fencing: **10 passed**;
- worker: **4 passed**;
- full suite: **764 run / 762 passed / 0 failed / 0 errors / 2 genuine-live skips**;
- changed-file Ruff, syntax/diff clean;
- clean exact-source two-fresh-process proof: stale takeover blocked, immediate fresh process blocked, 2 failure receipts / 0 finish / 0 decision calls, queue bytes stable.

No exact-SHA GitHub CI/workflow exists; results are author/integration engineering evidence.

### Formal verdict

**PR #15 persistent reconciliation repair ACCEPTED ENGINEERING at exact SHA `9de61f6c8db9d06b11e9c71a8f84b2f7802633c7`.**

This closes the first LEAD-063 SB-V11 defect only. `SB-V11-001` remains **CHANGES_REQUIRED** because the separate durable retry/backoff/exhaustion gap is still unresolved and implementation remains held.

---

## V0.4 activation-preparation diagnostic — formal finding

Native proposal/evidence:
- **`ac36e6fb6f8c415d24f1f432dc24c3290f549b28`**
- `social-bots/lead-reviews/CODEX_V04_APPROVAL_PREPARATION_20260922.md`
- `CODEX-V04-BOUND-APPROVAL-PREPARATION-20260922` hashed evidence.

At exact accepted source `8c86898d...`, both agent and root reproduced that:
- an internally consistent changed matrix/objective,
- with the same artifact scope, lane, run scope and call count,
- passes the real authorization gate,
- reaches an offline sentinel provider,
- while differing from the owner-reviewed matrix bytes.

Existing per-case context/prompt consistency checks are real, but they validate **the caller-supplied matrix**, not the specific matrix the owner/lead reviewed. Current authorization does not bind source SHA/tree or matrix/freeze closure.

### Formal disposition

**REWORK_FOUND.**

A live V0.4 batch must not be activated until exact reviewed source + matrix/evidence/prompt closure are bound into the executable authorization path and checked before budget/provider construction.

Zero live calls are authorized by this finding.

---

## Explicit E1/E2 suitability judgment

Current prepared snapshots:
- E1: `https://example.com/`
- E2: RFC 8259 from `rfc-editor.org`

Both are genuine HTTPS captures with trustworthy capture receipts and are useful **engineering fixtures for isolation/hash/gate tests**.

They are **NOT suitable as final V0.4 product-acceptance evidence** for SB-V04-002/SB-V04-004 because:
- they are controlled generic documents, not current social/audience evidence relevant to the bots' real persona/objective decisions;
- example.com carries essentially no meaningful changing social signal;
- RFC 8259 is a stable technical specification rather than current audience/context evidence;
- successful divergence on these inputs would prove model sensitivity to arbitrary content, not the required adaptive response to materially different current evidence in the Social Bots product context.

Therefore:
- the current E1/E2 matrix may remain ENGINEERING preparation evidence;
- the dormant owner approval request must **not** activate against these snapshots;
- before any live batch is approved/activated, E1/E2 must be replaced by two genuine, current, materially distinct public-source captures relevant to the bots' research/audience/content domain, captured through the accepted collector and frozen before call 1;
- E1/E2 suitability itself must be reviewed and pinned by the lead before activation.

No new public-source capture is authorized by LEAD-064.

---

## Bounded repair release — exact source/matrix execution binding

Codex is RELEASED to implement **one narrow binding repair**. No model calls.

### Required authorization binding

Extend the live V0.4 authorization contract so a grant cannot authorize an arbitrary internally consistent matrix.

A canonical manifest for this batch must bind at minimum:
- `source_sha`
- `source_tree`
- `execution_matrix_sha256`

The execution matrix digest must be a deterministic digest over the complete reviewed execution closure, including:
- run scope/lane/artifact scope;
- objective and held-constant state;
- provider configuration;
- exact ordered case sequence P0/P1/P2/P3/E0;
- each case's persona id/slot;
- each case context digest;
- each exact prompt digest + prompt-context digest;
- each evidence raw-bytes digest + receipt digest + evidence id/provenance;
- E1/E2 capture receipt identities and URLs;
- required comparison definitions.

Equivalent binding by a freeze-manifest digest is acceptable only if that digest explicitly and deterministically commits to this entire closure.

### Required enforcement path

1. `authorization.validate_manifest` requires these binding fields for the V0.4 divergence manifest.
2. `ExecutionGrant` carries them.
3. The batch executor obtains a trusted/read-only source identity and computes the current execution-matrix closure digest.
4. **Before CallBudget creation, slot reservation, provider construction, or dispatch scope installation**, compare actual source SHA/tree + execution matrix digest to the grant.
5. Mismatch => fail closed with no slot and no provider construction.
6. The deep dispatch/re-authorization path must also retain the binding context so a direct V0.4 dispatch under the same artifact/lane/run scope cannot omit the matrix/source binding.
7. Each reserved slot and resulting receipt/ledger evidence records the bound source SHA/tree + execution matrix digest.
8. Existing per-case `verify_written`, single-variable isolation, expiry, max-call, no-retry, no-spend, no-API-key and posture checks remain required.

### Tests

At minimum prove:
- reviewed exact source + exact matrix binding may pass preflight using an offline sentinel/engineering test path only;
- changed objective/matrix with same artifact/lane/run scope/count is refused before slot/provider;
- changed prompt/evidence closure is refused;
- changed source SHA or tree is refused;
- omitted binding fields are refused;
- deep/direct dispatch under the V0.4 production scope without required binding is refused;
- exact binding values appear in slot/receipt evidence;
- zero live/provider/network calls in tests.

Do not activate any manifest, capture new live evidence, or make any model call.

---

## Retry policy remains HELD

LEAD-063 retry policy remains specified but **not released for implementation**. PR15 acceptance does not change that.

## Dormant owner approval request state

The five-call owner request remains **NOT AUTHORIZED**.

It must not be presented for activation until:
1. the binding repair is accepted;
2. suitable current E1/E2 captures are prepared and lead-reviewed;
3. exact source SHA/tree and final execution-matrix digest are pinned in the dormant review package.

The prior one-call grant remains consumed. The unauthorized duplicate remains excluded.

## Authority

No Fable handoff. No new SESSION_ONCE. No model/provider/account/network/public action, scheduler/host action, spend, merge or deployment is authorized.
