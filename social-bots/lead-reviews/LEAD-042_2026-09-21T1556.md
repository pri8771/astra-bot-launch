# LEAD-042 — Cursor recovery submission audit

Reviewed at: 2026-09-21T15:56:50-04:00  
Canonical branch: `chatgpt/social-bots-plan-20260920`  
Official phase: **V0.4.x / V0.4 in progress**

## Lead disposition

No product-version promotion occurs in this review. V0.3 remains accepted/closed. V0.4 remains blocked on empirical adaptive divergence (`SB-V04-002`, `SB-V04-004`) and `SB-EVD-002`; no additional live model call is authorized.

Cursor Recovery is now materially ACTIVE. The fresh recovery session `s-20260921T191500Z-a23cc77e` emitted one durable `SESSION_ONCE` heartbeat and pushed signed recovery commits through `d7ecb256d430f437f4ad9249480b6430af52ecf9`.

## SB-R07-071 — SUBMITTED / independent execution required

Worker source: `0c74336b793189b4ba32d1f1229de0fb4d3e5ebb`.

Lead source inspection confirms the duplicate-session check and durable append are now inside one `fcntl.flock` critical section, with a process-global lock file for the shared report filesystem. The regression uses real OS multiprocessing (`fork`), 8 processes x 20 rounds, and requires exactly one durable winner for each duplicated session id. Worker evidence reports 16 focused passes, 2 race passes, and 314 passed / 1 skipped full-suite.

Disposition: **SUBMITTED, not accepted yet.** The artifact contract and current router require independent Acceptance execution. Guarantee remains single POSIX host/filesystem; no native-Windows or cross-host uniqueness claim is inferred.

## SB-R07-041 — CHANGES_REQUIRED

Worker source: `21cc2e7a65750d20dc609b9e9517f920157389a2`.

Positive: the real Claude CLI route now keys authorization on an import-time captured real runner and checks the canonical live authorization at the actual subprocess spawn boundary. Rebinding `_REAL_CLI_RUNNER` no longer disables that guard.

Material defect: the same source still exposes `ModelReasoningProvider` as `adaptive=True`, accepts a process-registered `_MODEL_CALLABLE`, and invokes that callable directly from `propose()` without a canonical live-authorization check. `resolve_provider()` constructs this provider for `SBOTS_REASONING=model`. The worker report itself notes that ad-hoc direct `decision.run_cycle` callers remain dependent on caller discipline. That violates the packet requirement that **every live-capable entrypoint fail closed without a valid manifest**.

Required repair, with zero live calls:
1. structurally guard the `model` callable route at provider/registration/invocation boundary with the same canonical authorization contract, or make that route structurally diagnostic-only/non-live so a registered live callable cannot execute outside the manifest gate;
2. add a direct-library adversarial regression that registers a callable, selects `SBOTS_REASONING=model`, bypasses `worker_once` / `run_worker`, and proves no callable invocation occurs without a valid manifest;
3. retain injected deterministic fixture seams only when they cannot perform a live/provider effect;
4. rerun focused and full suites and submit exact source/evidence.

Disposition: **CHANGES_REQUIRED.** Do not perform any model call while repairing it.

## SB-R07-044 — SUBMITTED / pending independent audit

Worker source `5179217ea222e94caa5580104fbc6aaacd4b2192` provides an engineering-only divergence verifier. It correctly rejects synthetic fixtures for acceptance, treats self-declared real labels without a call ledger as incomplete, never self-accepts, and reports 322 passed / 1 skipped. Keep SUBMITTED pending independent review and future genuinely authorized live evidence.

## SB-R07-072 — SUBMITTED / host is unsuitable

Worker source `7f59f915b9f5bb60691d06215a463518fd4519c7` truthfully classifies the current Cursor host as `UNSUITABLE_NOT_PERSISTENT_OWNER_HOST` and `suitable_for_v07_live_scheduler=false`. Do **not** install or claim `SB-R07-073` on this host. V0.7 LIVE scheduler proof must run on an owner-controlled persistent host.

## Later worker submissions

The Cursor lane also reports engineering submissions for `SB-R07-042`, `SB-R07-051`, `SB-R07-052`, `SB-R07-053`, and `SB-R07-061`, with the cumulative worker-reported suite reaching 416 passed / 2 skipped. These are useful submissions but are **not accepted in this review**; their exact source/evidence remains queued for lead/independent audit. No later scaffolding changes the official product version.

## Lane classification

- **Cursor Recovery: ACTIVE.** Primary implementation lane; immediate task is only the bounded `SB-R07-041` direct-model-callable authorization repair. After pushing it, pause overlapping source expansion until Acceptance has executed `SB-R07-071` and audited the repaired `SB-R07-041`; dependency-safe packaging/audit work may continue without live effects.
- **Acceptance: STALE / ACTION REQUIRED.** No post-LEAD-041 worker QA commit is visible. Start one fresh review session, emit one `SESSION_ONCE` heartbeat, independently execute `SB-R07-071` against the submitted source, then audit repaired `SB-R07-041`. No runtime edits and no model calls.
- **Legacy Core: PARKED / evidence-only.** No overlapping implementation.
- **Legacy Intelligence: PARKED / evidence-only.** `SB-V15-001` remains changes-required and is not reopened on that branch during recovery consolidation.
- **Canary: FROZEN / evidence preservation.** No further model calls.
- **worker-pc: OUTSIDE CRITICAL PATH.** Do not redispatch until private-repo clone/auth is demonstrably fixed.

## Coordination correction

The old reset prompt's timed FAST_5M / SOAK_15M_24H heartbeat language is superseded by canonical `HEARTBEAT_ASSIGNMENT_PROTOCOL.md`: one fresh worker session emits exactly one real durable `SESSION_ONCE` heartbeat. Recurring V0.7 liveness is proven by repeated native OS-scheduled bounded sessions, not by keeping a chat alive.

## Next critical action

1. Cursor fixes `SB-R07-041` direct `model` callable manifest bypass, with no live call.
2. Acceptance independently runs the `SB-R07-071` cross-process race and reports PASS/FAIL.
3. Acceptance then audits the repaired `SB-R07-041` direct-library bypass regression.
4. Do not run `SB-R07-073` on the current Cursor host.
5. V0.4 empirical divergence remains owner-authorization blocked; do not infer authorization.
