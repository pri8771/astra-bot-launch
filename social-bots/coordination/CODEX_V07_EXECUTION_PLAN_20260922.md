# Social Bots — V0.7 execution plan

Target: genuine Social Bots V0.7, then stop. The latest owner direction sets
V0.7 as this plan's ceiling. The current canonical state remains V0.4.x and
V0.3 is the latest closed milestone.

## Baseline

- Current frozen V0.4 source-input preparation: `033b973351a9e7d1b2204f9037f901bac1810140`.
- Exactly two trusted public capture receipts are pending lead suitability
  review; they are inputs only, not a V0.4 or V0.7 acceptance.
- R730 is a host candidate only. It is not an authorized Social Bots host and
  must remain independent from SwarmAI.

## Ordered path

1. **V0.4 — real adaptive canary.** Obtain lead suitability for the frozen
   captures, then a fresh bounded model-call grant and canonical manifest.
   Run only the approved count of actual subscription-authenticated adaptive
   calls, persist a local decision, and obtain independent evidence review.
2. **V0.5 — research/factual review.** Deliver machine-captured source
   receipts, claim-to-source review, native formatting repair, cultural evidence
   binding, and timestamped/hash-bound source evidence.
3. **V0.6 — three honest dry runs.** Produce real Social-A/B/C dry-run packets,
   an independent candidate review, and a validator proving no external
   publication occurred.
4. **V0.7 — persistent lead/worker loop.** After V0.4–V0.6 acceptance, obtain
   a separate host and scheduler grant for an owner-controlled persistent Bots
   host. Prove bounded native invocation receipts, one SESSION_ONCE for each
   fresh worker session, direct ChatGPT-direction acknowledgement and later
   consumption, crash/restart/stale-takeover/no-overlap behavior, and two
   worker-to-GitHub-to-lead-to-later-worker cycles without owner relay.
5. **Independent review package.** Bind every result to exact source, host,
   receipt, and review evidence. Stop at V0.7; do not activate V0.8 routes.

## Current safe implementation work

- Obtain the outstanding suitability verdict for the two frozen captures.
- Reproduce and repair only a concrete V0.4 production-path defect if lead
  review identifies one; tests must cover the adverse authority/failure path.
- Prepare, but do not dispatch, the V0.7 host/runbook and scheduler evidence
  request until V0.4–V0.6 are genuinely accepted.

## Evidence and stop rules

No fixture, injected runner, shell loop, open chat, or scheduler configuration
alone is V0.7 proof. No provider call, public action, account mutation,
credential entry, scheduler installation, or R730 reuse is authorized merely
by this plan.
