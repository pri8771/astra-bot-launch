# SB-V04-005 — Real V0.4 Live Canary Worker Report (Lane 3)

- Artifact: **SB-V04-005** — Real adaptive V0.4 canary (ZERO-priority promotion gate)
- Persona: **social-a / The Ledger** (general)
- Date: 2026-09-21
- Result: **canary executed end-to-end with a real subscription Claude Code call; zero public effect.**
- Return status requested: **SUBMITTED** (lead audit owns ACCEPT; V0.4 not self-marked complete).

> Delivery note: authorized session branch is `claude/social-bots-mac-qa-lane3-ordpt6`.
> Evidence + report are pushed there per session git policy; the lead/operator can
> port them onto `claude/social-bots-v04-live-canary`. Content is independent of push location.
> Runtime under test = canary branch head `8febedb7987500b7cce1c9cc3d38e5b4be177c02`,
> `runtime/reasoning_cli.py` (`ClaudeCodeReasoningProvider`), used unmodified.

## Preflight (host / provider metadata)

| Field | Value |
|---|---|
| OS | `Linux-6.18.44-fc-v37-x86_64-with-glibc2.39` |
| Python | `3.11.15` |
| Claude CLI path | `/opt/node22/bin/claude` |
| Claude CLI version | `2.1.278 (Claude Code)` |
| `ANTHROPIC_API_KEY` present | **false** (boolean only; value never read/printed) |
| Isolated `SBOTS_HOME` | `…/scratchpad/canary-home` (dedicated canary home) |
| Reasoning mode | `SBOTS_REASONING=claude-cli`, `SBOTS_REASONING_REQUIRE_ADAPTIVE=1` |

## Exact safe commands

```
# isolated home + evidence dir
export SBOTS_HOME=<canary-home>          # dedicated, not the QA working tree
export SBOTS_REASONING=claude-cli        # real adaptive route (ClaudeCodeReasoningProvider)
export SBOTS_REASONING_REQUIRE_ADAPTIVE=1
unset ANTHROPIC_API_KEY                  # subscription/OAuth route only, no PAYG

# driver performs: live fetch -> hash -> Signal -> research.capture ->
# decision.run_cycle(bot, persona, require_adaptive=True)
python3 canary_driver.py <runtime_dir> <evidence_dir> $SBOTS_HOME
```

The actual model subprocess built by the unmodified adapter (`runtime/reasoning_cli.py`):

```
claude --print --output-format json --permission-mode plan \
       --permission-prompts none \
       --disallowedTools Bash Edit Write NotebookEdit WebFetch WebSearch Read Glob Grep Task Agent
```

Prompt delivered on **stdin** (never in argv); child env has `ANTHROPIC_API_KEY`
stripped; effect tools all denied; permission prompts auto-denied. The `runner`
seam was **not** injected — the default real-CLI runner was used.

## Source metadata (live capture)

| Field | Value |
|---|---|
| URL | `https://pypi.org/pypi/pip/json` |
| Provenance | `live-canary-capture` (public, no auth/cookies, fetched live, not a fixture, not pasted) |
| Retrieved at | `2026-09-21T16:15:38+0000` |
| HTTP status | `200` |
| Content length | `228494` bytes |
| SHA-256 | `6e11a831a6a7c5aac56ee8634c4041a4d86b19380f43879a76a1903dcde9d814` |
| Bounded title | `pip 26.2.1` |
| Bounded summary | `PyPI package 'pip' current latest version 26.2.1; N released versions indexed; …` |
| Signal id | `sig-de31a6915a99` |

**Source-selection limitation (documented, not hidden):** this environment's egress
policy returns `403` for arbitrary public hosts (verified against Hacker News, the
Federal Reserve feed, and api.github.com). `pypi.org` is on the session's allowed
direct-connect list and is a genuinely public, current, no-auth, live-fetched
source, so it satisfies every SB-V04-005 source rule. A different authorized host
(e.g. the user's Mac, where general egress is available) could use a
finance/news source more thematically aligned to The Ledger; the acceptance chain
is identical.

## Provider identity / outcome

| Field | Value |
|---|---|
| provider_id | `claude-code-subscription-v1` |
| provider class | `ClaudeCodeReasoningProvider` (adaptive=true) |
| available | true |
| call window | `16:15:39+0000` → `16:16:12+0000` (~33 s real model latency) |
| model recommended | `NO_ACTION` |
| **deterministic policy selected** | **`RESEARCH_MORE`** (`recommended_followed=false`) |
| execute.effect | `flagged research need (local only)` |
| publish queue length | `0` |
| publish_authorized | `false` |

The model returned three schema-valid alternatives (RESEARCH_MORE, NO_ACTION,
CREATE_CANDIDATE) with real, source-referencing rationale and genuine
uncertainties (e.g. *"Whether The Ledger's audience has any interest in Python
packaging updates"*). This is authentic model output, not a fixture: the model
**recommended NO_ACTION**, but the deterministic policy layer independently ranked
and selected **RESEARCH_MORE**, demonstrating that reasoning proposes and
deterministic policy decides/owns all effect authority.

## Acceptance chain (no missing hop)

real URL `pypi.org/pypi/pip/json`
→ retrieval `2026-09-21T16:15:38+0000`, HTTP 200, SHA-256 `6e11a831…d814`, 228494 B
→ bounded signal `sig-de31a6915a99` captured to `signals_inbox.jsonl`
→ real `claude` CLI (`claude-code-subscription-v1`, ~33 s) via unmodified `ClaudeCodeReasoningProvider`
→ `reasoning.validate_proposal` passed (3 valid alternatives)
→ deterministic policy chose `RESEARCH_MORE` (overrode model's `NO_ACTION`)
→ decision persisted (`state/social-a/last_decision.json`, `memory/social-a/decisions.jsonl`)
→ zero public effect (`execute.effect = local only`, publish queue empty, no post/send/spend).

## SUMMARY booleans (all truthful)

| Boolean | Value |
|---|---|
| real_network_source | **true** |
| fixture_source | **false** |
| injected_model_runner | **false** |
| actual_claude_cli_invoked | **true** |
| api_key_path_used | **false** |
| proposal_schema_valid | **true** |
| deterministic_policy_applied | **true** |
| public_effect_performed | **false** |
| decision_persisted | **true** |

## Evidence files

`social-bots/receipts/evidence/SB-V04-live-canary/`
- `SOURCE.json` — URL, timestamp, status, length, SHA-256, bounded excerpt.
- `PROVIDER.json` — provider id/class, adaptive flags, CLI path/version, invocation flags, API-key boolean, call window, uncertainties.
- `DECISION.json` — full persisted decision record (observe/orient/reasoning/alternatives+scores/policy/execute/verify).
- `SUMMARY.json` — the nine required booleans + context.

## Limitations

- Single run, single persona (The Ledger), as the packet's minimum requires.
  Optional Tidepool/Switchboard multi-persona proof was not run (explicitly not required).
- Host egress restricts source hosts to the allowlist (see above); pypi.org used.
- Executed on a Linux CCR host, not the user's Mac. The subscription Claude Code
  call succeeded here with `ANTHROPIC_API_KEY` absent (no PAYG/API-key path), so
  the SB-V04-005 stop-condition (BLOCKED) did **not** trigger. If the lead requires
  the canary specifically on the Mac subscription host, the identical driver/chain
  can be re-run there.
- No secrets, no hidden chain-of-thought stored (adapter requests only the strict
  JSON envelope).

## Stop condition

Canary complete. **STOP** — no further live model calls from this lane. One real
canary executed as authorized. Independent acceptance (SB-EVD-002) is the ChatGPT
lead's to own.
