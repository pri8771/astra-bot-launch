# SB-V04-005 — Real V0.4 Live Canary Worker Report (Lane 2, canary branch)

- Artifact: **SB-V04-005** — Real adaptive V0.4 canary (ZERO-priority promotion gate)
- Branch: `claude/social-bots-v04-live-canary` (runtime head `8febedb`, `runtime/reasoning_cli.py` used unmodified)
- Persona: **social-a / The Ledger**
- Date: 2026-09-21
- Result: **real end-to-end canary executed with a live subscription Claude Code call; zero public effect.**
- Return status requested: **SUBMITTED** (lead audit / SB-EVD-002 owned by ChatGPT lead; V0.4 not self-marked complete).

## Preflight (host / provider)

| Field | Value |
|---|---|
| OS | `Linux-6.18.44-fc-v37-x86_64-with-glibc2.39` |
| Python | `3.11.15` |
| Claude CLI path | `/opt/node22/bin/claude` |
| Claude CLI version | `2.1.278 (Claude Code)` |
| `ANTHROPIC_API_KEY` present | **false** (boolean only; value never read/printed) |
| Isolated `SBOTS_HOME` | dedicated canary home (not the working tree) |
| Reasoning mode | `SBOTS_REASONING=claude-cli`, `SBOTS_REASONING_REQUIRE_ADAPTIVE=1` |

> Host reality (non-secret): execution host is a **Linux CCR container, not a
> macOS host**, and `gh` is not installed. The subscription Claude Code call
> nonetheless succeeded with `ANTHROPIC_API_KEY` absent (no PAYG/API-key path), so
> the BLOCKED stop-condition did not trigger. Heartbeats to Issue #3 were posted
> via the GitHub MCP tools rather than the `gh`-based reporter.

## Exact safe commands

```
export SBOTS_HOME=<isolated-canary-home>
export SBOTS_REASONING=claude-cli               # real adaptive route (ClaudeCodeReasoningProvider)
export SBOTS_REASONING_REQUIRE_ADAPTIVE=1       # fail closed if not adaptive
unset ANTHROPIC_API_KEY                          # subscription/OAuth route only, no PAYG

# driver: live fetch -> sha256/metadata -> research.Signal -> research.capture ->
#         decision.run_cycle(bot, persona, require_adaptive=True)
python3 canary_driver.py <runtime_dir> <evidence_dir> $SBOTS_HOME
```

Real model subprocess built by the unmodified adapter (`runtime/reasoning_cli.py`):

```
claude --print --output-format json --permission-mode plan \
       --permission-prompts none \
       --disallowedTools Bash Edit Write NotebookEdit WebFetch WebSearch Read Glob Grep Task Agent
```

Prompt delivered on **stdin**; child env has `ANTHROPIC_API_KEY` stripped; every
effect tool denied; permission prompts auto-denied. The `runner` seam was **not**
injected — the default real-CLI runner was used.

## Source metadata (live capture)

| Field | Value |
|---|---|
| URL | `https://pypi.org/pypi/beancount/json` |
| Provenance | `live-canary-capture` (public, no auth/cookies, fetched live, not fixture, not pasted) |
| Retrieved at | `2026-09-21T16:53:48+0000` |
| HTTP status | `200` |
| Content length | `275859` bytes |
| SHA-256 | `c65ae5bed8d29aaebfd9a5d4150c35c2772f257cefef90da798bdf0426dd3ce0` |
| Bounded title | `beancount 3.2.3` |
| Bounded summary | `PyPI package 'beancount' current latest version 3.2.3; N released versions indexed; …` |
| Signal id | `sig-af95737e8eb8` |

**Source-selection limitation (documented):** this environment's egress policy
returns `403` for arbitrary public hosts; `pypi.org` is on the allowed
direct-connect list and is a genuinely public, current, no-auth, live-fetched
source (`beancount` is a plain-text-accounting package, thematically apt for The
Ledger). All SB-V04-005 source rules are satisfied. On a host with open egress
(e.g. the user's Mac) a finance/news source could be used with the identical chain.

## Provider identity / outcome

| Field | Value |
|---|---|
| provider_id | `claude-code-subscription-v1` |
| provider class | `ClaudeCodeReasoningProvider` (adaptive=true) |
| available | true |
| call window | `16:53:48+0000` → `16:54:46+0000` (~58 s real model latency) |
| model alternatives | RESEARCH_MORE (1.45), NO_ACTION (0.865), CREATE_CANDIDATE (0.86) |
| model recommended | `RESEARCH_MORE` |
| deterministic policy selected | `RESEARCH_MORE` (`recommended_followed=true`) |
| execute.effect | `flagged research need (local only)` |
| publish queue length | `0` |
| publish_authorized | `false` |

Authentic model output (not a fixture): three schema-valid alternatives with
scores and genuine uncertainties (e.g. *"Unknown audience interest in beancount
specifically"*). Deterministic policy independently ranked and owns all effect
authority (`public_post`, `spend`, `messaging`, `review_gate`, …); here it agreed
with the model's advisory recommendation. No public/social effect performed.

## Acceptance chain (no missing hop)

real URL `pypi.org/pypi/beancount/json`
→ retrieval `2026-09-21T16:53:48+0000`, HTTP 200, SHA-256 `c65ae5be…`, 275859 B
→ bounded signal `sig-af95737e8eb8` captured to `signals_inbox.jsonl`
→ normal adaptive path (`SBOTS_REASONING=claude-cli`, adaptive-required)
→ real `claude` CLI (`claude-code-subscription-v1`, ~58 s) via unmodified `ClaudeCodeReasoningProvider`
→ `reasoning.validate_proposal` passed (3 valid alternatives)
→ deterministic policy chose `RESEARCH_MORE`
→ decision persisted (`state/social-a/last_decision.json`, `memory/social-a/decisions.jsonl`)
→ zero public effect (`execute.effect = local only`, publish queue empty).

## SUMMARY booleans (all truthful)

| Boolean | Value |
|---|---|
| real_network_source | **true** |
| fixture_source | **false** |
| injected_model_runner | **false** |
| actual_claude_cli_invoked | **true** |
| api_key_path_used | **false** |
| proposal_schema_valid | **true** |
| deterministic_policy_applied | **true** |
| public_effect_performed | **false** |
| decision_persisted | **true** |

## Evidence paths

`social-bots/receipts/evidence/SB-V04-live-canary/`
- `SOURCE.json`, `PROVIDER.json`, `DECISION.json`, `SUMMARY.json`

## Limitations

- Single run, single persona (The Ledger) — the packet's minimum. Optional
  Tidepool/Switchboard multi-persona proof not run (explicitly not required).
- Host egress restricts source hosts to the allowlist; pypi.org used.
- Linux CCR host, not a Mac; subscription call succeeded with no API-key path.
- No secrets, no hidden chain-of-thought stored (adapter requests only the strict
  JSON envelope).

## Stop condition

Canary complete and pushed. **STOP** — no further live model calls from this lane.
Independent acceptance (SB-EVD-002) is the ChatGPT lead's to own.
