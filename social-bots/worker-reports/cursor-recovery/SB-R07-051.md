# Worker report — SB-R07-051 operational fact-review integration

- **Artifact:** `SB-R07-051` — V0.5 operational fact-review assessor integration
- **Requested status:** `SUBMITTED` (not self-accepted)
- **Lane:** `cursor-recovery`
- **Branch:** `cursor/social-bots-recovery-v07-20260921`
- **Packet:** `social-bots/artifact-packets/recovery-v07/SB-R07-051.md`
- **Canonical coordination SHA read:** `13e1846c7ec337165722cbe090866dab90b3dc00`
- **Lead review read:** `LEAD-041` (+ SB-V05-002 LEAD-017/018/020 trust direction)
- **Implementation source SHA:** `6eb2e7c66c7daeb45dfae1f51462fa09fbd2b58f`
- **Evidence class:** `ENGINEERING` (no live model call; no public effect)

## What changed

- Brought hardened `runtime/factcheck.py` (claim-to-source review) onto the recovery branch.
- Closed operational route: policy-owned `BoundedPropositionAssessor` (exact-class allowlist; **no** public `register_operational_assessor`).
- Keyword/manual assessors remain diagnostic/test-only and cannot grant operational stance.
- Operational stance requires trusted-operational SB-V05-001 evidence; fixtures fail closed.
- `pipeline.fact_check` routes through `factcheck.review_candidate`.
- Heuristic claim extractor tightened (word-boundary phrases; meaningful numbers/years) so `because`/`signal 1` do not false-trigger.

## Adversarial coverage

Negation, unrelated relation, numeric/date mismatch, missing predicate, conflicting evidence, omitted material claim — all covered in `tests/test_factcheck.py`.

## Tests

| Command | Result |
|---|---|
| `python3 -m unittest tests.test_factcheck -v` | 24 passed |
| `python3 -m unittest discover -s tests -v` | 386 passed, 2 skipped |

## Limitations

- BoundedPropositionAssessor is deterministic entailment-lite, not a language model. Ambiguous prose returns PARTIAL/UNRELATED/REFUTES rather than overclaiming SUPPORTS.
- Adaptive-provider semantic assessor remains a future module-policy addition; callers still cannot self-register.
- No LIVE capture batch in this artifact.

## Next dependency-ready artifact

`SB-R07-052` — platform-native formatting gate. Skip `SB-R07-073` on this host.
