# Worker report — SB-R07-042 controlled V0.4 input freeze

- **Artifact:** `SB-R07-042` — V0.4 controlled divergence input freeze
- **Requested status:** `SUBMITTED` (not self-accepted)
- **Lane:** `cursor-recovery`
- **Branch:** `cursor/social-bots-recovery-v07-20260921`
- **Packet:** `social-bots/artifact-packets/recovery-v07/SB-R07-042.md`
- **Canonical coordination SHA read:** `13e1846c7ec337165722cbe090866dab90b3dc00`
- **Lead review read:** `LEAD-041`
- **Evidence class:** `ENGINEERING` (live network capture yes; **no model call**; not LIVE divergence acceptance)

## What was frozen

Immutable bundle at `evidence/SB-R07-042/freeze/`:

| File | Role |
|---|---|
| `E1.raw` / `E2.raw` | Exact HTTPS-retrieved bytes |
| `E1.receipt.json` / `E2.receipt.json` | SB-V05-001 collector receipts (`live-capture`, trusted transport) |
| `EVIDENCE_BUNDLE.json` | Prepare-compatible live-capture bundle |
| `PREPARED_MATRIX.json` + `prompts/` | Five hashed cases P0–P3 + E0 |
| `ISOLATION.json` | Single-variable assertions for required comparisons |
| `FREEZE_MANIFEST.json` | Top-level honesty + digests |

### Live sources captured

- **E1:** `https://example.com/` (559 bytes)
- **E2:** `https://www.rfc-editor.org/rfc/rfc8259.txt` (28360 bytes)

`acceptance_eligible: true` · `isolation_all_isolated: true` · `live_model_call_performed: false`

## Implementation

- Brought accepted SB-V05-001 `runtime/collector.py` (+ tests) onto recovery branch for trusted live capture.
- Added `runtime/divergence_freeze.py` + `bin/freeze_divergence_inputs.py`.
- Retains raw bytes via instance-bound recording fetch without losing UrllibFetcher trust identity.

## Tests

| Command | Result |
|---|---|
| `python3 -m unittest tests.test_divergence_freeze tests.test_collector -v` | 32 passed, 1 skipped |
| `SBOTS_LIVE_FREEZE_TEST=1 … LiveFreezeNetworkTest` | 1 passed (real HTTPS) |
| `python3 -m unittest discover -s tests -v` | 362 passed, 2 skipped |

## Limitations

- Title/summary on signals are operator annotations; provenance of truth is collector receipt + raw bytes.
- Freeze is input evidence for a future owner-authorized five-call batch — not empirical divergence acceptance.
- No adaptive/model provider was constructed.

## Next dependency-ready artifact

`SB-R07-051` — operational fact-review integration (no live model call). Skip `SB-R07-073` on this host.
