# Worker report — SB-V03-006 V0.3 adversarial regression / acceptance bundle

- **Artifact ID:** SB-V03-006
- **Lane:** Windows Core (`claude/social-bots-windows-core-host`), fast-track LEAD-021
- **Requested status:** PREPARED — **not yet acceptance-eligible** (see gate below).
  The reproducible bundle is regenerated from current repaired code; formal
  SB-V03-006 acceptance is gated on a registry precondition I do not control.

## Eligibility gate (honest — not claiming the precondition is met)

The SB-V03-006 packet requires "every required V0.3 artifact is ACCEPTED in the
canonical artifact registry." Current registry state:
- SB-V03-002, SB-V03-003 — ACCEPTED;
- SB-V03-004 — repair lead-**acknowledged** (LEAD-021), not yet marked ACCEPTED;
- SB-V03-005 — **SUBMITTED this session**, not yet ACCEPTED;
- SB-V03-001 — CHANGES_REQUIRED in the registry.

So the acceptance precondition is NOT satisfied yet. This bundle is therefore
submitted as PREPARED evidence generated by the current (repaired) code, ready to
be promoted to acceptance once the lead accepts V03-004/005 and reconciles the
V03-001 status. No readiness is overstated.

## Implementation ref & environment

- impl commit: `f73c337e66ccdd5bd09313e37f4c87b4f00df07e` (post LEAD-024 + LEAD-025 repairs)
- base ref: `origin/claude/social-bots-core-to-v2@874b6992`
- exact verbatim full-suite output committed at
  `receipts/evidence/SB-V03-006/FULL_SUITE_OUTPUT.txt` (`Ran 129 tests ... OK`).
- OS: `Linux 6.18.44-fc-v37`; Python `3.11.15`; `FLOCK_AVAILABLE=True`
  (single POSIX host, single local filesystem).
- Evidence dir: `social-bots/receipts/evidence/SB-V03-006/` (see `MANIFEST.json`
  for sha256 of each artifact).

## Focused acceptance results (real, current code — `FOCUSED_RESULTS.txt`)

| V0.3 concern | suite | result |
|---|---|---|
| signal-delta consumption | `tests/test_decision.py` | OK |
| required review-stop (fact/voice/cultural) | `tests/test_review_gate.py` | OK |
| stale takeover / active-cycle fencing (+ fenced migration + **post-cycle success-receipt fencing**, LEAD-024 P1) | `tests/test_fencing.py` | OK |
| shared-runtime persona isolation | `tests/test_isolation.py` | OK |
| persona decision-history no-bleed | `tests/test_isolation_decisions.py` | OK |
| persona-scoped production read boundary (interface) | `tests/test_persona_read_boundary.py` | OK |
| persona read boundary through **production paths** + **structural admin-only raw readers / bypass regression** (LEAD-024 P2, LEAD-025) | `tests/test_production_read_paths.py` | OK |
| no-overlap under concurrency | `tests/test_concurrency.py` | OK |
| **full suite** (exact verbatim output committed) | `unittest discover -s tests -v` | **Ran 129 tests … OK** |

## Regenerated worker evidence (by current code, not superseded logic)

- `dry_run_summary.json` — `bin/dry_run.py`: `all_pass=true`, `cultural_withheld=true`
  (also refreshed the checked-in `receipts/evidence/SB-007-dryruns/` bundle).
- `recurring_proof.json` — `bin/prove_recurring_host.py`: `all_pass=true` (two real
  separate-process invocations, no-overlap exit 3, stale takeover at gen ≥ 2).
- Sanitized start/finish/failure/heartbeat receipts are produced under the
  evidence `SBOTS_HOME` trees by the above runs.

## Confirmations

- No public effect, no spend, no secrets, no SwarmAI dependency.
- No GitHub CI claim is made here (CI is SB-CTL-006, owned by the Mac QA lane; a
  real run of it succeeded there — not part of this Core bundle).
- Portability: single POSIX host / local filesystem; native-Windows and WSL host
  proofs remain the separate SB-V07-WIN-001 concern (BLOCKED on a real host).

## Requested status

**PREPARED (SUBMITTED as evidence)** — promote to acceptance only after the lead
accepts SB-V03-004 and SB-V03-005 and reconciles SB-V03-001 in the registry.
