# Worker report — SB-V04-004 divergence acceptance + V04-001/002/003 re-audit (LEAD-027)

- **Lane:** Windows Core (`claude/social-bots-windows-core-host`), fast-track.
- **Requested status:** SUBMITTED (not self-accepted). Acceptance remains
  dependency-gated until V0.3 closes (V03-004 independent execution by Mac QA).
- **Scope guard:** engineering evidence only. NO live SB-V04-005 canary was run;
  no model/network call; no spend. The dedicated `claude/social-bots-v04-live-canary`
  lane exclusively owns the real subscription-authenticated canary.

## Context

LEAD-027 accepted SB-V03-005 (at `796d4e3`) and assigned dependency-safe V0.4
reconciliation while Mac QA independently executes the V03-004 lifecycle probe.

## Re-audit — SB-V04-001 / SB-V04-002 / SB-V04-003 (no defects found)

- **SB-V04-001** (adaptive-required production posture): `run_worker.py` defaults
  to `require_adaptive=True`; `resolve_provider(require_adaptive=...)` fails closed
  to `BLOCKED_REASONING_UNAVAILABLE` when no adaptive provider is available;
  baseline/contextual stay explicit debug/test modes; `CLOSE_EXPERIMENT` is in the
  schema vocabulary but has no executor. `tests/test_reasoning_contract.py`: OK.
- **SB-V04-002** (Claude Code CLI adaptive provider): bounded, effect-free,
  fail-closed on CLI-missing/timeout/non-zero/empty/invalid/schema; billing guard
  on `ANTHROPIC_API_KEY`; runner seam keeps every test injected/offline.
  `tests/test_reasoning_cli.py`: OK — and these injected-runner tests are clearly
  engineering-only, NOT the required real canary.
- **SB-V04-003** (deterministic policy boundary): provider `recommended_action`
  is advisory only; policy selects by deterministic ranking; authority/spend/
  messaging/review/dedup/platform/lease/scheduling/verify owned by deterministic
  code; publish never authorized. `tests/test_policy_boundary.py`: OK.

No source change was made to these — they hold against the current production path.

## SB-V04-004 — persona/evidence divergence acceptance suite (new)

`tests/test_v04_divergence_acceptance.py` (6 tests) drives real `decision.run_cycle`
with the deterministic `ContextualReasoningProvider` (`adaptive=False`) and proves
materially different proposals/decisions:

- **scope guard:** the suite asserts the provider is `contextual-deterministic-v1`
  / `adaptive=False` and documents that it is engineering evidence, not the canary;
- **same evidence, different personas diverge:** general → `candidate_created`;
  cultural (no named reviewer) → `withheld` on the same class of evidence;
- **persona divergence at the proposal level:** the cultural persona's
  CREATE_CANDIDATE estimate is materially different (>= risk, different
  expected_value) from the general persona's for the same signal/context;
- **same persona, different evidence diverges:** strong sourced/tagged
  live-capture → `CREATE_CANDIDATE`; thin untagged fixture → `RESEARCH_MORE`;
- **each action outcome reachable:** CREATE_CANDIDATE (strong), RESEARCH_MORE
  (thin), NO_ACTION (no-new-evidence deterministic path);
- **inspectable:** the provider records why it diverged (evidence_strength /
  confidence / novelty) in the decision record's `reasoning.uncertainties`.

## Test commands & results

```
cd social-bots && python3 -m unittest discover -s tests   # 136 passed
python3 -m unittest tests.test_v04_divergence_acceptance   # 6 passed
```

- Full suite **136 passed** (was 130; +6). V04 re-audit suites
  (`test_reasoning`, `test_reasoning_contextual`, `test_reasoning_contract`,
  `test_reasoning_cli`, `test_policy_boundary`): all OK.

## Confirmations

No public effect, no spend, no paid API, no secrets, no SwarmAI, no live canary.
Single POSIX host. V03-004/005/006 evidence preserved (unchanged).

## Requested status

**SUBMITTED** — SB-V04-004 divergence acceptance + V04-001/002/003 re-audit.
Acceptance dependency-gated until V0.3 closes (V03-004 independent execution).
