# SB-S23-008 — specialist lifecycle acceptance

- artifact_id: SB-S23-008
- lane: fable-fasttrack · session: `s-20260921T211438Z-d5589881` · branch: `fable/social-bots-v23-fasttrack-20260921`
- source_sha: `e33de096c7da2633376794b3297d82b320f6229d` (evidence bundle committed separately at `bbbb7b2df473a61c029a3098361cd0a084577d38`)
- evidence_class: **ENGINEERING** — bundle `classification: "fixture"`, `live: false`. Nothing here is LIVE evidence and it cannot promote any milestone.
- requested_status: **SUBMITTED** (independent Acceptance re-execution requested per packet)

## Files changed (new files only)

- `social-bots/runtime/specialist_lifecycle.py`
- `social-bots/bin/prove_v23_specialists.py`
- `social-bots/tests/test_v23_specialist_acceptance.py`
- `social-bots/receipts/evidence/SB-S23-008/run-20260921T215131Z/` (manifest, 4 lifecycle records, parent-state hashes, leases/scratch left-behind lists)

## What was built

- `run_specialist(contract, adapter, *, resolver, provider, inputs, adapter_kwargs, lease, fence, integrator_keep, deadline)` → `LifecycleRecord` with per-stage `{stage, at, ok, detail}`: CREATE (validate, depth guard, **worker id never reused** when kept evidence or a receipt exists, `leasing.acquire("specialist:<worker_id>")`, `Sandbox.create`), ASSIGN (context via resolver, parent inputs copied), RUN (adapter with `BudgetedProvider|None`, `Deadline`, shared `WorkerLedger`; any raise → synthetic `FAILED` result carrying truthful ledger counts), RETURN (`outputs/RESULT.json`), VERIFY (`integrator.verify`), ADOPT (fenced) / REJECT (failure receipt), RETIRE (scratch retired, lease released, `lease_gone` proven via `leasing.inspect`).
- `run_concurrent(jobs, max_workers)` on a thread pool hard-capped at `DEFAULT_MAX_WORKERS = 3` (callers can only narrow).
- Runner `bin/prove_v23_specialists.py`: seeds fixture parent stores in a fresh temporary `SBOTS_HOME`, runs researcher → concurrent analyst/writer/reviewer, hashes parent stores before/after, refuses to write a bundle if any lease or scratch remains, writes `manifest.json` with `source_sha`, command, `classification: "fixture"`, per-file sha256; `verify_bundle` re-hashes.

## Tests

Focused: `python3 -m unittest tests.test_v23_specialist_acceptance -v` → 11 tests OK (parent stores byte-identical in every test via `tearDown`).

| Packet case | Test |
|---|---|
| 1 three concurrent workers ADOPTED, receipts, empty scratch, no leases | `test_three_concurrent_workers_adopt` |
| 2 raising adapter → only it REJECTED with failure receipt | `test_raising_adapter_rejects_only_itself` |
| 3 output outside scratch → REJECTED at VERIFY | `test_escaping_result_is_rejected_at_verify` |
| 4 `EffectGuard.attempt("publish")` → `effect_attempts=1` → REJECTED | `test_effect_attempt_is_rejected` |
| 5 `spawn_specialist` → `ChildSpawnDenied` → REJECTED (+ depth-2 parent refused at CREATE) | `test_child_spawn_attempt_is_rejected` |
| 6 persona B reads A's context by path under concurrency → `SandboxEscape`; A intact | `test_persona_isolation_under_concurrency` |
| 7 deadline 0s → TIMED_OUT → REJECTED, cleanup complete | `test_timeout_is_rejected_with_cleanup` |
| 8 same worker id after RETIRE → CREATE fails (`WorkerIdReused`; held lease → `LeaseHeld`) | `test_worker_id_is_never_reused` |
| 9 lease gone after every record (+ lost parent fence during ADOPT ends REJECTED and clean) | `test_lease_is_gone_after_every_record` |
| 10 bundle manifest verifies; fixture-only; tamper detected | `test_bundle_manifest_verifies_and_is_fixture_only` |
| 11 5 contracts, `max_workers=3` → at most 3 active; 50 clamps to 3 | `test_max_workers_bounds_concurrency` |

Full suite: `python3 -m unittest discover -s tests` → **523 tests OK, 2 skipped** (512 + 11).

## Evidence

Bundle: `receipts/evidence/SB-S23-008/run-20260921T215131Z/` produced by
`python3 bin/prove_v23_specialists.py --bot social-a --personas social-a,cultural-primandir-atman --out receipts/evidence/SB-S23-008/run-20260921T215131Z --source-sha e33de096c7da2633376794b3297d82b320f6229d`
→ decisions: researcher/analyst/reviewer (social-a) and writer (cultural-primandir-atman) all `ADOPTED`, `cleanup RETIRED`, `lease_gone true`; `parent_state_unchanged: true`; `leases_after: []`; `scratch_after: []`; `verified: true`. Concurrency observed in tests: ≥2 and ≤3 simultaneously active adapters.

## Limitations

- In-process threads; no process/user isolation (V3 option). Single POSIX host/filesystem guarantees inherited from `leasing`.
- Deterministic adapters only; no provider was constructed in the bundle. A live specialist run requires a canonical manifest (`SB-V23-003` gate).
- Bundle `source_sha` is the source commit; the bundle itself is committed one commit later (`bbbb7b2`).

## Blockers

None for this slice. Independent Acceptance re-execution is required before lead acceptance (packet).

## Next artifact

The specialist track (SB-S23-001..008) is complete. No further S20–S23 slice is dependency-safe under the current index: S20-002 waits on SB-S20-000 (accepted SB-R07-041), S21-* on S20-004/006, S22-001 on S21-001, SB-V23-099 on S20-006/007, S21-006, S22-007. Lane pauses pending lead direction.
