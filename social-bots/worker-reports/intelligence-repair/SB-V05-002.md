# Worker report — SB-V05-002 (Intelligence repair wave 1)

Lane: Intelligence / Evidence Integrity
Branch: `claude/social-bots-intelligence-repair-v2`
Status: SUBMITTED — not self-accepted (for lead audit). Addresses CHANGES_REQUIRED / LEAD-014.

## Scope

Repair of the SB-V05-002 claim-to-source factual support review per
INTELLIGENCE_WAVE1 §2 and the LEAD-014 finding.

## Changes (`runtime/factcheck.py`)

1. **Attributable assessor, not caller-asserted stance.** Support stances are
   produced by a `SupportAssessor` (identity + version), not passed in by the
   caller. `SupportAssessment` binds claim → evidence excerpt/span/hash →
   assessor identity/version → stance. `operational` is derived from an
   operational-assessor registry (`register_operational_assessor` /
   `is_operational_assessor`), keyed on the assessor's exact class — a caller
   cannot make an assessment operational by setting an attribute.
2. **Operational vs fixture.** `KeywordSupportAssessor` is a deterministic,
   inspectable operational assessor (checks the claim's key terms against the
   cited excerpt; detects refutation cues; rationale names the drivers).
   `ManualAssessor` echoes a caller-declared stance but is `operational=False`
   and never grants support — the fixture/test-only path.
3. **No operational assessor => UNKNOWN/WITHHELD.** A required factual claim
   backed only by non-operational assessments resolves to `UNKNOWN` and is
   withheld.
4. **Material-claim identification (closes LEAD-014).** `HeuristicClaimExtractor`
   is a bounded, inspectable identifier: it splits candidate text into sentences
   and flags factual assertions (numbers/years or factual phrasing) as material
   claims while marking subjective/creative sentences. `reconcile_claims` /
   `review_candidate` merge caller claims with identified ones; a material
   factual claim the caller omitted is added as a required claim with no support
   → withheld. This proves omission cannot bypass factual review.
5. Preserved staleness semantics (changed content hash invalidates support) and
   the `SUPPORTED/PARTIAL/UNSUPPORTED/CONFLICTED/UNKNOWN` vocabulary. Exposed
   `ClaimSupportResult` for SB-V16-001 consumption.

No Core files modified.

## Acceptance mapping

- URL-present-but-unsupported fails — ✅ `test_url_present_but_unsupported_fails`.
- conflicting sources → CONFLICTED/withheld — ✅
  `test_conflicting_sources_yield_conflicted_and_withheld`.
- supported claim passes with exact evidence ref — ✅
  `test_supported_claim_passes_with_exact_evidence_ref`.
- caller cannot assert operational stance — ✅
  `test_caller_declared_stance_is_not_operational_support`,
  `test_manual_assessor_not_registered_operational`.
- changed source hash invalidates stale support — ✅
  `test_changed_source_hash_invalidates_support`.
- opinions/creative distinguished — ✅ `test_opinion_and_creative_not_gated`,
  extractor classification.
- material claim not pre-enumerated is identified and gated / omission cannot
  bypass — ✅ `test_material_claim_identification_finds_factual_sentences`,
  `test_omitted_material_claim_cannot_bypass_review`.

## Evidence

- Tests: `python3 -m unittest discover -s tests` → **145 passing** (+ new
  factcheck cases).
- Fixture evidence: `receipts/evidence/SB-V05-002-factcheck/SUMMARY.json`
  (`all_pass: true`), FIXTURE-labelled + deterministic assessor, not operational
  content review. Generator `bin/gen_sb_v05_002_evidence.py`.

## Known limits

- `KeywordSupportAssessor` is deterministic keyword/cue matching, not semantic
  entailment; it is intentionally simple and auditable. A model-backed
  operational assessor can register through the same seam later (fail-closed:
  absent an operational assessor, required factual claims are withheld).
- `HeuristicClaimExtractor` is marker-based; it favours recall on factual
  markers (fail-closed toward gating) and can over-identify. A richer extractor
  can replace it behind the `ClaimExtractor` protocol.

## Constraints honoured

No public posting, no spend, no new API keys, no secrets, no fabricated metrics,
no fixture presented as operational evidence, no SwarmAI dependency. No Core
files modified.

---

## LEAD-018 hardening addendum (resubmission)

Applied the LEAD-018 "Next 2" requirements:

- **No self-registered operational assessor.** Removed the public
  `register_operational_assessor` API. `_OPERATIONAL_ASSESSOR_POLICY` is a static
  internal frozenset that ships EMPTY — no operational semantic assessor is
  bundled, so the system fails closed (required factual claims withheld).
- **Built-ins are diagnostic.** `KeywordSupportAssessor` is now
  `operational=False` (diagnostic/test-only, not authoritative fact
  verification); `HeuristicClaimExtractor` remains conservative/diagnostic (not
  proof all facts found).
- **Operational stance requires trusted-operational SB-V05-001 evidence.**
  `EvidenceRef` carries an `evidence_class`; `assess_bindings` marks a binding
  operational only if the assessor is policy-operational AND the evidence is
  trusted-operational. Fixtures are explicit test-only.
- **No second model gateway.** The operational semantic assessor/extractor is a
  recorded dependency on the accepted Core adaptive provider; this module only
  defines the fail-closed interface.

Core interface request (recorded): Core's adaptive provider should supply the
operational semantic assessor (and optionally a semantic claim extractor) via the
policy seam; until then factual review is fail-closed.
