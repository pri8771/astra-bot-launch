# Artifact packet — SB-V03-004

Artifact: Race-safe lease fencing and stale takeover
Milestone: V0.3
Worker: Claude
Story points: 5
Current status: PLANNED until SB-V03-002 and SB-V03-003 checkpoint is submitted/reviewed.

## Goal

Exactly one worker may own a task/runtime fence during normal acquisition or stale takeover, including concurrent contenders.

## Required properties

- stale read + takeover must be conditionally atomic/fenced;
- two contenders cannot both believe they acquired ownership;
- losing contender receives an explicit non-owner result;
- previous stale owner's uncertain external effects are reconciled before new eligible effects;
- fencing remains correct across restart;
- receipts identify lease/fence generation or equivalent ownership token.

## Recommended decomposition

Claude still owns the implementation. Suggested subpackets:
A. SP2 — write/approve lease-state transition contract and adversarial test harness.
B. SP3 — implement atomic takeover/fencing primitive.
C. SP2 — wire receipt/fence-token semantics.
D. Final SP5 artifact acceptance — concurrent adversarial test and restart/reconciliation proof.

Do not weaken final SP5 acceptance just because implementation is split.

## Acceptance

- concurrent takeover test with multiple contenders yields exactly one owner;
- old owner cannot renew/commit after fence loss;
- ownership state is inspectable after race;
- no duplicate external-effect eligibility;
- all prior lease tests pass.

Expected return status after final integration: SUBMITTED.
