# Artifact packet — SB-V03-004

Artifact: Race-safe lease fencing and stale takeover
Milestone: V0.3
Worker: Claude
Story points: 5
Current status: CHANGES_REQUIRED pending independent execution after LEAD-025 source review of Core `7e4345b041b59b9d1b1036dfea388cedf79b4d3d`.

## Goal

Exactly one worker may own a task/runtime fence during normal acquisition, stale takeover, the entire active work unit, and durable completion evidence that implies successful ownership.

## Required properties

- stale read + takeover must be conditionally atomic/fenced;
- two contenders cannot both believe they acquired ownership;
- losing contender receives an explicit non-owner result;
- previous stale owner's uncertain external effects are reconciled before new eligible effects;
- fencing remains correct across restart;
- receipts identify lease/fence generation or equivalent ownership token;
- lease validity covers the entire work unit and success completion evidence, not only acquisition;
- an old owner that loses/expires its fence cannot commit state, content, experiments, receipts that imply success, or external-effect eligibility afterward.

## Verified repair progress

The current Core branch materially repairs all source-level defects previously identified by LEAD-019/024:
- POSIX `flock` serializes fresh acquisition/stale takeover on one local host;
- generation fencing prevents an obsolete owner from committing cycle state after takeover;
- `PersonaState.load()` stages legacy migration in memory without durable writes;
- migration/persona/runtime/decision/content/experiment/action writes in the decision-cycle flow through the ownership-fenced commit;
- Core `7e4345b...` now writes the post-cycle finish/success receipt through `fence.fenced_commit` as well;
- its FenceLost handler emits `fence_lost_post_commit` failure evidence with `candidate_succeeded=false` if ownership is lost after the cycle commit but before completion evidence;
- committed regression `test_stale_owner_cannot_write_success_finish_receipt_after_takeover` forces exactly that takeover window and asserts no finish receipt is written.

LEAD-025 source inspection finds this repair direction correct. Do not churn the implementation unless independent execution finds a concrete defect.

## Remaining acceptance work

Final lead acceptance still requires an independent execution/probe of the repaired branch because this is a high-risk fencing/lifecycle artifact. Mac QA is assigned to execute the exact post-cycle takeover regression against `7e4345b...`. `worker-pc` attempted an independent audit but failed before source/test execution because repository clone failed; it contributes no evidence.

## Acceptance

- concurrent fresh acquisition with multiple contenders yields exactly one owner;
- concurrent stale takeover with multiple contenders yields exactly one owner;
- force the active worker's lease to expire while it is executing, then allow takeover: the old owner cannot commit cycle state/effects afterward;
- force takeover after the cycle commit but before completion receipt: the old owner cannot emit a success/finish receipt afterward;
- ownership/fence generation is inspectable after the race;
- no duplicate state/effect eligibility or misleading duplicate success evidence;
- reconciliation happens before the new owner performs eligible work;
- migration/load path remains side-effect free until fenced commit;
- all prior lease/fencing tests pass;
- host/filesystem scope is explicitly documented; native-Windows/cross-host claims require separate proof rather than assumption.

## Evidence required on final return

- exact implementation SHA;
- independent execution command/result for the post-cycle takeover regression;
- focused/full fencing suite result;
- known host/filesystem limits;
- no-public-effect/no-spend confirmation.

Expected worker status: SUBMITTED. Only lead may ACCEPT.
