# Artifact packet — SB-V04-002

Artifact: Adaptive alternative generation and prioritization
Milestone: V0.4
Lane: Core
Story points: 5
Current status: CHANGES_REQUIRED after LEAD-014 review.

Goal: replace fixed heuristic changed-evidence decisions with genuinely adaptive, context-sensitive proposals.

Required:
- persona, objective, evidence, current strategy/state, active experiments and uncertainty affect alternatives;
- proposals may include NO_ACTION, RESEARCH_MORE, CREATE_CANDIDATE, CONTINUE/CLOSE_EXPERIMENT and other policy-supported bounded actions;
- alternatives/reasons differ materially across evidence/personas;
- malformed/model-invented actions are rejected or normalized by deterministic schema/policy;
- no adaptive model/provider route => fail closed, not baseline/contextual deterministic reasoning pretending to be adaptive;
- provider satisfying this artifact must truthfully report `adaptive=true` and be runtime-verified under the no-additional-spend contract;
- no SwarmAI dependency.

Acceptance:
- three persona-divergence cases using the adaptive provider;
- same persona with materially different evidence changes proposal/ranking;
- no-provider case blocks and leaves evidence pending;
- unsupported action cannot produce an effect;
- provider output is validated before policy scoring/execution;
- provider identity/runtime receipt proves the adaptive route was actually invoked without a paid API fallback.

## LEAD-014 finding

Core's `ContextualReasoningProvider` is useful and materially context-sensitive, but it is explicitly `adaptive=false`. Keep it as deterministic diagnostic/fallback infrastructure; it does not satisfy this artifact or V0.4 real adaptive autonomy.

Return exact SHA, tests, decision records, provider identity, limits, requested SUBMITTED.
