# Artifact packet — SB-V17-001

Artifact: Community observation, decision and memory
Strategic checkpoint: V1.7
Lane: Intelligence
Story points: 4
Current status: CHANGES_REQUIRED after LEAD-014 review.

Goal: safely observe comments/replies/community signals, decide whether response is useful, and retain community memory.

Required:
- ingest/read model separated from response action;
- spam/abuse/unsafe/content-policy gates;
- conversation/thread/content/persona/account linkage;
- response recommendation with confidence and authority requirement;
- no automatic public response without explicit authorized route;
- community themes can update evidence-backed audience memory;
- private community memory/theme aggregation is scoped by bot + persona/workspace on shared runtimes;
- any intentionally shared community insight uses an explicit shared-evidence/generalization path.

Acceptance:
- read-only observation works without response authority;
- unsafe/low-value reply is NO_ACTION;
- authorized-response proposal still passes deterministic review before effect;
- no fake engagement/voting/follow manipulation;
- one persona's community signals cannot silently enter another persona's private themes/audience evidence;
- theme-to-audience evidence carries persona/workspace scope.

## LEAD-014 finding

Submitted `runtime/community.py` correctly walls observation/proposals from public effects, but `community_signals.jsonl` and `community_themes(bot)` are bot-wide and `theme_to_audience_evidence()` omits persona scope. On shared runtimes this can blend cultural/general community learning. Scope private memory/themes before resubmission.
