# Execution slice — SB-S20-000 (PROPOSED; not yet in ARTIFACT_INDEX.json)

Artifact: Intelligence module consolidation onto the primary implementation branch  
Version slice: V2.0-prerequisite  
Owner: Cursor (primary branch owner)  
Story points: 2  
Evidence class: ENGINEERING  
Spec: `../../V23_CRITICAL_PATH.md` §2

## Goal

Make the Intelligence-lane modules that V2.0 consumes present and green on the primary implementation branch, without accepting or reopening any Intelligence artifact status.

## Dependencies

Canonical: none (proposed `depends_on: []`).  
Timing: after the `SB-R07-041` repair push, so it never races Cursor's in-flight `reasoning.py` edits.

## Owned paths

Copied from `claude/social-bots-intelligence-repair-v2` @ `33b9c7b36b4d8a0c71fad45012f7f37bb2d4f894` (lead-verified worker head `205295531e7755a5045fb1e458d3964d986edd56` is an ancestor; verify): `runtime/metrics.py`, `runtime/audience.py`, `runtime/experiment_engine.py`, `runtime/growth_evaluator.py`, `runtime/community.py`, `runtime/platform_selection.py`, `tests/test_metrics.py`, `tests/test_audience.py`, `tests/test_experiment_engine.py`, `tests/test_growth_evaluator.py`, `tests/test_community.py`, `tests/test_platform_selection.py`.

Conflicting files (dry `git merge-tree` result): keep the Cursor-side `runtime/factcheck.py` and `tests/test_factcheck.py` (SB-R07-051 integration) and the canonical `bin/heartbeat_reporter.py`. Do **not** take Intelligence's versions of `reasoning.py`, `state.py`, `worker.py` (they are older).

## Interfaces

No new interfaces. After the slice, `from runtime import growth_evaluator, metrics, audience, experiment_engine` imports on the primary branch and their own tests pass unchanged.

## Deterministic vs model responsibility

Not applicable; no model code is introduced (the copied modules are deterministic and network-free in tests).

## Fail-closed behavior

- If any copied module imports a symbol that no longer exists on the Cursor branch (e.g. from `reasoning`/`state`), stop and report; do not patch Cursor-owned modules to satisfy it. Record the exact ImportError.
- If the merged suite is not fully green, the slice is not submittable; report the first failure.

## Tests

1. Existing six Intelligence test files pass on the primary branch.
2. Full suite passes with the previously reported count plus the copied tests (report exact numbers; expected ≈ 416 + Intelligence's ~100+, but report only what runs).
3. `python3 -c "from runtime import growth_evaluator, metrics, audience, experiment_engine, community, platform_selection"` succeeds.

## Evidence to return (ENGINEERING)

Source SHA, the exact `git` operations used (cherry-pick / checkout of paths), conflict resolution notes, focused + full test output.

## Owner / lead gates

Lead: create this ID in `ARTIFACT_INDEX.json` (or rename), confirm it is dependency-safe packaging under LEAD-042's pause rule. It does not change SB-V15-001 / SB-V20-002 status.

## Non-goals

No repair of SB-V15-001 / SB-V20-002 findings, no pipeline/analytics edits, no new modules.

## Definition of done

Modules and tests present on the primary branch; full suite green; report filed; requested status `SUBMITTED`; only ChatGPT lead marks ACCEPTED.
