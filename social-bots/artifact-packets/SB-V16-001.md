# Artifact packet — SB-V16-001

Artifact: Content intelligence
Milestone: V1.6
Lane: Intelligence
Story points: 4
Current status: CHANGES_REQUIRED after LEAD-014 review.

Goal: generate/repair platform-native content while preserving facts, persona voice, novelty and experiment intent.

Required:
- platform-native constraints;
- hook/format/asset plan;
- fact bindings survive rewrite;
- novelty/dedup;
- series/repurpose lineage;
- repair loop rather than silent truncation;
- private content history and novelty comparisons are scoped by bot + persona/workspace on shared runtimes unless an explicit shared-brand dedup layer is invoked.

Acceptance:
- over-limit content is repaired or withheld;
- factual meaning not silently changed by compression;
- same concept can produce distinct platform-native variants;
- duplicate/near-duplicate detection works;
- a cultural persona's similar content cannot make a general persona on the same runtime appear duplicate/near-duplicate, and vice versa;
- persisted content-intelligence history carries persona/workspace scope.

## LEAD-014 finding

Submitted `runtime/content_intelligence.py` records history by bot and platform without a persona field, and `check_novelty()` scans all same-platform records on the runtime. This allows cross-persona duplicate contamination on shared runtimes. Scope private history/novelty by persona before resubmission.
