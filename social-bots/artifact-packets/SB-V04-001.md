# Artifact packet — SB-V04-001

Artifact: Reasoning-provider interface + fail-closed contract
Milestone: V0.4
Worker: Claude
Story points: 3
Current status: PLANNED; unlock after V0.3 acceptance bundle.

## Goal

Define a Social-Bots-owned reasoning interface for changed-evidence cycles without any SwarmAI dependency.

## Contract requirements

Input must support:
- persona/objective;
- changed evidence summaries/refs;
- relevant state/hypotheses;
- active experiments;
- authority context;
- bounded allowed action vocabulary or proposal schema.

Output must support:
- context-specific candidate actions;
- concise rationale;
- uncertainty/confidence;
- expected value/learning;
- requested evidence or BLOCKED/NO_ACTION;
- schema validation.

Deterministic policy code remains authoritative for safety, spend, posting authority, dedup, leases, scheduling and effect verification.

If no allowed no-additional-spend reasoning provider is available, the interface must fail closed to NO_ACTION/BLOCKED_REASONING_UNAVAILABLE. Never fall back to fake fixed "AI" output and claim V0.4 autonomy.

## Acceptance

- provider unavailable test fails closed;
- malformed output rejected;
- authority-breaching proposal rejected downstream;
- interface contains no SwarmAI import/service/queue/gateway dependency;
- provider identity/attempt/result can be recorded without secrets.

Expected return status: SUBMITTED.


## Lead research — candidate no-additional-spend provider route

Read `social-bots/CLAUDE_REASONING_ROUTE_RESEARCH.md`.

A candidate adapter may use an already-authenticated Claude Code subscription via non-interactive print mode.

Guardrails:
- fail closed if `ANTHROPIC_API_KEY` exists;
- do not create Console PAYG credentials;
- bounded non-interactive invocation only;
- structured output parsed and validated;
- auth/quota/parse/model failures -> `BLOCKED_REASONING_UNAVAILABLE`;
- no tools/external effects in the reasoning call;
- record provider/attempt/result metadata, never credentials.

Current lead pre-review of worker head `2a53046f`:
- provider seam is directionally useful;
- default `SBOTS_REASONING=baseline` is NOT acceptable for V0.4 changed-evidence autonomy because it continues fixed heuristics when adaptive reasoning is unavailable;
- model proposal action/numeric/payload fields require strict deterministic validation before policy consumes them.
