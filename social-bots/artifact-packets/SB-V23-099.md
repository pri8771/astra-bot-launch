# Artifact packet — SB-V23-099 (PROPOSED; not yet in ARTIFACT_INDEX.json)

Artifact: V2.3 engineering-readiness integration acceptance  
Milestone: V2.3-readiness  
Owner: ChatGPT  
Story points: n/a  
Planned status: PLANNED (proposed)

## Purpose

Accept, on engineering evidence only, that the strategy, planner and specialist tracks compose truthfully through V2.3 — the V2.3 twin of `SB-V20-099`. This is engineering readiness, **not** operational V2.3 (`SB-V23-003`), which stays behind the LIVE chain.

## Dependencies

- SB-S20-006 (rollback/contradiction proof) and the SB-S20-007 **rehearsal** bundle (fixture classification).
- SB-S21-006, SB-S22-007, SB-S23-008 acceptance bundles.
- SB-S20-000 present on the primary branch.

## Required integration path (fixtures, labeled)

1. Fixture `NormalizedObservation` / `ExperimentRecord` / `AudienceHypothesis` → `growth_evaluator.evaluate` → `growth_intake` → bounded `StrategyRevisionProposal` → `strategy_policy` ACCEPT → new `StrategyState` version (S20).
2. Scripted contrary/stale evidence → lifecycle COOLING/rollback/EXPIRED with every prior version intact (S21).
3. Active strategy + opportunities → `Goal` → `PlanDAG` within `PlannerLimits`; seeded blocker → replan preserving DONE tasks (S22).
4. One `SPECIALIST` task from the plan → `WorkerContract` → concurrent specialists → verify/adopt/reject → retire, parent state hash unchanged (S23).
5. Adopted `DraftResult` is returned to the parent as `DRAFT_UNPUBLISHED`; nothing is queued or published.

## Acceptance

- Independent Acceptance re-executes all four `bin/prove_*.py` bundles plus this integration script on the exact SHA; hashes match.
- Every record in the chain carries `bot`/`persona`/`schema_version`/`provenance="fixture"`.
- No live model call, no network, no public effect, no spend anywhere in the run (grep + guard tests).
- Authority never widens along the chain: policy verdicts, planner constraints and specialist authority are each shown rejecting an escalation attempt.
- Failure/uncertain paths cannot produce a stronger effect than success paths.

## Non-negotiable distinction

Fixtures prove composition. They never promote V2.0–V2.3 operationally. Operational promotion requires `SB-V20-004` → `SB-V23-003` with real measured evidence and owner-gated live runs.

## Return contract

Return: artifact ID SB-V23-099; exact SHA; commands/results; bundle paths; fixture classification; limitations; requested status (normally SUBMITTED by the integrating worker; ACCEPTED only by ChatGPT lead).
