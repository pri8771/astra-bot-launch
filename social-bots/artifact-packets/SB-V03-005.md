# Artifact packet — SB-V03-005

Artifact: Shared-runtime persona concurrency and workspace isolation
Milestone: V0.3
Worker: Claude
Story points: 4
Current status: CHANGES_REQUIRED after LEAD-014 review of Core lane through `874b6992fb4fff3e4832dcb8ae078828525f6a51`.

## Goal

Allow the three runtimes to host their assigned general/cultural personas without concurrent shared-state corruption or accidental persona workspace mixing.

## Current direction

`SB-V03-004` active-cycle fencing is now lead-accepted for one POSIX host/local filesystem.

Claude chose logical isolation for non-shared persona data and added `runtime/isolation.py`. This is directionally valid, but the artifact is not accepted yet because safe persona-filter helpers are optional rather than enforced: raw bot-wide readers remain available and production callers can bypass the isolation facade.

## Required properties

- exactly one active mutator for shared runtime state;
- a persona cycle that does not own the runtime fence cannot commit shared state;
- cultural/general concurrent invocations cannot lose counters, evidence-consumption state, hypotheses, or recovery state;
- persona-private/non-shared data has an explicit isolation contract that matches code;
- if physical persona namespaces are claimed, implement and test them;
- if logical isolation inside shared append-only stores is intentionally chosen instead, document the contract and prove every production read/filter path prevents cross-persona contamination;
- no public/external effect.

## Required repair after LEAD-014

1. Define one authoritative persona-scoped repository/read interface for non-shared stores.
2. Route production persona-specific reads through it, or make raw bot-wide readers explicitly internal/admin-only and prove normal production code cannot use them accidentally.
3. Add regressions exercising real production read paths, not only `isolation.audit()`/filter helpers.
4. Preserve intentionally shared runtime state separately from persona-private content/experiment/analytics/community/audience state.

## Acceptance tests

1. General + cultural persona race on one runtime: only the fenced owner mutates shared runtime state.
2. Force lease loss/takeover during a running persona cycle: the old owner cannot commit afterward.
3. Prove no duplicate signal consumption and no lost cycle/hypothesis update.
4. Prove non-shared persona history/experiments/content cannot be mistaken for another persona's data under the chosen isolation model.
5. Attempt to use each production persona-specific read path on a mixed shared-runtime store and prove it cannot return the other persona's private records.
6. Architecture documentation and implementation agree.

## Evidence required on return

- exact implementation SHA;
- exact isolation design choice;
- files changed;
- focused concurrency/isolation tests and results;
- production-read-path contamination regressions;
- full relevant suite result;
- known host/filesystem limitations;
- evidence that no public action occurred.

Expected return status: SUBMITTED.
