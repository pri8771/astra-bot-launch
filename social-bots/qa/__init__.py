"""Mac QA / Integration Control harness package (non-runtime).

Modules here are QA/acceptance tooling only. They do NOT import or implement the
runtime; they define the record contracts + assertion helpers the eventual merged
Core + Intelligence runtime will be validated against (SB-V20-099 engineering
acceptance prep). Nothing here performs any external effect.
"""
