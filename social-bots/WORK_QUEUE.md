# Work queue — LEAD-045

Official phase remains **V0.4.x / V0.4 in progress**. V0.3 is accepted/closed. No product-version promotion occurred in this review.

## 1. Cursor Recovery — STALE / ACTION REQUIRED
Cursor remains the primary recovery implementation owner. Do not expand scope until the immediate safety boundary is repaired.

1. **SB-R07-041 — CHANGES_REQUIRED:** repair the direct `ModelReasoningProvider` registered-callable path so every live-capable route fails closed without a valid canonical authorization manifest. Add a direct-library sentinel regression. **Zero live model calls.**
2. **SB-R07-071 — SUBMITTED:** preserve source `0c74336b...`; independent Acceptance multiprocess execution is still required.
3. Preserve **SB-R07-044 / SB-R07-072** and later recovery engineering submissions for review; do not self-accept them.
4. Do not install a scheduler on the current unsuitable Cursor cloud host.

## 2. Mac Acceptance — STALE / ACTION REQUIRED
Fresh review session required. Emit one real `SESSION_ONCE` heartbeat and perform, in order:

1. Independently execute the real **SB-R07-071** multiprocess duplicate-session race and verify exactly one durable winner.
2. After Cursor submits the repair, independently audit **SB-R07-041** through the direct-library model path with a harmless sentinel callable and prove the sentinel is not invoked without authorization. No real model call.
3. Then independently rerun/audit Fable **SB-S23-008** fixture lifecycle evidence at its pinned source/evidence refs.

Acceptance owns no runtime source.

## 3. Fable fast-track — MATERIAL SUBMISSIONS / PAUSED FOR REVIEW
The released non-overlapping new-files-only batch is complete. Fable has submitted engineering evidence for:

- **SB-S20-001**
- **SB-S23-001** through **SB-S23-008**

Worker baseline: **523 discovered / 521 passed / 0 failed / 0 errors / 2 skipped**. Lead source inspection confirms material implementation exists, but these artifacts are not operationally accepted from worker-local tests or fixture evidence.

**Current instruction:** preserve evidence and stop new source work until an explicit new lead release. Do not edit Cursor-owned existing source. `SB-S23-008` specifically awaits independent Acceptance rerun.

## 4. NR-01 integration baseline
Lead-owned `next-round/BASELINE.json` pins the current canonical/Cursor/Acceptance/Fable refs and acceptance predicates.

The next-round package is closure/integration work around existing artifacts, not a bypass around dependencies. `SB-S20-000` remains blocked behind accepted `SB-R07-041`; integrated V2.0–V2.3 readiness cannot skip the operational chain.

## 5. V0.4 empirical gate — BLOCKED OWNER AUTHORIZATION
`SB-V04-002` and `SB-V04-004` remain blocked on fresh explicit owner authorization for a controlled five-call adaptive causal-divergence batch. `SB-EVD-002` remains withheld.

No additional Claude/adaptive/model call is authorized.

## 6. Parked/frozen lanes
- Legacy Core: evidence-only; no overlapping implementation.
- Intelligence: evidence-only; `SB-V15-001` remains changes-required.
- V0.4 canary: frozen evidence preservation only.
- `worker-pc`: excluded from the critical path until private-repository clone/auth is demonstrably repaired.

## Heartbeat truth
Canonical policy is **ONE SESSION = ONE HEARTBEAT**. The old FAST_5M/SOAK experiment is superseded. Issue #3 is visibility only and never substitutes for source/test/evidence review.

V0.7 recurring liveness still requires repeated native OS-scheduled bounded worker sessions on a verified persistent owner-controlled host, each with a session heartbeat plus invocation receipt.

## Authority
No public effect, account creation, paid API/PAYG/new spend, destructive external action, credentials/secrets, engagement manipulation, or SwarmAI dependency is authorized by LEAD-045.
