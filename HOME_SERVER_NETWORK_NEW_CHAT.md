# New session prompt — Home servers, network, Unraid and Docker

Take ownership of the **Home Infrastructure planning discussion**, while a separate session continues the autonomous bots. Help me choose a practical end-to-end design and produce an actionable, staged plan. This assignment is **planning and read-only discovery**; do not install, restart, migrate, re-IP, adopt devices, change credentials, move data or enable scheduled jobs in this session. We will authorize implementation from a concrete plan afterward.

## Working arrangement and continuity

The Astra repository on my Mac is `/Users/pchordia/Documents/ChatGPT/Astra`. Read applicable repository instructions. Write your own documents only under `planning/home-infrastructure/<your-session-date-and-name>/`. Create `CONTINUE.md` immediately and update it after meaningful findings and decisions and before any handover: what was verified, when, what changed in the plan, unresolved questions, next steps and exact supporting files. Keep a concise index so another model can continue without rereading our full history. Do not modify the bots' plans, launch packets, shared coordinator status, `CONTINUE_HERE.md`, active remote-access setup or other agents' files.

Use repository documents as the planning source of truth. Prepare a Jira outbox with existing issue references and proposed updates, but **do not write Jira or claim tickets are complete**. Another session owns Jira writes. Preserve historical observations and distinguish them from current truth and design proposals. If working on a machine without these files, request or obtain an authorized copy of the specific evidence needed; do not treat absent local files as nonexistent projects or invent results.

## My goals and current direction

- Treat home infrastructure as its own project serving all my other projects.
- The **R730 is the eventual central hub for Kai and local bots**, in addition to appropriate storage/media services. **Windows is the fallback and available development/temporary execution host.** My Intel i9 Mac with 64 GB RAM and a 5700 XT eGPU is a **low-priority secondary fallback/staging host**: use it only where it serves a useful purpose now. My mobile Mac should not be required for an always-on service.
- My rack has an R730 with one 25-bay expansion and a T640 with two 25-bay expansions. Both servers should have real purposes; T640 as a dependable backup server is acceptable. Productive AI use could justify its power draw, but measure useful capacity and electricity rather than assuming it is efficient.
- The T640 Unraid address I supplied is `172.16.5.8`; its iDRAC address is `172.16.1.8`. Do not include credentials in plans, prompts, logs or repositories. Use established authenticated access when available; ask for only genuinely missing access.
- I want media/Plex, independent recoverable backups, useful local AI, sensible Docker placement and storage shares, Mac/Windows access, reliable networking and Apple Home integration. I want good use of 10 GbE and an evidence-based choice about SMB Multichannel, RSS and dual-port use.
- Eventually I may re-IP the whole house, including containers needing dedicated IPs. That migration is lower priority. Design stable names, paths and service boundaries now so urgent work does not need to be redone later.
- There are 75+ devices, mostly IoT. My original intent was default `192.x`; VLAN 101 management on `172.16.1.x` for UniFi and iDRAC; VLANs 102/103 for historical 10 Gb paths; 104/105 for printers, TVs, Apple TV and Xbox; 106 for Wi-Fi. Actual placement has drifted. These are owner descriptions, not verified current subnets or correct security boundaries.
- I want non-Apple devices, including eligible Ring devices, to work through the iOS Home app where supported. Compare native HomeKit/Matter, Home Assistant and Homebridge based on the actual devices and maintenance burden.
- Review the Docker apps I pinned on the R730: explain each and classify **needed, useful later, or remove from the shortlist**, with reasons and overlap. Pins are candidates, not installation authorization or an exhaustive list. Suggest useful missing services separately. If installation is later approved, use the **Unraid Apps / Community Applications catalog**, as I requested, while checking the actual template/image maintainer and upstream provenance; presence in that catalog does not make an image vendor-official.
- I previously wanted a UniFi Docker container. First establish the authoritative controller already in use. Do not create a second active controller or change adoption merely to perform an audit.

## Read these existing records before starting another audit

All paths below are relative to the Astra repository. Read the summaries first and fetch raw evidence only for a specific unresolved question. They are dated records, not proof of the current condition.

1. `planning/infrastructure-2026-09-06/CURRENT_EVIDENCE_INDEX.md`
2. `planning/infrastructure-2026-09-06/CURRENT_SERVER_AUDIT.md`
3. `planning/infrastructure-2026-09-06/CURRENT_NETWORK_AUDIT.md`
4. `planning/infrastructure-2026-09-07/T640_READ_RECEIPT.json` and **`T640_START_RECEIPT.json`** in the same directory.
5. `planning/infrastructure-2026-09-06/PINNED_APPS_REVIEW.md` and `PINNED_APPS_INVENTORY.csv`.
6. `planning/parallel-projects-2026-09-06/unraid/UNRAID_SETUP_PLAN.md` as a prior proposal to improve, not a rule that overrides this request.
7. `planning/infrastructure-2026-09-06/t640-followup/T640_IDRAC_AUDIT.md` only for the hardware/boot history and unresolved questions it identifies.

Pay attention to chronology. The September 6 audit reported a T640 POST halt, but the September 7 start receipt records an authorized successful array start, online `app_cache`/`nvme_cache`, a degraded `media_pool`, and legacy SSD pools not imported. It explicitly does **not** prove a backup job started. Do not repeat the old “cannot boot” statement as current truth. Similarly, my recollection of 15 TB drives and 2 TB SSD space must be reconciled to exact current disk identities and pool topology before any capacity or backup promise.

The older R730 record contains a degraded backup pool, an almost-full SSD pool and unresolved SMART alerts; refresh these efficiently before designing heavy writes or calling storage protected. The network record identifies an existing UDM Pro controller. Reuse that evidence and verify changes instead of assuming we need a new controller. Do not report any old IP, client count, link speed, software version or health state as newly measured.

## What I want you to produce

1. **Current truth and unknowns:** physical server/shelf/HBA/NIC/GPU inventory; usable memory/compute; firmware and boot issues; UPS/power and recovery; exact pool topology, filesystem, capacity, free space and health; controller, switches, APs, wiring, link negotiation, VLANs, DHCP/DNS, firewall rules, discovery and remote management. Label owner report, dated evidence, fresh observation and unknown separately.
2. **Server and service placement:** a clear role for each server and fallback, with appdata/configuration/model/cache/media/backup placement, CPU/GPU/RAM capacity assumptions, contention limits, failure domains and migration order. Avoid forcing every workload onto one machine simply because it can run there.
3. **Storage/share plan:** source of truth, pool/dataset/share paths, ownership and permissions, Docker mounts, Mac/Windows protocols, snapshots/retention, independent backups and restoration tests. Distinguish sync, replication, snapshots and backup. Map media/download paths so hardlinks or atomic moves work where the actual filesystem supports them. For code, evaluate independent local Git clones plus a shared artifact/backup store; do not default to concurrent agents editing one shared working tree over SMB.
4. **Network plan:** physical topology, sensible segmentation, management recovery, addressing/DHCP reservations, local DNS, required cross-VLAN flows and Apple discovery, Wi-Fi/IoT compatibility and remote access. Explain the tradeoffs among bridge/host/dedicated-IP container networking based on actual Unraid support. Keep a service-to-port-to-data-access matrix. Do not expose administration or SMB directly to the public internet.
5. **Measured 10 GbE plan:** establish one correct path first, then assess SMB Multichannel and RSS on the actual server/client versions and adapters. Compare independent NICs and bonding/LACP without promising that a single transfer automatically exceeds 10 Gb/s. Include switch/cabling/client/storage bottlenecks, baseline and repeatable benchmarks, validation, failure tests and rollback; jumbo frames are a choice to justify, not a default requirement.
6. **Docker and integration catalog:** the refreshed pinned-app review; a minimal initial stack; useful optional additions; dependencies, duplicate functions, update/backup/restore needs and estimated ongoing operating costs. Cover Plex and media management, AI inference and bot hosting, monitoring, Home Assistant/Homebridge/Matter, authentication/secrets and protected backup services. Keep Kai/Pri/product behavior implementation with the bots session; define infrastructure interfaces only.
7. **Prioritized implementation backlog:** quick useful work, health/recovery prerequisites, near-term foundation, later house re-IP and optional optimization. Each task needs its goal, evidence/dependencies, suggested owner/host, estimated effort range with assumptions, downtime/data implications, validation and rollback. Link existing Jira identities where verified; propose new work without inventing issue keys. Do not overwrite original estimates or fabricate actual effort, dates, performance or completion.

Start by explaining what the existing records already tell us, what is stale, and the few decisions that materially affect the design. Continue independent planning while awaiting answers. Ask for a targeted photo, cable/port identification or login only when the evidence cannot answer it. Research current official vendor documentation when a compatibility or design recommendation depends on it, and cite it. Do not perform blanket repeated scans or consume model tokens polling unchanged state.

We are finished with this planning phase when I can understand the recommended design, choose between the meaningful alternatives and hand a complete staged task list to implementers. Give me the first safe implementation wave to review; do not quietly turn this planning session into server or network changes.
